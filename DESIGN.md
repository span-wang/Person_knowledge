# 知识闪卡复习工具：前端设计规范

## 1. 设计定位

前端采用 iOS 式设计语言：清晰层级、内容优先、轻量分组、直接操作、克制动效。参考 iOS 的交互原则和视觉秩序，不机械复制系统应用，也不把 Web 页面做成营销页。

整体气质：安静、专注、可靠、轻盈。界面服务于反复阅读和快速操作，不能用大段说明文字、装饰性数据或多层卡片填满页面。

## 2. 核心原则

1. 内容先于容器：闪卡正文是视觉中心，界面装饰主动退后。
2. 操作就近出现：高亮、挖空、状态和 AI 操作靠近当前内容。
3. 渐进展示：低频功能放入菜单、Sheet 或设置页。
4. 一屏一个重点：每个页面只有一个清晰主任务。
5. 状态始终可见：保存、筛选、同步、AI 生成和错误状态要有明确反馈。
6. 不写说明书：主界面不使用功能介绍段落，不用卡片解释“这个功能有什么用”。

## 3. iOS 风格基线

### 3.1 页面层级

- 顶级页面使用大标题，例如“复习”“资料”“设置”。
- 闪卡页使用紧凑导航栏，避免大标题挤占阅读空间。
- 次级操作通过底部 Sheet、上下文菜单或分组列表展开。
- 删除、覆盖和恢复等高风险操作使用确认 Sheet。
- Toast 只反馈短结果，不承载说明性内容。

### 3.2 容器

- 页面背景使用系统分组背景，内容区使用白色或深色表面。
- 卡片圆角不超过 8px。
- Sheet 和对话框可以使用 12-16px 圆角。
- 主要依靠间距、分组和分隔线建立层级，不堆叠阴影。
- 禁止卡片套卡片、彩色左边框卡片和大面积毛玻璃。
- 模糊效果只允许用于顶部栏、底部栏和 Sheet 背景。

### 3.3 颜色

浅色主题：

| Token | 色值 | 用途 |
|---|---|---|
| `background` | `#F2F2F7` | 页面背景 |
| `surface` | `#FFFFFF` | 正文、列表、Sheet |
| `text-primary` | `#111111` | 主要文字 |
| `text-secondary` | `#6C6C70` | 元数据和辅助文字 |
| `separator` | `rgba(60, 60, 67, 0.18)` | 分隔线 |
| `accent` | `#007AFF` | 链接和主操作 |
| `mastered` | `#34C759` | 掌握 |
| `familiar` | `#FF9F0A` | 了解 |
| `effort` | `#FF3B30` | 努力 |
| `highlight` | `#FFF2A8` | 阅读高亮 |
| `mask` | `#D1D1D6` | 挖空遮罩 |

深色主题：

| Token | 色值 | 用途 |
|---|---|---|
| `background` | `#000000` | 页面背景 |
| `surface` | `#1C1C1E` | 正文和列表 |
| `surface-raised` | `#2C2C2E` | Sheet 和浮层 |
| `text-primary` | `#F2F2F7` | 主要文字 |
| `text-secondary` | `#98989D` | 元数据和辅助文字 |
| `separator` | `rgba(84, 84, 88, 0.65)` | 分隔线 |
| `accent` | `#0A84FF` | 链接和主操作 |
| `mastered` | `#30D158` | 掌握 |
| `familiar` | `#FF9F0A` | 了解 |
| `effort` | `#FF453A` | 努力 |
| `highlight` | `#4B4530` | 阅读高亮 |
| `mask` | `#48484A` | 挖空遮罩 |

蓝色只用于交互，不让全界面变成单一蓝色主题。掌握状态使用绿、橙、红三组语义色。

### 3.4 皮肤

- “设置 > 外观”提供 `简约` 与 `秋日枫叶` 两项皮肤，以及 `跟随系统`、`浅色`、`深色` 三档显示模式，选择仅保存在本机；默认跟随系统。
- 秋日枫叶将枫红、银杏金与苔绿限制为小面积环境和交互细节色；页面背景、导航栏、分组列表、Sheet 与表单可以使用主题 token。
- 闪卡标题和正文必须保持白色或深色纯净表面、高对比文字和稳定宽度；叶片、叶脉等装饰不得进入正文区、遮挡公式或改变内容行高。
- 枫叶装饰只能是静态、低对比度的边缘细节，不使用铺满页面的图案、飘落叶片或背景循环动画。

## 4. 字体与排版

优先字体：`-apple-system`、`BlinkMacSystemFont`、`PingFang SC`、`Segoe UI`、sans-serif。

| 层级 | 字号 | 字重 | 用途 |
|---|---:|---:|---|
| 页面标题 | 28px | 700 | 顶级页面 |
| 卡片标题 | 20px | 650 | 知识点名称 |
| 正文 | 17px | 400 | 闪卡内容 |
| 控件文字 | 16px | 500 | 按钮、分段控件 |
| 辅助文字 | 15px | 400 | 章节、筛选摘要 |
| 元数据 | 13px | 400 | 序号、时间、状态 |

- 中文正文行高为 1.55-1.7。
- 字间距固定为 0。
- 不随视口宽度连续缩放字号。
- 大标题只用于顶级页面，不用于卡片和设置分组。
- 正文宽度限制在适合阅读的范围，桌面端不横向铺满。

## 5. 文案规则

### 5.1 强制规则

- 主界面禁止出现产品介绍、功能介绍和操作教程段落。
- 按钮文字原则上不超过 4 个汉字。
- 页面副标题最多一行，原则上不超过 20 个汉字。
- 空状态最多一句话，不超过 18 个汉字。
- 错误信息先说结果，再说原因；最多两句。
- 复杂说明只放在帮助页、确认弹窗或错误详情中。
- 图标能明确表达动作时使用图标；陌生图标必须提供 Tooltip 或无障碍标签。

### 5.2 标准按钮词表

| 场景 | 使用 | 禁止使用 |
|---|---|---|
| 导航 | `返回`、`上一张`、`下一张` | `点击这里返回上一页面` |
| 复习 | `开始`、`继续`、`挖空`、`显示全部` | `开始进行知识点复习` |
| 状态 | `掌握`、`了解`、`努力` | `我已经完全掌握这个知识点` |
| 内容 | `高亮`、`取消高亮`、`编辑`、`应用` | `对当前选中文字进行高亮` |
| AI | `AI 讲解`、`生成`、`重试`、`删除` | `让 AI 对当前知识点进行详细讲解` |
| 资料 | `导入`、`导出`、`新增`、`移动`、`删除` | `从本地选择并导入资料` |
| 设置 | `保存`、`测试连接`、`立即备份`、`恢复` | `保存当前所有配置修改` |

### 5.3 禁止文案

- “欢迎使用……”
- “在这里你可以……”
- “点击下方按钮即可……”
- “让 AI 帮助你更好地理解……”
- 重复页面标题含义的说明文字。
- 按钮旁再次解释同一按钮作用的描述。

## 6. 导航结构

主导航只保留四个入口：

- `复习`
- `资料`
- `导入`
- `设置`

iPhone 使用底部 Tab Bar。桌面端使用窄侧栏，图标和文字保持一致。低频入口放入对应页面，不增加第五个“更多”主导航。

复习页顶部使用紧凑分段控件在 `闪卡` 与 `刷题` 间切换；科目页顶部使用 `资料` 与 `题库` 分段控件。题库页内固定显示 `章节题`、`真题`、`模拟题` 三个入口，不新增主导航。

## 7. 页面规范

### 7.1 复习面板

- 标题：`复习`
- 首要入口：`资料`
- 快捷入口：`全部`、`未评估`、`努力`
- 数据只显示有行动价值的数量，不展示趋势图和装饰性统计。
- “资料”使用紧凑列表，每项显示资料名称、闪卡数量和可继续的闪卡标题，不使用大面积推荐卡。
- 不显示欢迎语和功能说明。

### 7.2 闪卡页

顶部栏：返回、资料/章节上下文、更多菜单。

正文区：

- 卡片标题清晰但不过大。
- 正文默认完整展示。
- 表格可横向滚动。
- 编辑态表格点击单元格后显示轻量选中边界；行操作和合并/拆分操作放在表格下方紧凑工具区，空白单元格保持可直接输入。
- 公式保持完整，不与正文重叠。
- AI 讲解是正文下方的折叠区，默认只显示标题和展开图标。
- 秋日枫叶皮肤只作用于正文外的环境层；正文区不出现叶片、叶脉或彩色底纹。

底部栏：

- 上一张、当前进度、下一张保持稳定位置。
- 掌握状态使用三段式控件：`掌握`、`了解`、`努力`。
- `挖空` 和 `AI 讲解` 放在次级工具区，不与三种状态争夺主层级。

### 7.2.1 刷题

- 刷题开始页使用 `背题`、`检测` 分段控件，以及全部/章节范围选择；真题和模拟题不显示章节范围。
- 单选题和判断题在背题模式中点击选项即提交；多选题通过 `确认` 提交。提交后正确选项为绿色、用户误选项为红色，其他选项保持中性。
- 检测模式作答时不显示红绿、正确答案、解析或知识点；单选与判断点击后自动下一题，多选确认后自动下一题。返回已答题时，用户可修改答案。
- 检测结果页显示正确、错误和未作答数量及正确率；错题列表行主体打开题目详情，详情可查看答案、解析、知识点和 AI 讲解。
- 题库详情按题库信息、开始刷题、统计、章节/题目、作答记录的顺序组织。统计使用紧凑分组列表展示作答数、正确数、错误数、正确率和最近完成时间；章节题显示章节统计，真题和模拟题不显示虚构章节。
- 题目编辑使用可视化编辑器，题干、每个选项、解析均不显示 Markdown 源码。选项按实际内容动态增删，最多六项；新增选项必须填写，删除选项后答案仍须有效。
- 题目 AI 讲解在结果详情的折叠区显示，旧版本保留在历史列表；题目变化后旧版本显示“题目已更新，讲解可能过时”。

### 7.3 筛选

- iPhone 使用底部 Sheet。
- 桌面端使用侧栏或弹出层。
- 资料使用列表选择，不提供章、节或关键词选择。
- 掌握状态使用多选项。
- 底部按钮只使用 `重置` 和 `应用`。
- 不在筛选项下面放解释文字。

### 7.4 导入

- 页面标题：`导入`
- 内容类型通过紧凑分段控件选择 `闪卡` 或 `题目`；闪卡文件类型选择 `Markdown`、`JSON` 或 `Excel`，题目文件类型选择 `JSON` 或 `Excel`；主按钮：`选择文件`。
- 题目导入先使用分段控件选择 `章节题`、`真题` 或 `模拟题`，再选择既有课程和科目；当前类型的 JSON 和 Excel 分段项旁提供下载图标，Tooltip 和无障碍名称为 `下载模板`；模板下载不弹确认框。
- 解析后直接展示层级预览和错误数量。
- 错误列表显示位置、原因和修复建议，不使用泛化错误。
- 每条可修正的正文 Markdown、GFM 表格或 LaTeX 格式错误右侧显示带 Tooltip 的 `优化格式` 操作；一次只把该条错误所在闪卡的 `body` 正文发送给 AI，可并发处理多条。JSON 语法或协议错误不显示该操作，AI 不得改写正文文字。
- 主要动作：`应用`
- 次要动作：`取消`
- 不展示格式教程；字段说明与来源限制通过独立 `格式说明` Sheet 打开。

### 7.5 资料管理

- 资料入口按 `课程 → 科目 → 资料` 逐层进入；课程和科目使用单列 iOS 分组列表，行主体进入下一层，右侧 `更多` 菜单提供低频操作。
- 课程支持新增、改名、排序和删除；科目支持新增、改名、排序、移动和删除；非空课程/科目不可直接删除，必须先移动或清空。
- 科目页使用响应式九宫格展示整套资料：桌面端 3 列、iPhone 端 2 列，资料数量超过 9 个继续向下排列，不分页、不横向滚动。
- 资料格只显示可选封面、资料名称和有效闪卡数量。无封面时封面区域透明，不生成首字、图标或背景占位；资料名称最多两行，完整名称在详情页可见。
- 资料详情页使用单列内容流，顺序为资料信息、四状态横向堆叠分布、最近 30 天状态快照趋势、章/节和单张闪卡列表。趋势按 `Asia/Shanghai` 的每日结束状态计算，四种状态为 `掌握`、`了解`、`努力`、`未评估`。
- 资料可在详情页 Sheet 中编辑名称、上传/替换/移除封面；封面只支持本地 PNG/JPEG/GIF/WebP，最大 5MB，保存原图并生成缩略图。新增资料仍只通过导入产生。
- 左滑或上下文菜单提供 `移动`、`删除`；新增使用导航栏加号图标，Tooltip/无障碍名称为 `新增`；编辑正文在原位置进入可视化状态，操作只保留 `取消`、`应用`。

### 7.6 设置

使用 iOS 分组列表，不使用 Dashboard 卡片。

分组：

- `外观`
- `AI`
- `存储`
- `备份`
- `账号`
- `关于`

“外观”分组提供 `简约`、`秋日枫叶` 皮肤选择和 `跟随系统`、`浅色`、`深色` 显示模式分段控件，不额外放功能说明文字。

布尔设置使用 Switch；模式使用分段控件；Provider 和模型使用选择列表；路径和密钥使用表单行。

## 8. 控件规范

- 触控目标不小于 44 x 44px。
- 图标使用 Lucide，保持统一线宽。
- 上一张/下一张优先使用箭头图标，提供无障碍名称。
- 二元设置使用 Switch，不用文字按钮模拟开关。
- 掌握状态使用分段控件，不使用三个独立大按钮。
- 选项集合使用列表、菜单或 Sheet。
- 数值设置使用步进器、滑杆或输入框。
- 高风险动作使用红色文字，不使用大面积红底按钮。

## 9. 高亮与挖空

- 选中文字后显示紧凑浮动菜单，只含 `高亮` 或 `取消高亮`。
- 浮动菜单不能遮挡当前选区。
- 高亮颜色只有一种。
- 挖空态按原文字数保留占位。简约皮肤使用中性灰色遮罩；秋日枫叶皮肤使用低饱和暖灰底、1px 枫棕描边和极淡叶脉纹理，短词不使用纹理。
- 点击遮罩只揭示当前项。
- `显示全部` 放在正文工具栏，不在每个遮罩旁重复出现。
- 公式以完整元素遮罩；图片不提供遮罩操作。

## 10. 动效与反馈

- 页面切换和 Sheet 使用 180-260ms 动效。
- 遮罩揭示使用 120-180ms 淡入。
- 除 AI 生成进度指示外，不使用循环动画、漂浮装饰和按钮弹跳。
- 保存成功不弹阻塞弹窗，只显示短暂状态。
- AI 生成使用无文字的主题化不定进度环和独立停止图标，不编造百分比；停止必须真实取消当前请求，保留旧讲解且不写入半成品。
- Reduce Motion 开启时取消位移动画，只保留透明度变化。
- 遮罩揭示在 120-180ms 内以透明度变化显示正文，不位移或缩放卡片内容。

## 11. 响应式规则

### iPhone

- 单列布局。
- 底部栏使用 `env(safe-area-inset-bottom)`。
- 不使用左右滑动切换闪卡，避免和选词、表格冲突。
- Sheet 不超过可视高度，内容可滚动，主操作固定在底部。

### 桌面

- 最大正文宽度建议 760-860px。
- 资料树或筛选栏可固定在左侧。
- 底部操作仍保持与移动端相同顺序。
- 不把移动端页面简单拉宽成全屏卡片。

## 12. 无障碍与验收

- 所有图标按钮必须有可访问名称。
- 键盘焦点清晰可见。
- 状态不能只依赖颜色。
- 正文、辅助文字和禁用态满足可读对比度。
- 动态提示使用适当的 ARIA live 区域。
- 无文字的生成进度和停止图标必须分别提供可访问名称；减少动态效果时进度环不得旋转。
- iPhone Safari、Windows Chrome 和 Edge 是首版验收环境。
- 交付前检查按钮文案长度、页面描述文字数量、文本溢出和底部安全区域。

## 13. 前端验收红线

出现以下任一情况不得验收：

- 主页面出现功能介绍长段落。
- 一个按钮包含完整句子。
- 页面使用超过两层卡片容器。
- 复习正文被大标题、统计或装饰抢占首屏。
- 前端出现 Markdown 或 LaTeX 源码编辑态。
- 手机端底部按钮覆盖正文或 Home Indicator。
- 文字、公式、表格或按钮发生重叠。
