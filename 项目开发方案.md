# 知识闪卡复习工具：项目开发方案

## 1. 技术基线

| 层级 | 方案 | 责任 |
|---|---|---|
| 前端 | React + TypeScript + Vite | 响应式 Web、复习面板、卡片渲染、可视化编辑 |
| 文档编辑 | Tiptap/ProseMirror 文档模型 | 表格、公式、图片、文本高亮和可视化编辑 |
| 后端 | Node.js + TypeScript | 导入解析、业务 API、权限、AI 请求、备份任务 |
| 数据库 | MySQL 8.x | 资料与题库层级、结构化正文、标注、状态、作答记录和 AI 讲解 |
| 内容解析 | unified/remark/rehype、GFM、KaTeX、xlsx-populate | Markdown、内容级 JSON 和 Excel 一次性导入，以及公式和表格解析 |
| 资源存储 | 用户指定的 Windows 文件目录 | 图片资源、备份和导出文件 |
| 外网入口 | `cloudflared tunnel` | HTTPS 与 `review.panspan.cloud` 转发 |
| 运行方式 | Windows 原生项目包 + 固定 Node.js 运行时 | 一键启动和一键关闭，不依赖 Docker |

## 2. 数据边界

Markdown、内容级 JSON 和 Excel 是导入边界，不是长期真相源。导入后将内容解析为结构化文档 JSON，正文、层级和用户编辑全部保存到 MySQL；原 Markdown、ZIP、JSON 与 Excel 源文件不修改、不参与后续同步。

图片复制到资源目录，数据库保存资源 ID、相对路径、尺寸和校验信息。资料可以关联独立封面资源，保留原图并生成最长边 512px 的受管缩略图；无封面时资料格保留但封面区域透明。JSON 备份包含结构化正文、课程/科目归属、资料封面、图片元数据、标注、当前状态、掌握状态历史、AI 讲解、复习记录、题库、题目、题目 AI 讲解版本、作答会话、作答快照和应用设置，但不包含 API Key。Markdown 导出只包含资料正文层级，不写入课程、科目、封面或统计历史。

题目 JSON 和 Excel 同样只承担一次性导入，不参与同步；题目、选项、标准答案、解析、知识点、排序、AI 讲解和作答历史均在 MySQL 中长期保存。题干、选项与解析导入后转换为现有安全结构化内容节点，应用内仅使用可视化编辑，不显示 Markdown 或 LaTex 源码。题目首版不导入图片或外部资源。

## 3. 核心实体

- `materials`：资料名称、来源文件摘要、导入时间、软删除状态。
- `courses`：课程大类名称、排序和软删除状态；课程只保留一层。
- `subjects`：课程下的科目名称、排序和软删除状态；科目只能归属一个课程但可移动。
- `material_covers`：资料封面原图、缩略图、尺寸、校验信息和资源引用关系。
- `chapters`：资料下的章，保存顺序。
- `sections`：章下的节，保存顺序。
- `cards`：节下的闪卡，保存结构化正文、状态、排序和软删除状态。
- `highlights`：卡片内的文本/公式高亮范围和节点定位。
- `ai_explanations`：每张卡片当前唯一的 AI 讲解、提示词、Provider、模型和生成时间。
- `review_records`：首次查看、最近查看、状态变更时间和查看次数。
- `review_status_history`：闪卡状态变更前后值和时间，用于按 Asia/Shanghai 计算资料最近 30 天四状态快照。
- `ai_provider_profiles`：多套 Provider、接口地址、模型和加密后的密钥引用。
- `sync_locks`：卡片正文编辑锁，支持多设备同时打开。
- `backup_records`：备份时间、目录、文件清单和恢复结果。
- `trash_items`：资料、章、节、卡片的可恢复删除记录。
- `question_banks`：科目下的题库名称、入口类型（章节题、真题、模拟题）、排序和软删除状态。
- `question_chapters`：仅章节题使用的章节名称、排序和软删除状态。
- `questions`：题库或章节下的题干、题型、结构化选项、标准答案、解析、知识点、排序和软删除状态。
- `question_ai_explanations`：题目 AI 讲解版本、实际 Provider、模型、提示词、题目版本标识和生成时间。
- `practice_sessions`：背题/检测、整套/本次错题/累计错题来源、题库范围、进行中或已完成状态和时间。
- `practice_attempts`：会话内每题最终答案、判题结果、题目快照和章节快照；题目编辑、移动或删除后仍按当时版本展示历史。

## 4. 导入协议

支持单个 `.md`、单个 ZIP 资料包、内容级 `.json` 或单个 `.xlsx` 资料文件。ZIP 必须包含一个 Markdown 文件和可被相对路径引用的图片资源。

标题层级固定为：`#` 资料、`##` 章、`###` 节、`####` 闪卡。空标题、缺失层级和无法归属正文属于结构错误；错误必须显示文件名、行号、章/节/卡片和修复建议后才能应用。

内容级 JSON 固定为 `format: "knowledge-flashcards-material"`、`version: 1`、`title` 和嵌套的 `chapters → sections → cards`；每张卡片包含 `title` 与 Markdown 正文 `body`，可选 `highlights` 数组。文本高亮使用 `{ "text": "目标文本", "occurrence": 1 }`，公式高亮使用 `{ "formula": "F = ma", "occurrence": 1 }`；`occurrence` 表示同一目标在当前卡片完整 `body` 中按正文从前到后出现的序号，从 1 开始，不是 highlights 数组下标；计数跨普通文本、粗体等节点，省略时目标必须在当前卡片 body 中唯一匹配，重复时不会隐式选择第一项。单条 `text` 仍不能跨结构化节点，导入时会解析为当前节点路径和字符范围；找不到、重复不明确或正文修正后不再匹配时阻止应用。Excel 只读取名为“闪卡”的单个工作表，固定列为“资料、章、节、闪卡、正文”，每行代表一张闪卡。JSON 与 Excel 正文沿用 Markdown/GFM/公式解析；本步不导入资源文件、课程科目归属、学习状态、AI 讲解、备份或密钥。导入页可下载 JSON 和 Excel 模板。

解析结果先进入导入预览，用户可以编辑卡片标题和正文、处理未归属内容；应用前必须选择一个课程和一个科目，也可在选择器中新增。相同文件通过哈希在未删除资料范围内全局检测，默认跳过；回收箱中的资料不参与去重。需要换归属时移动已有资料，不重复导入。

题库导入仅支持单个 `.json` 或单个 `.xlsx`。导入页先选择既有课程、科目，以及“章节题”“真题”或“模拟题”入口，不在导入页隐式创建课程或科目。章节题 JSON 使用 `format: "knowledge-flashcards-question-bank"`、`version: 1`、根级 `title` 和 `chapters[]`，每章含 `title` 与 `questions[]`；真题和模拟题 JSON 使用相同根级标识与 `title`，但只含根级 `questions[]`。每题固定字段为 `stem`、`type`、`options`、`answer`、`analysis`、`knowledgePoints`；`type` 仅允许 `single`、`multiple`、`true_false`，`options` 为从 `A` 连续到 `F` 的对象映射，`single` 和 `true_false` 的 `answer` 是单个字母，`multiple` 的 `answer` 是至少两个字母的数组。导入时去除空格、统一字母大写、去重排序并校验答案与选项完全对应；判断题严格为 `A: 对`、`B: 错`。

章节题 Excel 固定工作表名为“题目”，列为“题库、章节、题干、题型、选项A、选项B、选项C、选项D、选项E、选项F、答案、解析、知识点”；真题/模拟题使用同名工作表但移除“章节”列。题库列在同一文件中必须一致；章节题的章节不得为空，真题与模拟题不设置章节。题干、题型、至少两个实际选项和答案必填；解析、知识点可空。知识点在 Excel 中使用英文逗号分隔，在 JSON 中使用字符串数组，均会去空、修剪和去重。导入预览必须显示定位明确的字段错误，重复题库名不得应用；下载模板随当前入口切换。

课程、科目和资料目录采用逐层导航：资料 → 课程 → 科目 → 资料九宫格 → 资料详情 → 章/节/单张闪卡。课程和科目使用单列列表，资料九宫格桌面端 3 列、移动端 2 列；资料详情页使用单列，显示封面、四状态分布横向堆叠条和最近 30 天每日状态快照趋势，随后显示章、节和单卡列表。

## 5. 复习主链路

1. 打开复习面板，选择资料或掌握状态；进入某份资料时直接恢复该资料保存的最后闪卡。
2. 使用资料和掌握状态组合筛选。
3. 在当前结果集中按原始顺序查看卡片。
4. 阅读完整正文，必要时进入重点挖空模式。
5. 点击高亮遮罩逐项揭示重点。
6. 手动选择“掌握 / 了解 / 继续努力”，即时保存。
7. 需要补充时点击 AI 讲解；结果默认收起并持久化。

## 5.1 刷题主链路

1. 在复习页的“闪卡 / 刷题”分段中进入刷题；在科目页的“资料 / 题库”分段中管理题库，题库再固定分为章节题、真题、模拟题三个入口。
2. 章节题可选择全部或一个章节；真题和模拟题只按整套题库出题。出题顺序固定为题库排序，首版不随机出题。
3. 背题模式中，单选/判断点击选项即判题并显示绿色正确项、红色误选项、答案、解析、知识点和 AI 讲解；多选题选择完后点击“确认”再判题。单选、多选、判断均以答案集合完全匹配判定正确。
4. 检测模式在作答过程中只显示中性选中状态并隐藏答案、解析与对错；单选/判断点击即记录并自动进入下一题，多选点击“确认”后进入下一题。用户可前后导航并修改已答题，最终答案为准。
5. 最后一题后进入结果页，显示总题数、已作答、未作答、正确数、错误数和正确率；错题可以查看完整解析与 AI 讲解。
6. 未完成会话自动保存为进行中，下次提供继续或放弃；只有完成会话进入统计。错题专项支持本次错题和累计错题，累计错题定义为最近一次有效作答为错误的题目，专项答对后立即移出该范围。

## 6. 标注模型

普通文本和表格文字支持任意范围高亮；公式以完整公式为单位高亮；图片不支持标注。阅读态高亮使用单一颜色，挖空态按原内容长度保留占位。秋日枫叶皮肤下，遮罩使用低饱和暖灰底、枫棕细描边和极淡叶脉纹理；短词不使用图案，避免影响文字长度判断和行高。点击单项遮罩仅以短暂淡入显示原文，揭示状态只在当前卡片会话中有效，离开卡片后重置。

## 7. AI 链路

设置页保存多套 Provider 配置，并选择一套当前启用配置。卡片页不选择模型，只使用当前配置。用户提示词可选且仅本次生效；未填写时使用系统默认讲解模板。

AI 请求只由后端发送，API Key 不下发浏览器。AI 输出安全渲染，支持文本、列表、表格、公式和代码；每张卡片只保存当前一条讲解，重新生成覆盖旧内容。题目 AI 讲解可带可选临时提示词，每次只发送当前题目的题干、选项、标准答案、用户最终答案、解析、知识点和模式；答错时解释错误原因，未作答时说明解题思路。题目讲解每次生成均保留版本历史；题目编辑后旧版本不删除，但须标注“题目已更新，讲解可能过时”。生成中的取消操作必须将取消信号传递至后端及当前 Provider 请求；取消后不写入半成品，并保留已有讲解。

## 8. 备份与迁移

每天自动生成 MySQL 逻辑备份和资源目录归档，默认保留 7 份，备份目录由用户指定。支持手动备份、恢复前自动留档和完整恢复。新建课程/科目、资料封面、掌握状态历史、题库、章节、题目、题目 AI 讲解版本和作答会话/快照必须随 JSON/备份恢复；旧版仅含闪卡的 JSON 备份继续兼容恢复。新 JSON 备份升级版本号，不包含 API Key。旧 JSON 缺少课程/科目字段时归入不可删除的“待整理 / 待整理”。

Markdown 导出只导出资料层级和当前正文；JSON 导出保存完整学习状态。导出内容不写回源文件。

## 9. 部署与启动

- `start.ps1`：检查 MySQL、读取本机配置、启动后端、启动前端服务和 `cloudflared`。
- `stop.ps1`：只停止本项目记录的进程，不关闭 MySQL。
- 项目目录保留 `cloudflared` 配置和凭证路径，但凭证不提交版本库。
- 启动脚本输出本地地址、`review.panspan.cloud` 和各服务状态，不自动打开浏览器。
- PWA 只提供“添加到 iPhone 主屏幕”，第一版必须联网。

## 10. 安全约束

- 单账号密码功能放在开发顺序最后，但在公网开放前必须完成。
- 密码使用安全哈希，登录失败限速，会话使用安全 Cookie。
- Markdown、ZIP、JSON、Excel、HTML、图片和 AI 输出都要做路径、脚本和资源安全校验。
- ZIP 解压必须防止路径穿越；资源只能写入用户指定目录。
- Cloudflare Tunnel 凭证、MySQL 密码和 AI Key 不进入 Markdown 或 JSON 导出。

## 11. 测试策略

- 导入解析：标题层级、未归属正文、重复文件、ZIP 路径、JSON/Excel 模板结构、公式、表格和图片。
- 资料目录：课程/科目逐层导航、单列列表、资料九宫格、封面上传/透明无封面、移动/排序/软删除、旧数据默认归属和失效 URL。
- 复习链路：资料与状态筛选、首尾切换、每份资料独立进度、状态即时保存和备份恢复。
- 学习统计：四种状态当前分布、状态变更历史、Asia/Shanghai 最近 30 天每日快照、无历史和无闪卡空状态。
- 标注链路：多高亮、表格文字、整段公式、遮罩重置。
- 编辑链路：可视化保存、编辑锁、资源替换和数据库恢复。
- AI 链路：多 Provider 配置、服务错误、空提示词、覆盖当前讲解。
- 题库导入：章节题与真题/模拟题 JSON、Excel 模板和预览，题型/连续选项/答案/判断题/空必填/重复题库校验，以及 Markdown、GFM 表格和公式结构化转换。
- 题库管理：课程→科目→三类入口、题库与章节排序/软删除/恢复、可视化题目编辑、选项动态增删和题型切换校验。
- 刷题与统计：背题/检测反馈隔离、会话续答、最终答案覆盖、题目快照、错题专项、题库/章节/题型/模式累计统计及无数据状态。
- 题目 AI：上下文最小化、临时提示词、取消、历史版本和题目更新过时标记。
- 外观与反馈：简约/秋日枫叶及跟随系统/浅色/深色显示模式切换与本机持久化、浅色/深色对比度、正文无干扰、AI 真实取消、减少动态效果和文本/公式挖空揭示。
- 运维链路：每日备份、恢复、启动/关闭脚本、Tunnel 状态和 iPhone Safari。

## 12. 外观皮肤与专注反馈

设置页“外观”提供“简约”和“秋日枫叶”两套皮肤，以及“跟随系统”“浅色”“深色”三档显示模式，选择均仅保存在本机。默认跟随系统；秋日枫叶根据显示模式提供浅色枫叶与深色夜枫。主题 token 覆盖页面背景、导航、分组列表、Sheet 和表单，但不改变既有功能信息架构。

秋日视觉仅用于页面环境层和细节层：可使用枫红、银杏金与苔绿作为低面积强调色，配合静态、低对比度的枫叶细节。闪卡标题与正文区域必须维持纯净、高对比、无叶片和无纹理的阅读表面，主题不得挤占正文宽度、改变卡片层级或引入循环背景动画。

AI 生成期间仅在操作位显示无文字的主题化不定进度环，并提供带无障碍名称的独立停止图标。开启减少动态效果时进度环保持静态；不得显示虚构百分比，不移动正文或闪卡容器。

## 13. 实施阶段

1. 工程基线与 MySQL 连接。
2. 单资料导入、严格校验和数据库写入。
3. 复习面板、筛选、切换、掌握状态和基础统计。
4. 高亮、挖空、公式/表格/图片渲染。
5. 可视化内容和层级编辑。
6. AI Provider、提示词和讲解保存。
7. 回收站、导出、恢复和每日备份。
8. PWA、Cloudflare Tunnel、双设备同步。
9. 单账号密码登录和公网发布验收。
10. 作为独立 P1 步骤实现全局秋日枫叶皮肤、AI 真实取消与无文字生成反馈、挖空遮罩视觉优化；不与其他功能扩展合并。
11. 作为独立 P1 步骤实现课程/科目/资料目录、资料封面和状态历史趋势；不改变 Markdown 标题协议，不把课程/科目加入复习筛选，不实现新建空白资料。
12. 作为独立 P1 步骤补齐本机深浅色显示模式切换；不新增皮肤、跨设备同步或账户级偏好。
13. 作为独立 P1 步骤补齐内容级 JSON 与 Excel 单资料导入及模板下载；保留 Markdown/ZIP 链路，不把完整 JSON 恢复、资源包、批量导入或学习状态迁移混入本步。
14. 作为独立阶段实现题库基础协议、数据库与备份迁移、章节题/真题/模拟题导入、题库管理、双模式刷题、统计错题专项和题目 AI 讲解历史；不实现题目与闪卡联动、题目图片导入、随机出题、部分得分或 AI 主观题评分。
