# 项目开发进度

> 本文件由 `progress/config.json`、`progress/tasks/*.json`、`progress/risks.json`、`progress/tech-debts.json` 和 `progress/logs.json` 自动生成。
>
> 请不要直接修改本文件；手工改动会在下次执行 `npm run progress:build` 时被覆盖。

## 1. 文档目的

本文件跟踪 **知识闪卡复习工具** 的阶段、里程碑、模块、任务、风险、技术债和每次可验证推进。

业务与工程基线：

- `项目产品定位.md`
- `项目开发方案.md`
- `DESIGN.md`
- `AGENTS.md`

## 2. 跟踪原则

1. 以可验证的完成结果作为进度单位，不以已经开始代替已经完成。
2. 每次只推进一个边界清楚的小步，并在完成后立即登记结构化进度源。
3. 用户确认下一步方案后，开始改代码前必须写明本步纳入、本步明确不纳入和后续归属任务。
4. 进度项必须关联阶段、里程碑、模块和验收标准，避免模糊描述。
5. 阻塞、返工和范围变化必须同步登记风险与日志。
6. 结构化 JSON 是唯一真相源，Markdown 进度文档只能由脚本生成。

## 3. 完成判定

- 有明确任务 ID 和实施对象。
- 有代码、文档、测试、配置或可运行流程等实际产出。
- 有自动化命令或人工验收结果作为验证证据。
- 任务卡、风险、技术债和日志已按实际情况同步。

## 4. 状态定义

| 状态 | 含义 | 使用规则 |
|---|---|---|
| `未开始` | 尚未投入实施 | 仅完成范围识别，尚无可验收产出。 |
| `进行中` | 已开始推进 | 已有实施动作，但尚未满足验收标准。 |
| `已完成` | 已达到验收标准 | 必须记录结果、验证方式和影响文件。 |
| `阻塞` | 当前无法继续 | 必须记录原因、影响范围和解除条件。 |
| `暂缓` | 当前版本不实施 | 必须记录暂缓依据和后续归属。 |

## 5. 阶段

| 阶段 | 目标 | 计划周期 | 状态 |
|---|---|---|---|
| 第零阶段：产品与治理基线 | 冻结产品定位、技术方案、设计规范和结构化进度跟踪规则 | 1-2 天 | `已完成` |
| 第一阶段：工程基础与资料导入 | 建立 TypeScript Web 工程、MySQL 数据层、Windows 运行脚本和单资料导入主链路 | 2-3 周 | `已完成` |
| 第二阶段：核心复习体验 | 完成复习面板、组合筛选、顺序切卡、掌握状态和内容渲染 | 2-3 周 | `已完成` |
| 第三阶段：标注与可视化编辑 | 完成高亮挖空、公式遮罩、可视化正文编辑、层级维护和多设备编辑锁 | 3-4 周 | `已完成` |
| 第四阶段：AI 与数据治理 | 完成多 Provider AI、讲解保存、回收站、导出恢复和每日备份 | 2-3 周 | `已完成` |
| 第五阶段：移动端与发布 | 完成 iPhone PWA、Cloudflare Tunnel、一键运行、账号保护和发布验收 | 2-3 周 | `进行中` |
| 第六阶段：题库与刷题 | 完成章节题、真题和模拟题的题库导入、管理、双模式刷题、统计、错题专项、AI 讲解与完整备份 | 4-6 周 | `进行中` |
| 第七阶段：检索、巩固与洞察 | 在发布与题库主链路稳定后，完成全局检索、错题巩固和学习洞察 | 3-5 周 | `进行中` |
| 第八阶段：文字与手写备注 | 为闪卡和题目提供可恢复的当前文字与手写备注，并在题目解析阶段呈现 | 1-2 周 | `已完成` |

## 6. 里程碑

| 里程碑 | 完成标准 | 状态 |
|---|---|---|
| `M0` M0 方案可执行 | 产品、开发、设计和进度治理基线完整且可校验 | `已完成` |
| `M1` M1 资料可导入 | 合法 Markdown/ZIP 可预览、校验并写入 MySQL | `已完成` |
| `M2` M2 可连续复习 | 组合筛选、顺序切卡、状态保存和内容渲染完整可用 | `已完成` |
| `M3` M3 可标注可编辑 | 高亮挖空、可视化编辑、层级维护和双设备编辑锁可用 | `已完成` |
| `M4` M4 AI 与数据可用 | AI 讲解、备份恢复、导出和回收站闭环可用 | `已完成` |
| `M5` M5 资料可组织 | 课程、科目、资料九宫格、封面和学习状态趋势可用 | `进行中` |
| `M6` M6 题库可刷可追溯 | 题库导入、题目管理、背题/检测、统计、错题专项、AI 题目讲解历史和备份恢复闭环可用 | `进行中` |
| `GA` GA 可安全发布 | iPhone PWA、Cloudflare Tunnel、登录保护和 Windows 发布包通过验收 | `进行中` |
| `M7` M7 学习效率可定位可复盘 | 全局检索、可备注错题巩固和基于既有记录的周期学习洞察通过数据、交互与备份验收 | `进行中` |
| `M8` M8 备注可随手记 | 闪卡和题目文字、手写备注可跨设备保存恢复，题目备注在解析阶段可见且不泄露检测答案 | `已完成` |

## 7. 模块

| 模块 | 能力层 | 状态 | 说明 |
|---|---|---|---|
| 项目治理 | 治理层 | `已完成` | 产品定位、开发方案、设计规范、Agent 约定和结构化进度跟踪基线已建立。 |
| 运行与配置 | 基础设施层 | `进行中` | TypeScript 工程、MySQL 连接、路径配置、Windows 启停脚本和固定 Node.js 运行时。 |
| 资料导入 | 内容治理层 | `进行中` | 单 Markdown/ZIP 解析与严格结构校验已完成，预览修正、资源复制和重复检测待后续任务。 |
| 复习体验 | 业务层 | `进行中` | 已完成按资料续读、资料与掌握状态筛选、顺序切换、掌握状态和复习记录；PH5-13 的背默模式及登录后浏览器回归仍在进行。 |
| 内容渲染 | 展示层 | `已完成` | 安全渲染结构化正文、表格、代码、数学公式和受管图片；移动端表格压缩换行且不产生横向滚动。 |
| 高亮与挖空 | 交互层 | `已完成` | 文本、表格和整段公式高亮，以及仅当前会话有效的重点挖空与揭示重置已完成；可视化编辑会安全清理可能漂移的高亮。 |
| 可视化编辑 | 编辑层 | `已完成` | 已完成不展示 Markdown/LaTeX 源码的结构化正文、表格、公式和受管图片编辑，以及章、节、闪卡的新增、移动、排序和软删除；正文编辑通过设备租约锁避免跨设备同时修改。 |
| AI 讲解 | 智能服务层 | `已完成` | 已完成多 Provider 配置、后端加密密钥存储、OpenAI 兼容请求适配、连接测试、默认/临时提示词、当前讲解持久化及真实环境组合验收。 |
| 数据治理 | 支撑层 | `进行中` | 已提供闪卡层级软删除、回收站、导出恢复、每日备份和恢复前留档；第六阶段将题库、题目 AI 讲解版本及作答快照纳入备份恢复，并兼容旧闪卡备份。 |
| 题库与题目 | 业务层 | `进行中` | 在课程、科目下分别管理章节题、真题和模拟题；覆盖严格导入、目录、章节、题目与结构化题目内容。 |
| 刷题与统计 | 交互层 | `进行中` | 提供背题、检测、会话续答、最终答案判定、错题专项和题库/章节/题型/模式统计。 |
| 题目 AI 讲解 | 智能服务层 | `进行中` | 复用当前启用 Provider，为单题提供带作答上下文的讲解、取消和持久化版本历史。 |
| 全局检索 | 业务层 | `已完成` | 已完成跨课程检索有效资料、闪卡和题目，并以受控摘要定位到具体内容。 |
| 错题巩固 | 业务层 | `已完成` | 以累计错题口径提供个人备注、维度筛选与复练入口，不改写作答快照。 |
| 学习洞察 | 业务层 | `进行中` | 已基于有效复习记录和已完成会话展示 7/30 天学习量、状态变化、正确率与薄弱知识点。 |
| 文字与手写备注 | 交互层 | `已完成` | 已为闪卡与题目保存当前文字和矢量笔迹备注，并在题目解析阶段显示；高级笔迹编辑能力另立任务。 |
| 发布与安全 | 交付层 | `进行中` | 账号密码门禁和 Windows 发布包已完成；iPhone PWA、Cloudflare Tunnel 与 GA 公网安全验收仍在进行。 |

## 8. 当前执行概览

- 当前阶段：第五阶段：移动端与发布（`进行中`）
- 当前里程碑：`M5` M5 资料可组织（`进行中`）

### 8.1 进行中或阻塞任务

| 任务 | 标题 | 优先级 | 状态 | 验收标准 |
|---|---|---|---|---|
| `PH5-01` | 完成 iPhone Safari 响应式与 PWA 主屏幕安装 | P0 | `进行中` | iPhone 可添加到主屏幕，选词、高亮、表格、公式和底部安全区域通过验收。 |
| `PH5-02` | 接入 Cloudflare Tunnel 与 review.panspan.cloud | P0 | `进行中` | 项目脚本可启动/关闭 cloudflared，域名 HTTPS 稳定访问且凭证不入库。 |
| `PH5-05` | 完成 GA 全链路验收 | P0 | `进行中` | Windows 本机、iPhone、MySQL、Tunnel、备份恢复和账号安全完整通过发布验收。 |
| `PH7-04` | 重构学习洞察统计图表 | P1 | `进行中` | 洞察面板能在 7 天和 30 天周期下清楚呈现每日复习量、四种状态变化、题库正确率和薄弱知识点；图表比例诚实、标签不重叠、表格可扫描，颜色不是唯一判断依据，空数据和移动端布局正确。 |
| `PH5-09` | 修复 AI 讲解 Markdown 渲染 | P1 | `进行中` | AI 讲解中的 Markdown 标题、强调、列表、分隔线、表格、公式和安全链接以只读结构化内容显示，不出现 Markdown 标记原文；兼容模型常见的 `**“强调文本”**` 与 `*“强调文本”*` 行内强调写法。 |
| `PH5-10` | 新增 JSON 与 Excel 资料导入及模板下载 | P1 | `进行中` | 用户能在导入页选择 Markdown、JSON 或 Excel 单文件，下载 JSON/Excel 模板，并将符合模板的资料预览、校验、重复检测后应用为层级闪卡。 |
| `PH5-13` | 按资料续读并简化复习资料选择 | P1 | `进行中` | 每份资料独立保存并恢复最后一张闪卡；当前学习资料选择器仅显示资料名称，并决定顶部统计与快捷开始范围；复习页筛选只保留资料和掌握状态，不再提供章节、节或关键词选择。 |
| `PH5-14` | 在资料详情提供逐卡内容编辑与排序 | P1 | `进行中` | 资料详情按章、节展示独立闪卡编辑卡；用户可在卡内可视化编辑正文，表格支持空白单元格输入、加删行、相邻单元格横纵合并与拆分，并可在同一节内上移或下移，刷新后顺序和表格跨度保持一致。 |
| `PH6-15` | 支持乱序、固定数量与未做筛选练习 | P1 | `进行中` | 用户可在题库、章节题和科目收藏题的背题/检测创建入口选择全部或固定题目数量、只练未做或不区分，并可开启乱序；服务端在创建会话时一次性筛选、抽取并固化题目集合和顺序，刷新、续答、完成、放弃及完整 JSON 导出恢复后保持一致；数量超过可用题目时按可用题目数练习，不改变判题、统计和检测答案隐藏语义。 |
| `PH6-16` | 在学习工作区提供悬浮问答助手 | P1 | `进行中` | 刷题与单张闪卡阅读页均提供默认收起的悬浮助手；展开后用户可围绕当前题目或当前闪卡进行流式问答，生成完成的回答可一键复制，桌面与 390px 移动端不遮挡正文、选项、键盘焦点或底部操作。检测模式未作答题仅允许概念性提示，服务端不得将答案、解析或知识点传给模型，也不得返回直接答案。 |
| `PH6-17` | 实现 AI Provider 优先级与失败自动降级 | P0 | `进行中` | 设置页可将多个 AI Provider 纳入候选池并调整优先级；任一 AI 请求会按启用顺序尝试渠道，首段有效输出前的网络、超时、Provider 状态或内容格式失败会自动切换下一渠道，全部失败时返回明确错误。流式回答一旦已经向用户输出内容，不得切换渠道或混拼不同 Provider 的结果。 |
| `PH6-18` | 收敛复习工作台的层级与稳定交互 | P1 | `进行中` | 复习工作台在桌面与 390px 视口中形成清晰的课程/科目、模式、继续学习和内容列表层级；核心按钮与展开区域尺寸稳定，按压、展开和加载反馈不引起布局抖动，主导航与工具图标具备可访问名称和可见焦点；不改变现有复习、刷题、统计和 API 语义。 |
| `PH5-25` | HTML 表格导入与格式优化诊断 | P1 | `进行中` | Markdown、ZIP 和内容级 JSON 正文中的安全 HTML 换行与表格可解析为结构化正文并正常阅读；脚本、样式、事件属性和其他未支持 HTML 继续被拒绝。 |

### 8.2 当前阶段下一批任务

当前阶段没有待开始任务。

## 9. 全部任务

### 第零阶段：产品与治理基线

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `GOV-001` | 建立产品、开发、设计与进度治理基线 | P0 | `M0` | `已完成` | 基线文档、AGENTS 规则、结构化进度源和生成/校验脚本全部存在且校验通过。 |
| `GOV-002` | 收口 iOS 风格前端设计与精简文案规范 | P0 | `M0` | `已完成` | DESIGN.md 明确 iOS 风格、页面结构、组件、文案长度、按钮词表和移动端验收红线。 |

### 第一阶段：工程基础与资料导入

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `PH1-01` | 建立 React + Node.js + TypeScript 工程骨架 | P0 | `M1` | `已完成` | 前后端可安装、启动、类型检查和构建，共享类型边界清晰。 |
| `PH1-02` | 建立 MySQL 数据模型、迁移和路径配置 | P0 | `M1` | `已完成` | 独立数据库用户可完成迁移，数据/资源/备份路径可以配置并验证可读写。 |
| `PH1-03` | 建立 Windows 一键启动与关闭脚本 | P0 | `M1` | `已完成` | 脚本可检查 MySQL、启动/停止本项目进程、输出状态且不自动打开浏览器。 |
| `PH1-04` | 实现单 Markdown/ZIP 解析与严格校验 | P0 | `M1` | `已完成` | 标题层级、未归属内容、公式、表格、图片和 ZIP 路径错误可精确定位。 |
| `PH1-05` | 实现导入预览、修正和数据库应用 | P0 | `M1` | `已完成` | 用户可修正单份资料后应用，资源进入受管目录，重复文件默认跳过。 |
| `PH1-06` | 验收 M1 资料导入主链路 | P0 | `M1` | `已完成` | 合法资料完整入库，非法资料被准确阻止，源文件保持不变。 |

### 第二阶段：核心复习体验

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `PH2-01` | 实现复习面板与继续上次闪卡 | P0 | `M2` | `已完成` | 首页展示核心数量、快捷入口和上次离开闪卡。 |
| `PH2-02` | 实现组合筛选、搜索和顺序切卡 | P0 | `M2` | `已完成` | 资料、章、节、状态和关键词可组合，左右按钮只在结果集内移动。 |
| `PH2-03` | 实现掌握状态、进度和复习记录 | P0 | `M2` | `已完成` | 未评估及三种状态即时保存，首次/最近查看和查看次数正确记录。 |
| `PH2-04` | 实现正文、表格、公式、代码和图片安全渲染 | P0 | `M2` | `已完成` | 桌面和 iPhone Safari 均可正确阅读复杂卡片，危险 HTML/脚本被过滤。 |
| `PH2-05` | 验收 M2 连续复习主链路 | P0 | `M2` | `已完成` | 用户能从面板进入、筛选、阅读、评估并连续切换完整结果集。 |

### 第三阶段：标注与可视化编辑

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `PH3-01` | 实现文本、表格和公式重点高亮 | P0 | `M3` | `已完成` | 多高亮即时保存，公式按完整元素标记，图片不可标记。 |
| `PH3-02` | 实现重点挖空与揭示重置 | P0 | `M3` | `已完成` | 遮罩保留原长度，可逐项揭示，离开卡片后恢复遮罩。 |
| `PH3-03` | 实现结构化可视化卡片编辑 | P0 | `M3` | `已完成` | 正文、表格、公式和图片可视化编辑并应用，前端不出现 Markdown/LaTeX 源码。 |
| `PH3-04` | 实现章、节、闪卡维护和回收站入口 | P0 | `M3` | `已完成` | 可新增、重命名、移动、排序和软删除层级内容。 |
| `PH3-05` | 实现多设备同步和卡片编辑锁 | P1 | `M3` | `已完成` | 电脑和手机可同时打开，同一张卡片只允许一个设备进入正文编辑。 |
| `PH3-06` | 验收 M3 标注与编辑主链路 | P0 | `M3` | `已完成` | 标注、挖空、编辑、层级维护和双设备访问均通过回归。 |

### 第四阶段：AI 与数据治理

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `PH4-01` | 实现多 Provider 与模型配置 | P0 | `M4` | `已完成` | 设置页可维护多套配置并切换当前启用项，密钥只在后端保存。 |
| `PH4-02` | 实现卡片 AI 讲解与当前结果保存 | P0 | `M4` | `已完成` | 默认/临时提示词可生成讲解，结果默认收起、只读且重新生成覆盖旧内容。 |
| `PH4-03` | 实现 Markdown 内容导出与 JSON 完整恢复 | P0 | `M4` | `已完成` | Markdown 只导出内容，JSON 可恢复完整学习状态且不包含秘密。 |
| `PH4-04` | 实现每日备份、恢复和回收站永久删除 | P0 | `M4` | `已完成` | 每天备份数据库和资源，默认保留 7 份，恢复和永久删除具有安全确认。 |
| `PH4-05` | 验收 M4 AI 与数据治理主链路 | P0 | `M4` | `已完成` | AI、导出、恢复、备份和回收站在真实 MySQL/资源目录环境中通过验证。 |
| `PH4-06` | 修复本机 AI Provider 加密配置 | P0 | `M4` | `已完成` | 本机 API 具备长度符合要求的 AI Provider 加密口令，新增 Provider 可正常写入，且口令不进入代码库、Markdown 或 JSON 导出。 |

### 第五阶段：移动端与发布

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `PH5-01` | 完成 iPhone Safari 响应式与 PWA 主屏幕安装 | P0 | `GA` | `进行中` | iPhone 可添加到主屏幕，选词、高亮、表格、公式和底部安全区域通过验收。 |
| `PH5-02` | 接入 Cloudflare Tunnel 与 review.panspan.cloud | P0 | `GA` | `进行中` | 项目脚本可启动/关闭 cloudflared，域名 HTTPS 稳定访问且凭证不入库。 |
| `PH5-03` | 制作包含固定 Node.js 运行时的 Windows 发布包 | P0 | `GA` | `已完成` | 目标 Windows 电脑无需开发环境即可配置并启动应用。 |
| `PH5-04` | 实现单账号密码登录和公网安全门禁 | P0 | `GA` | `已完成` | 安全哈希、会话 Cookie、失败限速和未登录访问拦截通过安全测试。 |
| `PH5-05` | 完成 GA 全链路验收 | P0 | `GA` | `进行中` | Windows 本机、iPhone、MySQL、Tunnel、备份恢复和账号安全完整通过发布验收。 |
| `PH5-06` | 实现全局秋日枫叶皮肤与专注复习反馈 | P1 | `GA` | `已完成` | 可在设置页切换并本机保存简约/秋日枫叶皮肤；浅色和深色模式下闪卡正文保持专注可读；AI 生成显示无文字进度与真实取消；文本和公式挖空按主题视觉规则逐项揭示。 |
| `PH5-07` | 建立课程、科目与资料九宫格导航及学习趋势 | P1 | `M5` | `已完成` | 资料入口可按课程→科目→资料逐层导航；资料九宫格、封面管理、详情页四状态分布与最近 30 天趋势、导入归属、移动排序、JSON/备份恢复和桌面/移动端 URL 返回通过验收。 |
| `PH5-08` | 补齐本机深浅色显示模式切换 | P1 | `GA` | `已完成` | 设置页可选择跟随系统、浅色或深色显示模式；简约和秋日枫叶皮肤均即时适配，选择刷新后仍保留。 |
| `PH5-09` | 修复 AI 讲解 Markdown 渲染 | P1 | `GA` | `进行中` | AI 讲解中的 Markdown 标题、强调、列表、分隔线、表格、公式和安全链接以只读结构化内容显示，不出现 Markdown 标记原文；兼容模型常见的 `**“强调文本”**` 与 `*“强调文本”*` 行内强调写法。 |
| `PH5-10` | 新增 JSON 与 Excel 资料导入及模板下载 | P1 | `M5`、`GA` | `进行中` | 用户能在导入页选择 Markdown、JSON 或 Excel 单文件，下载 JSON/Excel 模板，并将符合模板的资料预览、校验、重复检测后应用为层级闪卡。 |
| `PH5-11` | 临时屏蔽本机账号密码登录 | P0 | `GA` | `暂缓` | 本机地址访问业务接口和前端时不再出现登录表单，公网 Tunnel 已停止且账号哈希仍仅保留在本机忽略配置。 |
| `PH5-12` | 恢复公网账号门禁与 Tunnel | P0 | `GA` | `已完成` | review.panspan.cloud 可通过 HTTPS 对外访问，未登录业务接口仍返回 401，本机凭证和 Tunnel 凭证未进入进度源。 |
| `PH5-13` | 按资料续读并简化复习资料选择 | P1 | `GA` | `进行中` | 每份资料独立保存并恢复最后一张闪卡；当前学习资料选择器仅显示资料名称，并决定顶部统计与快捷开始范围；复习页筛选只保留资料和掌握状态，不再提供章节、节或关键词选择。 |
| `PH5-14` | 在资料详情提供逐卡内容编辑与排序 | P1 | `GA` | `进行中` | 资料详情按章、节展示独立闪卡编辑卡；用户可在卡内可视化编辑正文，表格支持空白单元格输入、加删行、相邻单元格横纵合并与拆分，并可在同一节内上移或下移，刷新后顺序和表格跨度保持一致。 |
| `PH5-15` | 实现资料九宫格长按拖入回收站快捷删除 | P1 | `GA` | `已完成` | 在资料九宫格长按任一资料后，可将其拖入页面顶部垃圾桶，资料随即移入回收站并从当前网格移除；未拖入时不改变页面或打开资料详情。 |
| `PH5-16` | 支持 JSON 内容级高亮导入 | P1 | `M5`、`GA` | `已完成` | 内容级 JSON 能导入文本与公式高亮，模板可表达该协议；歧义、缺失或正文修正后的失效匹配会在预览或应用时阻止写入。 |
| `PH5-17` | 明确 JSON 高亮重复匹配判定标准 | P1 | `M5`、`GA` | `已完成` | 下载的 JSON 模板明确说明重复按当前卡片 body 的连续子串匹配判定、包含关系与表格文本均会计数，并提供 occurrence 示例且能被解析器直接通过。 |
| `PH5-18` | 将 JSON 高亮去重要求改为模型可执行指令 | P1 | `M5`、`GA` | `已完成` | JSON 模板包含模型生成高亮前必须执行的逐卡精确匹配、自检、优先扩展唯一短语和 occurrence 填写规则，明确禁止按 highlights 数组顺序猜测序号。 |
| `PH5-19` | 固化 JSON 高亮重复判定算法与反例 | P1 | `M5`、`GA` | `已完成` | JSON 模板以结构化字段写出命中单位、包含与重叠、节点边界、occurrence 编号和通过/失败条件，并覆盖短数字、跨节点、公式和重复声明反例；模板可直接解析。 |
| `PH5-20` | 支持 JSON body 内联高亮标记导入 | P1 | `M5`、`GA` | `已完成` | JSON 卡片 body 可用 [[hl:原文]] 直接标记任意重复文本位置；导入后的正文不包含标记，生成正确结构化锚点；数组高亮仍兼容公式与旧 text/occurrence 文件，模板以内联标记为主。 |
| `PH5-21` | 兼容表格单元格内联高亮标记 | P1 | `M5`、`GA` | `已完成` | 合法 GFM 表格单元格内的 [[hl:原文]] 可导入、正文不含标记且锚点位于该单元格；跨单元格或 Markdown 结构的标记继续阻止；原有错误表格分隔行校验保持有效。 |
| `PH5-22` | 优化复习闪卡切换性能 | P1 | `M2`、`GA` | `已完成` | 正常上一张、下一张切换只读取目标闪卡，不请求当前筛选集的完整列表；单张响应只附带相邻卡片 ID、序号和总数；状态切换、筛选、续读位置和复习记录保持原有语义。 |
| `PH5-23` | 降低深色模式闪卡高亮刺眼度 | P1 | `M2`、`M3`、`GA` | `已完成` | 深色模式下文本、表格和公式阅读高亮使用低饱和深色背景与可读前景，不再出现刺眼的浅色/亮橙色；浅色模式、高亮数据和挖空遮罩保持原有语义。 |
| `PH5-24` | 导入正文格式 AI 修正 | P1 | `M5`、`GA` | `已完成` | 导入预览的“需要处理”列表中，每条可修正正文 Markdown 格式问题均提供独立“优化格式”操作；每次只向当前启用 Provider 发送该条问题和对应的一张卡片正文，允许多条并发，且用户确认后仍通过既有应用操作写入。JSON 无法读取或语法不合规时不显示且不能调用 AI 修正。 |
| `PH5-25` | HTML 表格导入与格式优化诊断 | P1 | `M5`、`GA` | `进行中` | Markdown、ZIP 和内容级 JSON 正文中的安全 HTML 换行与表格可解析为结构化正文并正常阅读；脚本、样式、事件属性和其他未支持 HTML 继续被拒绝。 |
| `PH5-26` | 建立课程复习工作台上下文与聚合入口 | P1 | `M5`、`GA` | `已完成` | 复习入口以课程为持久上下文，支持课程搜索、按课程记忆具体科目和闪卡/题库分段；上下文存入数据库并可跨端恢复，URL 可恢复课程工作台和具体练习；课程工作台按当前科目范围提供闪卡资料与题库摘要，复用现有闪卡/刷题入口，不展开单张内容。 |
| `PH5-27` | 新增库洛米精致主题 | P1 | `GA` | `已完成` | 设置页可切换并在本机持久化库洛米主题；主题在浅色、深色、系统模式下保持高对比、可读焦点、稳定触控尺寸和无横向溢出，正文阅读面不被装饰侵入。 |

### 第六阶段：题库与刷题

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `PH6-01` | 冻结题库领域协议与阶段拆分 | P0 | `M6` | `已完成` | 产品定位、开发方案、设计规范和结构化进度源已一致定义章节题/真题/模拟题入口、JSON/Excel 模板、题型校验、双模式刷题、统计、错题专项、AI 历史与备份兼容；后续任务均有明确边界。 |
| `PH6-02` | 建立题库数据模型与备份恢复兼容 | P0 | `M6` | `已完成` | MySQL 迁移、共享 TypeScript 契约及 JSON 备份恢复支持题库、章节、题目、题目 AI 版本、作答会话和快照；旧 version 1 闪卡备份仍可恢复，且不包含任何 API Key。 |
| `PH6-03` | 实现题库 JSON/Excel 导入与模板 | P0 | `M6` | `已完成` | 导入页可在既有课程、科目和三类题库入口下下载对应 JSON/Excel 模板、预览和应用；章节题按章节建树，真题/模拟题无章节，所有已确认必填、连续选项、答案、判断题和重名题库校验均有定位错误。 |
| `PH6-04` | 实现课程科目下的题库目录管理 | P0 | `M6` | `已完成` | 科目页可在资料与题库间切换，题库页固定显示章节题、真题、模拟题入口；各入口支持空题库新建、改名、排序、软删除与恢复，章节题额外支持章节新增、改名、排序、移动和删除约束。 |
| `PH6-05` | 实现可视化题目编辑 | P0 | `M6` | `已完成` | 题库内可新增、编辑、移动、排序和软删除题目；题干、选项、解析均通过可视化编辑，选项动态增删且最多六项，保存时严格复用题型、答案和判断题校验，不出现 Markdown 或 LaTex 源码编辑态。 |
| `PH6-06` | 实现背题与检测会话 | P0 | `M6` | `已完成` | 题库可按整套或章节启动背题、检测；背题即时按绿色正确/红色误选反馈，检测过程不泄露结果且支持返回修改，单选/判断自动下一题、多选确认后下一题，未完成会话可继续或放弃并保存最终答案快照。 |
| `PH6-07` | 实现题库统计与错题专项 | P1 | `M6` | `已完成` | 完成会话后可查看总题数、已作答、未作答、正确、错误与一位小数正确率；题库、章节题章节、题型和模式均有累计统计，支持本次错题和累计错题专项，累计错题答对后立即移出。 |
| `PH6-08` | 实现题目 AI 讲解与版本历史 | P1 | `M6` | `已完成` | 题目详情可使用当前启用 Provider 与可选临时提示词生成、取消和安全渲染讲解；请求仅含当前题目及本次作答上下文，每次生成保留版本，题目变更后旧版本标注可能过时且不删除。 |
| `PH6-09` | 验证题库主链路与备份恢复 | P0 | `M6`、`GA` | `已完成` | 真实 MySQL、HTTP、JSON 备份恢复和桌面/390px 浏览器验收覆盖三类题库导入、编辑、两种刷题模式、续答、统计、错题专项与 AI 历史；无文本溢出、答案泄露、错误统计或未受保护的业务接口回归。 |
| `PH6-10` | 重构独立刷题页与题目解析入口 | P1 | `M6`、`GA` | `已完成` | 刷题会话通过独立页面展示，不再以弹窗覆盖题库或复习工作台；页面提供题干、选项、进度、返回/放弃、完成结果和题目 AI 解析入口。背题模式提交后停留在当前题，用户点击下一题才进入下一题；检测模式保持既有自动/确认前进与答案隐藏规则。 |
| `PH6-11` | 放开题干长度限制 | P1 | `M6` | `已完成` | 题库 JSON/Excel 导入和应用内题目保存均可接受超过 255 个字符的非空题干；题干仍受既有结构化内容 100 万字符防护约束，其他字段长度、题型、选项和答案校验保持不变。 |
| `PH6-12` | 实现题目收藏与科目收藏题练习 | P1 | `M6` | `已完成` | 用户可在题目工作区及刷题页收藏或取消收藏单题；每个科目复习页的题库分段可对该科目全部已收藏题启动背题或检测，未完成会话可继续，完整 JSON 导出恢复后收藏状态和会话范围保持一致。 |
| `PH6-13` | 为章节题增加工作台选章刷题入口 | P1 | `M6` | `已完成` | 课程复习工作台的章节题题库卡片显示有效章节及题数，用户选择章节后可分别启动背题或检测；真题、模拟题仍只能整库启动，未选择章节时章节题仍可整库启动。 |
| `PH6-14` | 重构课程复习工作台界面 | P1 | `M6`、`GA` | `已完成` | 课程复习工作台在闪卡和题库两种模式中均以清晰的上下文、内容行和操作行组织；课程/科目选择、继续学习、资料与题库操作在 390px 和桌面端无横向溢出，新增交互具备可访问名称、可见键盘焦点与稳定按压反馈。 |
| `PH6-15` | 支持乱序、固定数量与未做筛选练习 | P1 | `M6` | `进行中` | 用户可在题库、章节题和科目收藏题的背题/检测创建入口选择全部或固定题目数量、只练未做或不区分，并可开启乱序；服务端在创建会话时一次性筛选、抽取并固化题目集合和顺序，刷新、续答、完成、放弃及完整 JSON 导出恢复后保持一致；数量超过可用题目时按可用题目数练习，不改变判题、统计和检测答案隐藏语义。 |
| `PH6-16` | 在学习工作区提供悬浮问答助手 | P1 | `M6` | `进行中` | 刷题与单张闪卡阅读页均提供默认收起的悬浮助手；展开后用户可围绕当前题目或当前闪卡进行流式问答，生成完成的回答可一键复制，桌面与 390px 移动端不遮挡正文、选项、键盘焦点或底部操作。检测模式未作答题仅允许概念性提示，服务端不得将答案、解析或知识点传给模型，也不得返回直接答案。 |
| `PH6-17` | 实现 AI Provider 优先级与失败自动降级 | P0 | `M6` | `进行中` | 设置页可将多个 AI Provider 纳入候选池并调整优先级；任一 AI 请求会按启用顺序尝试渠道，首段有效输出前的网络、超时、Provider 状态或内容格式失败会自动切换下一渠道，全部失败时返回明确错误。流式回答一旦已经向用户输出内容，不得切换渠道或混拼不同 Provider 的结果。 |
| `PH6-18` | 收敛复习工作台的层级与稳定交互 | P1 | `M6` | `进行中` | 复习工作台在桌面与 390px 视口中形成清晰的课程/科目、模式、继续学习和内容列表层级；核心按钮与展开区域尺寸稳定，按压、展开和加载反馈不引起布局抖动，主导航与工具图标具备可访问名称和可见焦点；不改变现有复习、刷题、统计和 API 语义。 |

### 第七阶段：检索、巩固与洞察

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `GOV-003` | 冻结检索、错题巩固与学习洞察范围 | P1 | `M7` | `已完成` | 产品定位、开发方案、设计规范和结构化进度源一致定义全局检索、错题巩固与学习洞察的目标、数据边界、交互约束、任务顺序与风险；不产生应用或数据库实现。 |
| `PH7-01` | 实现跨课程全局检索与目标定位 | P1 | `M7` | `已完成` | 用户可在认证后跨课程检索有效资料名称、闪卡标题/正文和题干，以课程、科目、内容类型收窄结果；每个结果显示受控摘要并能在刷新后直接定位到资料、闪卡或题目，桌面与 390px 视口无横向溢出。 |
| `PH7-02` | 实现错题备注、筛选与巩固复练 | P1 | `M7` | `已完成` | 用户可为题目保存当前错误原因/备注，并在同一科目内按知识点、题型和最近错误筛选累计错题后开始背题或检测；备注与不可变作答快照隔离，完整 JSON 与每日备份恢复后保持一致。 |
| `PH7-03` | 实现周期学习洞察与薄弱知识点 | P1 | `M7` | `已完成` | 用户可查看最近 7 天、30 天的闪卡复习量、掌握状态变化、已完成题库会话正确率和达到最小样本量的薄弱知识点；统计按 Asia/Shanghai 计算，图表有文本摘要且无数据、边界日期、软删除与进行中会话均正确处理。 |
| `PH7-04` | 重构学习洞察统计图表 | P1 | `M7` | `进行中` | 洞察面板能在 7 天和 30 天周期下清楚呈现每日复习量、四种状态变化、题库正确率和薄弱知识点；图表比例诚实、标签不重叠、表格可扫描，颜色不是唯一判断依据，空数据和移动端布局正确。 |

### 第八阶段：文字与手写备注

| 任务 | 标题 | 优先级 | 里程碑 | 状态 | 验收标准 |
|---|---|---|---|---|---|
| `PH8-01` | 实现闪卡与题目的文字、手写备注 | P1 | `M8` | `已完成` | 用户可为任一闪卡和题目保存当前文字、手写备注，并在刷新、跨设备读取、完整 JSON 导出恢复和每日备份恢复后保持一致；题目文字与笔迹仅在背题提交后或检测完成后紧随解析显示，检测作答过程不泄露备注、解析或答案。 |

## 10. 已确认执行边界

### `PH1-01` 建立 React + Node.js + TypeScript 工程骨架

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：先建立可运行的 TypeScript Web 工程和配置骨架，不提前实现产品功能。
- 本步纳入：
  - 建立前端、后端和共享类型目录。
  - 建立开发环境变量与本机私有配置加载规则。
  - 建立基础健康检查、日志和错误处理入口。
  - 建立类型检查、构建和最小测试命令。
- 本步明确不纳入：
  - 不实现 Markdown 导入、复习界面、高亮、AI 或登录。
  - 不接入 Cloudflare Tunnel，不制作最终发布包。
- 后续归属任务：
  - MySQL 数据模型归属 PH1-02。
  - Windows 启停脚本归属 PH1-03。
  - 资料导入归属 PH1-04 和 PH1-05。

### `PH2-01` 实现复习面板与继续上次闪卡

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：建立以复习为默认入口的最小面板，并仅持久化最后停留的闪卡位置。
- 本步纳入：
  - 提供已导入资料的核心数量、快捷起点和上次闪卡查询接口。
  - 提供复习首页、继续/全部/状态预设入口、单章起点和最小安全文本卡片视图。
  - 使用 app_settings 保存最后停留的闪卡 ID，不修改 review_records。
  - 保留现有导入入口，且前端默认进入复习面板。
- 本步明确不纳入：
  - 不实现资料、章、节、状态和关键词的组合筛选、搜索或结果集内前后切换。
  - 不更新掌握状态、首次/最近查看、查看次数或状态变更时间。
  - 不实现表格、公式、代码、图片的正式富内容渲染。
  - 不实现高亮、挖空、可视化编辑、AI 讲解或登录。
- 后续归属任务：
  - 组合筛选、搜索和顺序切卡归属 PH2-02。
  - 掌握状态、进度和复习记录归属 PH2-03。
  - 正文、表格、公式、代码和图片安全渲染归属 PH2-04。
  - 高亮挖空、内容编辑、AI 和登录分别归属 PH3、PH4、PH5 任务。

### `PH3-01` 实现文本、表格和公式重点高亮

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：在既有结构化阅读页中，为文本、表格文字和整段公式提供可重复保存的重点高亮。
- 本步纳入：
  - 为闪卡读取结果返回受校验的高亮记录，并提供新增、删除高亮的受校验 API。
  - 以结构化内容节点路径和节点内字符范围保存文本高亮；表格文字沿用同一定位模型。
  - 让阅读页可选择单个文本节点范围后即时保存，并可点击公式标记或取消整段公式重点。
  - 在阅读态呈现已有高亮，拒绝图片、无效节点路径、空范围与不安全锚点，并补充 API 和服务测试。
- 本步明确不纳入：
  - 不实现重点挖空、遮罩揭示或离开卡片后的揭示重置。
  - 不实现正文、表格、公式或图片的可视化编辑。
  - 不改变既有复习筛选、掌握状态、复习记录、层级结构或资源访问权限。
- 后续归属任务：
  - 重点挖空与揭示状态归属 PH3-02。
  - 结构化内容编辑、编辑后的高亮迁移归属 PH3-03。
  - 层级维护和编辑锁分别归属 PH3-04、PH3-05。

### `PH4-01` 实现多 Provider 与模型配置

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：实现 AI Provider 配置的后端安全存储、维护接口和设置页管理流程。
- 本步纳入：
  - 复用 ai_provider_profiles 表，提供配置列表、新增、编辑、删除和当前启用项切换；同一时刻最多一套配置处于启用状态。
  - 配置列表和所有浏览器响应只返回是否已配置密钥，API Key 仅提交至后端并使用本机环境变量派生的密钥加密后存储。
  - 新增设置主导航与 iOS 分组列表中的 AI 配置管理界面，支持 Provider、接口地址、模型、密钥和启用项维护。
  - 补充服务、HTTP、真实 MySQL 以及桌面和 390x844 移动端的验收，并同步进度真相源。
- 本步明确不纳入：
  - 不向任意 AI Provider 发起请求，不实现测试连接。
  - 不实现卡片 AI 讲解、默认或临时提示词、讲解持久化或覆盖逻辑。
  - 不实现 Markdown 导出、JSON 恢复、每日备份、回收站恢复或永久删除。
- 后续归属任务：
  - Provider 请求适配、连接测试和卡片讲解归属 PH4-02。
  - 导出与恢复归属 PH4-03；备份、恢复和永久删除归属 PH4-04。

### `PH5-01` 完成 iPhone Safari 响应式与 PWA 主屏幕安装

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：为现有复习 Web 应用补齐 iPhone Safari 的 PWA 安装入口和移动端阅读适配，并以真实移动视口验证选词、高亮、表格、公式、顶部空间与底部安全区域。
- 本步纳入：
  - 补齐 Web App Manifest、iPhone Safari 元数据、图标资源和受控的 Service Worker 注册/缓存策略。
  - 调整移动端页面、导航、复习卡片、状态操作和 Sheet 的响应式布局，确保正文与底部安全区域不被遮挡。
  - 移除已登录状态下占用全局顶部空间的账户工具栏，在设置页账号分组提供当前账号和退出操作。
  - 将移动端常用页首操作收敛为带 Tooltip 与无障碍名称的图标按钮，标题与操作区同一基线并压缩标题尺寸。
  - 将移动端底部主导航压缩至原有约三分之一的视觉高度，并同步收紧主内容底部预留。
  - 验证 iPhone Safari 视口下文本选词、高亮、表格换行或滚动、公式展示和页面无横向溢出。
  - 补充可重复执行的构建与浏览器验收证据。
- 本步明确不纳入：
  - 不实现离线复习、离线编辑、后台同步或缓存 API 数据。
  - 不接入 Cloudflare Tunnel、review.panspan.cloud、公网发布、账号登录或 Windows 发布包。
  - 不修改账号、密码哈希、认证接口、会话 Cookie、失败限速或主导航业务规则。
  - 不扩展复习、导入、编辑、AI、备份和恢复业务规则。
  - 不改变桌面端导航、导航入口顺序、safe-area 语义或任何导航业务逻辑。
- 后续归属任务：
  - Cloudflare Tunnel 与域名接入归属 PH5-02。
  - 固定 Node.js Windows 发布包归属 PH5-03。
  - 单账号密码门禁归属 PH5-04；GA 全链路验收归属 PH5-05。

### `PH6-01` 冻结题库领域协议与阶段拆分

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：将用户确认的刷题模块决策固化为可实施、可验收的领域协议和最小任务序列。
- 本步纳入：
  - 明确课程→科目→章节题/真题/模拟题→题库→题目层级，其中仅章节题在导入与管理中拥有章节树。
  - 明确章节题与真题/模拟题的 JSON、Excel 模板差异，及题干、题型、选项、答案必填和 A-F 连续选项校验。
  - 明确背题、检测、会话续答、错题专项、统计、可视化编辑、题目 AI 多版本与备份迁移的交互和数据边界。
  - 新增第六阶段任务卡、范围风险和变更日志，并同步自动生成的展示层。
- 本步明确不纳入：
  - 不创建数据库迁移、共享 API 契约、题库导入接口、前端入口或任何刷题界面。
  - 不实现题目与闪卡知识点联动、题目图片导入、随机出题、部分得分、主观题 AI 评分、趋势图或日期筛选。
  - 不修改现有闪卡导入、闪卡复习、现有 AI 讲解存储或已发布备份内容。
- 后续归属任务：
  - PH6-02 负责数据库、共享契约和备份恢复迁移。
  - PH6-03 至 PH6-08 分别负责题库导入、目录管理、可视化编辑、刷题会话、统计错题专项和题目 AI。
  - PH6-09 负责整链路真实 MySQL、浏览器和备份回归。

### `GOV-003` 冻结检索、错题巩固与学习洞察范围

- 状态：已完成
- 确认日期：2026-08-20
- 边界摘要：将用户确认的三个 P1 能力拆分为可独立验收的后续任务，并冻结不扩展自动复习算法、离线、批量导入或多用户范围。
- 本步纳入：
  - 定义跨资料与题库的全局检索范围、受控摘要和目标定位原则。
  - 定义与不可变作答快照隔离的题目错误原因/备注，以及累计错题、知识点、题型和最近错误筛选边界。
  - 定义基于既有记录的 7 天、30 天学习洞察口径、最小样本量、空状态和无障碍图表摘要。
  - 新增第七阶段、M7、任务卡、风险、日志并生成展示层。
- 本步明确不纳入：
  - 不创建数据库迁移、搜索索引、API 契约、前端页面或测试代码。
  - 不实现随机复练、间隔重复、提醒、打卡、排名、AI 学习建议、离线同步、批量导入或多用户协作。
  - 不改变现有闪卡、题目、会话、统计、备份恢复或发布安全门禁。
- 后续归属任务：
  - PH7-01 负责全局检索的数据模型、接口、页面和性能验收。
  - PH7-02 负责错题备注、筛选复练、备份恢复和历史边界验收。
  - PH7-03 负责周期学习洞察、统计口径、无障碍摘要和边界日期验收。

### `PH8-01` 实现闪卡与题目的文字、手写备注

- 状态：已完成
- 确认日期：2026-08-20
- 边界摘要：以一份当前文字与矢量笔迹记录满足随手书写，不改变正文、题目版本或作答快照。
- 本步纳入：
  - 为闪卡新建当前文字与手写备注，为既有题目当前文字备注增加手写笔迹字段；文字和归一化矢量笔画均可新增、撤销、清除和保存。
  - 提供认证读写 API、输入规模校验和移动端画板 Sheet；闪卡从复习页次级工具区进入，题目从题库单题工具区进入。
  - 在背题提交后及检测会话完成后，将题目的当前文字与手写备注放在解析之后显示；检测作答中不得返回或显示备注。
  - 将两类当前备注纳入完整 JSON、每日备份和恢复，保持旧 JSON 及现有题目纯文字备注兼容；补充迁移、服务、HTTP、数据治理、类型、构建和响应式验收。
- 本步明确不纳入：
  - 不实现手写识别、文字转写、笔迹搜索、AI 处理、图片或 PDF 导入、附件、录音、协作分享、权限细分或备注历史版本。
  - 不修改闪卡正文、题目题干/解析、题目版本、题目 AI 讲解版本、复习记录、统计口径或不可变作答快照。
  - 不在检测作答阶段显示题目答案、解析、文字备注或手写笔迹，不改变既有背题和检测判题语义。
- 后续归属任务：
  - 多颜色、压感、荧光笔、橡皮擦区域擦除、画布背景模板和笔迹历史另立编辑器能力任务。
  - 手写识别、笔迹检索和 AI 归纳另立智能输入任务。

### `PH1-02` 建立 MySQL 数据模型、迁移和路径配置

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：建立可重复执行的 MySQL 初始迁移、后端数据库连接配置和三类存储路径读写检查，为资料导入提供稳定数据边界。
- 本步纳入：
  - 建立覆盖产品核心实体的 MySQL 初始迁移和迁移命令。
  - 建立数据库连接参数读取与连接检查，不把密码写入代码或进度文件。
  - 建立数据、资源、备份路径配置，并提供创建、写入、读取和清理验证。
  - 为配置解析、路径检查和迁移基础能力补充自动化测试。
- 本步明确不纳入：
  - 不实现 Markdown/ZIP 解析、导入预览或资料写入业务 API。
  - 不实现 Windows 启停脚本、每日备份/恢复、回收站或账号登录。
  - 不在本步启动 MySQL 服务或提交任何本机密码、密钥和凭证。
- 后续归属任务：
  - Windows 服务检查和进程启停归属 PH1-03。
  - Markdown/ZIP 解析与严格校验归属 PH1-04。
  - 导入预览和数据库应用归属 PH1-05。
  - 备份执行、恢复和 JSON 完整恢复归属 PH4-03 和 PH4-04。

### `PH2-02` 实现组合筛选、搜索和顺序切卡

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：在现有复习面板上建立可组合的结果集筛选，并让卡片页按当前结果集顺序前后切换。
- 本步纳入：
  - 提供资料、章、节、掌握状态和关键词筛选参数。
  - 返回稳定顺序的筛选结果集及当前卡片位置。
  - 前端提供筛选入口、结果数量和上一张/下一张切卡。
  - 筛选为空或卡片不存在时返回可识别错误，并补充自动化测试。
- 本步明确不纳入：
  - 不修改掌握状态、首次/最近查看、查看次数或状态变更时间。
  - 不实现表格、公式、代码、图片的正式安全渲染。
  - 不实现高亮、挖空、可视化编辑、AI 讲解、备份或登录。
- 后续归属任务：
  - 掌握状态、进度和复习记录归属 PH2-03。
  - 正文、表格、公式、代码和图片安全渲染归属 PH2-04。
  - 高亮与编辑归属 PH3，AI 与数据治理归属 PH4，登录与发布归属 PH5。

### `PH3-02` 实现重点挖空与揭示重置

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：在既有闪卡阅读页中，基于已持久化的文本、表格文字和公式高亮实现仅当前会话有效的重点挖空与逐项揭示。
- 本步纳入：
  - 复用当前闪卡返回的有效高亮，在挖空模式将文本高亮范围按原文字数显示为可点击遮罩，并将公式高亮按完整公式显示为遮罩。
  - 支持逐项揭示和显示全部；遮罩状态仅保存在当前阅读页内存中，不写入数据库。
  - 在当前闪卡切换、重新进入闪卡或挖空模式关闭后重置揭示状态，并适配现有桌面与 390x844 移动阅读布局。
  - 补充可验证的前端单元测试或页面自动化验收，覆盖文本、表格、公式、无高亮卡片、逐项揭示和切卡重置。
- 本步明确不纳入：
  - 不改变高亮锚点模型、已有高亮读写 API 或数据库结构。
  - 不实现正文、表格、公式或图片的可视化编辑，也不处理编辑后的高亮迁移。
  - 不改变复习筛选、顺序切卡、掌握状态、复习记录或资料层级维护。
- 后续归属任务：
  - 结构化内容编辑和高亮迁移归属 PH3-03。
  - 章、节、闪卡维护与回收站入口归属 PH3-04。
  - 多设备编辑锁归属 PH3-05。
  - M3 标注与编辑整体验收归属 PH3-06。

### `PH4-02` 实现卡片 AI 讲解与当前结果保存

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：实现当前启用 AI Provider 的请求适配、单配置连接测试、默认/临时提示词、卡片当前讲解覆盖保存，以及卡片页默认收起的只读讲解区。
- 本步纳入：
  - 后端读取唯一启用的 Provider 配置，解密 API Key，并调用 OpenAI 兼容的 chat completions 接口。
  - 设置页可对指定 Provider 发起最小连接测试，成功或失败均只返回短状态，不向浏览器返回 API Key。
  - 支持系统默认讲解模板和仅本次请求生效的临时提示词；校验响应并将当前讲解、Provider、模型和提示词保存到 ai_explanations。
  - 提供卡片讲解查询与生成 API；重新生成覆盖同一卡片的旧讲解，浏览器响应不返回 API Key。
  - 复习卡片页增加默认收起、只读的 AI 讲解区，支持展开、临时提示词、生成和覆盖反馈。
  - 补充 Provider 请求适配、讲解保存和 HTTP/API 前端契约测试。
- 本步明确不纳入：
  - 不实现 Markdown 内容导出、JSON 完整恢复、每日备份、恢复或回收站永久删除。
  - 不保存 AI 历史版本，不增加多卡批量生成、流式输出或离线队列。
  - 不实现账号登录、权限模型或公网发布。
- 后续归属任务：
  - 导出与完整恢复归属 PH4-03；备份、恢复和永久删除归属 PH4-04。
  - 历史版本、批量生成和流式输出作为后续独立需求评估。

### `PH5-02` 接入 Cloudflare Tunnel 与 review.panspan.cloud

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：为 Windows 启停流程接入专用 Cloudflare Tunnel 和 review.panspan.cloud DNS 路由，补齐源站诊断及受控生命周期，保留 PH5-04 公网安全门禁。
- 本步纳入：
  - 提供不含秘密的 cloudflared 配置模板，固定 review.panspan.cloud 到本机 Web 源站的 ingress 规则。
  - 使用现有 Cloudflare 登录材料创建 review-panspan-cloud Tunnel，并将 review.panspan.cloud 路由到该 Tunnel。
  - 启动前校验 cloudflared 可执行文件、配置文件、凭证路径、源站地址和配置语法，并输出可定位的错误。
  - 启动脚本记录 Tunnel 独立日志、进程状态和公网访问地址；停止脚本可靠停止 Tunnel 与应用进程树。
  - 提供可重复执行的本地配置诊断和 cloudflared ingress 校验命令。
  - 验证凭证与本机敏感配置不会被提交或写入进度导出。
- 本步明确不纳入：
  - 不创建新的 Cloudflare 账户，不复制或提交 Tunnel 凭证，不修改其他域名或 Tunnel。
  - 不在 PH5-04 完成前启动公网连接器或开放未登录访问，不修改 PUBLIC_ACCESS_READY 的安全门禁语义。
  - 不制作固定 Node.js 运行时或 Windows 发布包。
  - 不实现单账号密码登录、Cloudflare Access 或其他身份认证。
- 后续归属任务：
  - 连接器启动、review.panspan.cloud HTTPS 稳定性和公网页面验收待 PH5-04 账号密码门禁完成后进行。
  - 单账号密码门禁归属 PH5-04；Windows 固定运行时发布包归属 PH5-03；GA 全链路归属 PH5-05。

### `PH6-02` 建立题库数据模型与备份恢复兼容

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：先建立与闪卡隔离的题库持久化和完整备份契约，使后续导入、编辑、刷题和 AI 功能建立在可恢复的数据基础上。
- 本步纳入：
  - 新增题库、章节题章节、题目、题目 AI 讲解版本、刷题会话和作答快照的 MySQL 迁移及迁移测试。
  - 扩展共享 TypeScript 类型与 JSON 导出/恢复，当前导出使用 version 2，恢复兼容已有 version 1 闪卡备份。
  - 保存题目结构化正文、选项、答案、知识点、题目版本、会话范围、作答快照和最终判定所需字段；背题、检测和错题专项均可作为最近有效结果来源。
  - 补充 JSON 导出恢复与敏感字段隔离测试，并运行真实 MySQL 迁移与现有数据治理回归。
- 本步明确不纳入：
  - 不提供题库导入模板、预览、应用接口或前端导入入口。
  - 不提供题库目录、章节、题目编辑、刷题、统计、错题专项或题目 AI 请求接口与界面。
  - 不实现题目和闪卡知识点联动、题目图片、随机抽题、部分得分、主观题 AI 评分或统计趋势。
- 后续归属任务：
  - PH6-03 使用本步模型实现题库 JSON/Excel 模板、预览和应用。
  - PH6-04、PH6-05 分别提供目录和可视化编辑的业务接口与界面。
  - PH6-06 至 PH6-08 分别实现刷题会话、统计错题专项和题目 AI 讲解。

### `PH7-01` 实现跨课程全局检索与目标定位

- 状态：已完成
- 确认日期：2026-08-20
- 边界摘要：建立可缩小范围、可定位、性能可测的内容检索入口，不把结构化正文降级为 Markdown 源码或将完整正文批量传回前端。
- 本步纳入：
  - 检索未软删除资料的名称、有效闪卡标题与结构化正文中的可读文本、有效题目的题干。
  - 提供课程、科目和资料/闪卡/题目内容类型约束，返回稳定排序、有限数量的摘要和命中目标路径。
  - 通过认证 API、桌面与 390px 页面接入搜索、筛选、空状态和目标定位。
  - 使用真实规模 EXPLAIN、分页或结果上限验证查询性能，并补充 API、浏览器和权限回归。
- 本步明确不纳入：
  - 不检索已软删除内容、AI 讲解、题目解析、作答快照、备份文件或本机文件系统。
  - 不提供高级检索语法、保存搜索、搜索历史、全文高亮编辑、标签、推荐或 AI 语义检索。
  - 不改变资料导入、正文编辑、题库排序、复习筛选和现有 URL 业务语义。
- 后续归属任务：
  - PH7-02 负责错题备注与错题维度复练。
  - PH7-03 负责周期学习洞察。
  - 检索规模超过本步定义的体验阈值时，另立性能任务评估专用索引、异步索引或更细分页。

### `PH1-03` 建立 Windows 一键启动与关闭脚本

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：建立 Windows 本机开发环境的一键启停入口，用可双击的 CMD 包装 PowerShell 脚本，并用受管进程记录确保停止操作只影响本项目。
- 本步纳入：
  - 在启动前检查 Node.js、MySQL 应用连接和三类存储路径。
  - 构建共享类型后启动本项目 API 与 Web 开发服务，记录启动进程和日志路径。
  - 轮询 API 与 Web 健康状态，输出本地访问地址和失败日志位置，不自动打开浏览器。
  - 根据受管进程记录停止本项目进程树，并输出停止后的状态。
  - 集成受配置控制的 cloudflared tunnel 启动与停止，并记录独立日志和进程。
  - 提供可双击的启动和停止 CMD 入口，错误时保留窗口以便查看结果。
- 本步明确不纳入：
  - 不启动、停止或配置 MySQL 服务。
  - 不在未完成单账号密码保护前开放公网 Tunnel。
  - 不配置域名、Tunnel 凭证或最终公网发布验收。
  - 不制作固定 Node.js 运行时或 Windows 发布包。
  - 不实现 Markdown/ZIP 导入、备份恢复或账号登录。
- 后续归属任务：
  - Cloudflare Tunnel 的域名稳定性、凭证管理和公网验收归属 PH5-02。
  - 固定 Node.js 运行时和 Windows 发布包归属 PH5-03。
  - 资料导入主链路归属 PH1-04、PH1-05 和 PH1-06。
  - 账号密码与公网安全门禁归属 PH5-04。

### `PH2-03` 实现掌握状态、进度和复习记录

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：在既有筛选结果集与闪卡页上，补齐单卡掌握状态的即时保存及复习记录。
- 本步纳入：
  - 提供受校验的掌握状态更新 API，并返回保存后的闪卡状态与复习记录。
  - 打开或切换到有效闪卡时记录首次查看、最近查看和查看次数。
  - 在闪卡页提供掌握、了解、努力三段式状态控件；保存后同步当前结果集与复习面板数量，并自动进入当前结果集的下一张闪卡。
  - 覆盖状态校验、已删除或不存在闪卡、复习记录的首次与重复查看，以及前端可见状态。
- 本步明确不纳入：
  - 不实现正文、表格、公式、代码或图片的正式安全渲染。
  - 不实现高亮、挖空、可视化编辑、AI 讲解、备份或登录。
  - 不改变既有筛选条件、结果集排序或资料层级结构。
- 后续归属任务：
  - 富内容安全渲染归属 PH2-04。
  - M2 主链路整体验收归属 PH2-05。
  - 高亮挖空、内容编辑、AI 和登录分别归属 PH3、PH4、PH5 任务。

### `PH3-03` 实现结构化可视化卡片编辑

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：在既有闪卡阅读页中编辑受限的结构化内容节点，并在保存时保留可验证的高亮、清理无法继续精确定位的高亮。
- 本步纳入：
  - 提供闪卡标题和正文结构化节点的读取、校验和应用 API，正文仅接收白名单内的安全节点属性。
  - 在阅读页原位提供段落、表格单元格、公式和图片替换信息的可视化编辑，不显示 Markdown 或 LaTeX 源码。
  - 编辑后在同一事务中更新正文及高亮：仍指向同类型节点且范围有效的高亮保留，其余高亮删除并向前端反馈数量。
  - 补充服务、HTTP 与浏览器验收，覆盖持久化、表格/公式/图片编辑，以及编辑后高亮保留和失效。
- 本步明确不纳入：
  - 不实现章、节、闪卡的新增、重命名、移动、排序或软删除。
  - 不实现多设备编辑锁、协同编辑、冲突合并或版本历史。
  - 不改动复习筛选、顺序切卡、掌握状态、AI 讲解、导出备份或资源目录迁移。
- 后续归属任务：
  - 章、节、闪卡维护和回收站入口归属 PH3-04。
  - 多设备同步和卡片编辑锁归属 PH3-05。
  - M3 标注与编辑整体验收归属 PH3-06。

### `PH4-03` 实现 Markdown 内容导出与 JSON 完整恢复

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：建立数据导出与替换式恢复闭环，Markdown 面向内容迁移，JSON 面向完整学习状态恢复。
- 本步纳入：
  - 提供活动资料的 Markdown 导出，包含资料、章、节、闪卡层级和当前结构化正文，不改写数据库源数据。
  - 提供 JSON 导出，包含资料层级、卡片正文、掌握状态、复习记录、高亮、当前 AI 讲解、回收站记录、资源元数据与资源内容。
  - 提供事务化 JSON 恢复，恢复前校验版本、结构、ID、层级关系和资源路径，并替换当前可恢复数据。
  - 导出和恢复响应、文件名与页面反馈不包含 API Key、密文、MySQL 密码或 Tunnel 凭证。
  - 补充服务/API 契约测试和资料页桌面、390x844 移动端导出/恢复入口验收。
- 本步明确不纳入：
  - 不实现每日自动备份、备份保留策略、备份恢复前留档或回收站永久删除。
  - 不导出或恢复 AI Provider 配置及其 API Key 密文，不实现账号登录、权限模型或公网发布。
  - 不新增 Markdown 源码编辑态，导出文件仅作为下载结果，不写回原始导入文件。
- 后续归属任务：
  - 每日备份、手动备份、恢复前自动留档和回收站永久删除归属 PH4-04。
  - AI Provider 配置历史或无密钥配置迁移作为后续独立需求评估。
  - 真实 MySQL、资源目录与浏览器组合验收归属 PH4-05。

### `PH5-03` 制作包含固定 Node.js 运行时的 Windows 发布包

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：制作可复制到目标 Windows 电脑的生产包：使用编译后的 API 同源托管 Web 静态文件，携带生产依赖和固定 Node.js 可执行文件，并提供配置模板及一键启停入口。
- 本步纳入：
  - 建立生产态 API 入口，在同一端口托管编译后的 Web dist 和 /api 请求。
  - 建立 Windows 发布包脚本，执行共享类型、Web、API 构建并组装生产目录。
  - 将 API 生产依赖、共享包产物、固定 Node.js 运行时和版本信息复制到发布包。
  - 提供不含秘密的 .env.example、数据库迁移入口、启动/停止 PowerShell 与可双击 CMD 入口。
  - 在当前 Windows 机器上用真实 MySQL、HTTP 健康检查和静态首页完成发布包冒烟验收。
- 本步明确不纳入：
  - 不打包或安装 MySQL，不自动创建数据库账号，不改变现有迁移和存储路径规则。
  - 不启动 Cloudflare Tunnel，不实现 review.panspan.cloud 公网访问或账号密码登录。
  - 不实现离线复习、离线编辑、后台同步或固定 Node.js 之外的系统依赖安装。
- 后续归属任务：
  - 单账号密码门禁和公网安全开放归属 PH5-04。
  - Tunnel 连接器启动、HTTPS 稳定性和发布安全归属 PH5-04 与 PH5-05。
  - Windows 本机、iPhone、MySQL、Tunnel、备份恢复和账号安全完整验收归属 PH5-05。

### `PH6-03` 实现题库 JSON/Excel 导入与模板

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：建立题库 JSON/Excel 的下载、解析、严格预览和原子写入 API，为后续导入页接入准备稳定协议。
- 本步纳入：
  - 提供章节题与真题/模拟题各自适用的 JSON、Excel 模板；题目 JSON/Excel 仅描述一套题库，课程、科目和入口类型由请求选择。
  - 校验题库名称、章节题章节、题干、题型、A-F 连续实际选项、答案、判断题固定选项、解析/知识点可空以及同一课程科目类型下的重名题库。
  - 支持导入预览、取消、应用和精确的行/字段错误；验证课程、科目及二者归属，使用事务写入题库、章节和题目。
  - 补充共享 API 契约、服务单元测试、HTTP 契约测试和真实 MySQL 导入回归。
- 本步明确不纳入：
  - 不接入现有导入页面、模板下载按钮、前端文件上传或任何题库管理页面。
  - 不支持题目图片、ZIP、Markdown、批量题库、同名覆盖/追加、AI 格式修正或从闪卡资料导入。
  - 不实现题目应用内编辑、刷题会话、统计、错题专项、题目 AI 讲解或题目与闪卡联动。
- 后续归属任务：
  - PH6-04 和 PH6-05 接入题库目录、题目管理及可视化编辑。
  - 导入页的内容/类型分段、模板图标、文件选择和预览界面随 PH6-04 的题库入口一并接入。
  - PH6-06 至 PH6-08 分别实现刷题、统计错题专项和题目 AI 讲解。

### `PH7-02` 实现错题备注、筛选与巩固复练

- 状态：已确认
- 确认日期：2026-08-20
- 边界摘要：在既有累计错题和会话机制上增加用户可控的错误备注与筛选复练，不引入自动归因或新的出题算法。
- 本步纳入：
  - 新增每题当前错误原因/备注的可视化编辑、读取和删除，以及独立于 practice_attempts 的持久化与备份恢复。
  - 复用最近一次有效作答为错误的累计错题口径，按同一科目内知识点、题型和最近错误时间筛选练习范围。
  - 从筛选结果创建背题或检测会话，并补充迁移、服务、HTTP、真实 MySQL、备份恢复与桌面/390px 回归。
- 本步明确不纳入：
  - 不自动生成、分类、改写或共享错误原因，不调用 AI 处理备注。
  - 不改变历史作答快照、题库累计统计、题目版本语义或收藏题会话范围。
  - 不实现随机复练、间隔重复、跨科目混合练习、错误图片或附件。
- 后续归属任务：
  - PH7-03 负责周期学习洞察。
  - 随机复练、间隔重复和提醒归属后续独立的复习策略阶段。
  - 复杂备注历史、富文本、附件或多用户共享须另立数据模型任务。

### `PH1-04` 实现单 Markdown/ZIP 解析与严格校验

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：建立单 Markdown/ZIP 导入包的解析与严格校验能力，为后续预览和数据库应用提供稳定的结构化结果与可定位错误；本步只处理解析边界，不进入导入应用链路。
- 本步纳入：
  - 解析单个 Markdown 文件，识别资料、章、节、闪卡四级标题层级。
  - 把正文转换为可供后续预览/写库使用的结构化节点，保留公式、GFM 表格和图片引用。
  - 对空标题、缺失层级、标题外正文和无法归属内容生成文件名、行号、上下文和修复建议。
  - 解析单个 ZIP 资料包，校验恰好一个 Markdown 文件、相对图片引用和路径穿越风险。
  - 补充纯函数与 ZIP 解析自动化测试。
- 本步明确不纳入：
  - 不实现导入预览界面、卡片标题/正文修正或数据库写入。
  - 不复制资源到正式资源目录，不做重复文件哈希检测。
  - 不解析 PDF、HTML 或多个 Markdown 文件，不实现前端导入页面。
- 后续归属任务：
  - 导入预览、修正和数据库应用归属 PH1-05。
  - 资源复制、重复跳过和合法资料完整入库验收归属 PH1-06。
  - 正文安全渲染与可视化编辑归属 PH2/PH3 内容渲染与编辑任务。

### `PH2-04` 实现正文、表格、公式、代码和图片安全渲染

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：在现有闪卡阅读页中，将导入时保存的结构化正文安全渲染为适合桌面与 iPhone 阅读的富内容。
- 本步纳入：
  - 为段落、标题、列表、引用、表格、行内/块级代码、链接、图片和数学公式提供安全的阅读态渲染。
  - 在阅读 API 与前端渲染层执行节点和链接协议白名单，拒绝原始 HTML、脚本和不安全资源地址。
  - 处理正文排版、无横向滚动的表格换行适配、公式布局、代码可读性与移动端适配。
  - 补充内容安全与渲染契约测试，并在桌面和 iPhone 尺寸验证实际页面。
- 本步明确不纳入：
  - 不实现正文、表格或公式的可视化编辑。
  - 不实现文本高亮、重点挖空或揭示状态。
  - 不实现 AI 讲解、备份、登录或改变既有筛选和掌握状态逻辑。
- 后续归属任务：
  - 高亮与挖空归属 PH3 任务。
  - 可视化正文和层级编辑归属 PH3 任务。
  - M2 主链路整体验收归属 PH2-05。

### `PH3-04` 实现章、节、闪卡维护和回收站入口

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：在资料管理页维护章、节和闪卡的层级、顺序与软删除，并提供只读的回收站入口。
- 本步纳入：
  - 提供资料树读取、章/节/闪卡新增与重命名、同层排序、跨父级移动及软删除的受校验 API，并在事务中维护连续排序。
  - 资料管理页使用既有 iOS 分组列表展示层级内容，提供新增、重命名、移动、排序和删除操作，以及删除确认。
  - 将软删除对象写入既有回收站记录，并提供可查看的回收站入口；复习与资料树均不再返回已删除内容。
  - 补充服务、HTTP 与浏览器验收，覆盖层级变更、排序一致性、软删除隔离和移动端布局。
- 本步明确不纳入：
  - 不实现回收站恢复、永久删除、导出、备份或恢复流程。
  - 不实现多设备编辑锁、协同编辑、冲突合并或版本历史。
  - 不改变既有正文可视化编辑、高亮挖空、复习筛选、掌握状态、AI 讲解或资源目录迁移。
- 后续归属任务：
  - 多设备同步和卡片编辑锁归属 PH3-05。
  - M3 标注与编辑整体验收归属 PH3-06。
  - 回收站恢复、永久删除、导出、备份和恢复归属 PH4-03、PH4-04。

### `PH4-04` 实现每日备份、恢复和回收站永久删除

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：补齐备份记录、数据库与资源归档、保留策略、恢复前自动留档，以及回收站永久删除的后端和资料页操作入口。
- 本步纳入：
  - 提供手动备份服务并复用同一流程作为每日备份入口：生成结构化 JSON 快照、资源归档和清单，写入 backup_records。
  - 增加每日备份调度配置与启动时触发检查；默认保留最近 7 份成功备份并清理更早备份目录及记录。
  - JSON 恢复前自动生成一份当前数据备份，备份失败时阻止恢复；恢复成功后保留恢复前留档。
  - 提供备份列表、备份触发、指定备份恢复和回收站永久删除 API；永久删除同步清理层级关联数据、资源文件和回收站记录。
  - 资料页增加备份/恢复列表与永久删除确认 Sheet，危险操作明确反馈并保持移动端无横向溢出。
  - 补充服务、HTTP、真实 MySQL/资源目录和桌面/390x844 移动端验收。
- 本步明确不纳入：
  - 不实现账号登录、权限模型、Cloudflare Tunnel 或公网发布。
  - 不改变 Markdown 导出和 JSON 文件格式，不导出或恢复 AI Provider 配置和密钥。
  - 不实现跨版本迁移、备份加密或多用户并发恢复。
- 后续归属任务：
  - 账号安全门禁归属 PH5-04；公网启动与 Tunnel 归属 PH5-02。
  - 真实环境下 M4 全链路组合验收归属 PH4-05。

### `PH5-04` 实现单账号密码登录和公网安全门禁

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：为单账号发布环境建立后端密码门禁和前端登录入口，使用安全哈希、受限失败重试和 HttpOnly 会话 Cookie；配置完成后 PUBLIC_ACCESS_READY 允许进入后续公网连接器验收。
- 本步纳入：
  - 通过 AUTH_USERNAME 和 AUTH_PASSWORD_HASH 配置单账号，密码只保存为 scrypt 哈希，不写入代码、进度记录或导出文件。
  - 提供登录、会话检查和退出接口，成功登录返回带 HttpOnly、SameSite=Lax、Max-Age 的会话 Cookie。
  - 对登录失败按客户端指纹限速，达到阈值返回 429 和 Retry-After；未登录请求拦截全部受保护 /api 业务接口。
  - 保留健康检查和认证接口公开访问，前端启动先恢复会话，未登录显示登录表单，登录后刷新不重复登录并提供退出操作。
  - PUBLIC_ACCESS_READY=true 时强制要求账号门禁配置，保持 Cloudflare Tunnel 是否启动由既有脚本单独控制。
- 本步明确不纳入：
  - 不实现多用户、注册、角色权限、二步验证或 Cloudflare Access。
  - 不在本步启动 Cloudflare Tunnel 或完成 review.panspan.cloud HTTPS 稳定性验收。
  - 不把登录凭证写入 JSON 备份、Markdown 导出、发布包或进度展示层。
- 后续归属任务：
  - Cloudflare Tunnel 连接器启动和公网 HTTPS 验收归属 PH5-05。
  - Windows 发布包中的实际环境账号初始化与公网 cookie 安全配置归属发布验收。
  - 多账号与更细权限模型不纳入第一版。

### `PH6-04` 实现课程科目下的题库目录管理

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：在既有课程到科目的资料目录中增加题库分段与目录管理，使空题库和已导入题库能在独立领域模型上被安全组织。
- 本步纳入：
  - 扩展共享 API 契约与认证服务，提供按科目读取题库目录，以及题库新建、改名、排序、软删除、恢复和章节目录管理。
  - 实现题库软删除、恢复与目录约束：删除会将题库及其章节题目一起移入回收站；章节删除前必须不含有效题目，章节移动只限同一科目的章节题库。
  - 在课程、科目路径下增加资料/题库分段、章节题/真题/模拟题入口、题库目录和导入入口；复用 PH6-03 的导入 API 与模板能力。
  - 为目录服务和 HTTP 契约补充单元/集成回归，并执行前端类型和生产构建校验。
- 本步明确不纳入：
  - 不实现题目新增、编辑、删除、排序或可视化题干/选项/解析编辑。
  - 不实现背题、检测、会话续答、统计、错题专项或题目 AI 讲解。
  - 不修改 PH6-03 导入文件格式、预览校验或原子写入规则，不支持题目图片、随机出题或题目与闪卡联动。
- 后续归属任务：
  - PH6-05 负责题目可视化编辑与题目级管理。
  - PH6-06 至 PH6-08 分别负责刷题会话、统计错题专项和题目 AI 讲解。
  - PH6-09 负责题库主链路、备份恢复和浏览器端整体验收。

### `PH7-03` 实现周期学习洞察与薄弱知识点

- 状态：已确认
- 确认日期：2026-08-20
- 边界摘要：从已存在的复习记录、状态历史和已完成刷题会话计算周期洞察，仅呈现可解释数据而不改变用户的复习节奏。
- 本步纳入：
  - 按 Asia/Shanghai 聚合近 7 天、30 天有效闪卡复习量、四种掌握状态变化和已完成会话的题库正确率。
  - 从有效题目的知识点和已完成作答计算满足最小样本量的薄弱知识点，并同时显示作答数和正确率。
  - 提供认证 API、响应式洞察页面、图表文本摘要、空状态和真实 MySQL 边界日期回归。
- 本步明确不纳入：
  - 不写入新的学习历史，不改写 review_records、review_status_history、practice_sessions 或 practice_attempts。
  - 不提供打卡、连续天数、排名、推送、AI 建议、预测、学习目标或自动复习排程。
  - 不将样本量不足的知识点标记为薄弱，不把进行中会话、软删除内容或跨时区日期混入统计。
- 后续归属任务：
  - 学习目标、提醒、间隔重复和自动推荐归属后续复习策略阶段。
  - 多课程比较、长期自定义日期和数据导出归属后续分析能力任务。
  - 更多可视化图表或账户级报表须根据真实使用反馈另立任务。

### `PH1-05` 实现导入预览、修正和数据库应用

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：完成单个 Markdown/ZIP 文件从选择、预览、修正到应用的最小闭环；预览使用短期令牌保存解析结果，应用时事务写入 MySQL 并安全托管 ZIP 资源。
- 本步纳入：
  - 建立导入预览 API：接收单个 .md/.zip，返回层级预览、结构错误、资源摘要和源文件哈希重复状态。
  - 建立短期预览令牌与应用 API，允许在不展示 Markdown 源码的前提下修正资料、章、节和闪卡标题及卡片正文文本。
  - 将合法且确认应用的资料、章、节、闪卡写入 MySQL；同源文件默认按 SHA-256 跳过重复导入。
  - 将 ZIP 资源复制到受管资源目录，写入资源元数据，并保证数据库事务或文件复制失败时不留下不可追踪的半成品。
  - 建立中文导入页面，支持选择文件、查看层级与错误、修正可编辑字段、应用/取消和结果反馈。
  - 为预览令牌、事务应用、重复跳过、资源托管和 HTTP 错误补充自动化测试。
- 本步明确不纳入：
  - 不实现既有资料编辑、复习面板、掌握状态、高亮挖空或 AI 讲解。
  - 不实现最终正文安全渲染、公式/表格可视化编辑和资源访问接口；预览只展示结构化节点摘要。
  - 不支持 PDF、HTML、多个文件批量导入，不开放公网 Tunnel，不实现账号登录。
- 后续归属任务：
  - 真实 MySQL、合法与非法样本的完整主链路验收归属 PH1-06。
  - 正文安全渲染与可视化结构编辑归属 PH2/PH3 内容渲染与编辑任务。
  - 资源访问、替换、回收站、导出和备份归属 PH3/PH4 数据治理任务。
  - 单账号密码保护和公网发布归属 PH5-04 及后续发布验收。

### `PH2-05` 验收 M2 连续复习主链路

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：对既有复习能力执行 M2 端到端验收，确认用户可在真实数据上完成从面板进入到连续复习的闭环。
- 本步纳入：
  - 使用隔离的真实 MySQL 验收资料，覆盖复习面板进入、资料/章/节/状态/关键词组合筛选、富内容阅读和结果集内前后切换。
  - 验证掌握、了解、努力状态即时保存、查看记录更新、结果集与面板统计同步，以及末张卡片不循环的行为。
  - 以 HTTP 和浏览器自动化验证桌面与 390x844 移动视口的完整主链路，并运行类型检查、单元测试、构建和进度校验。
  - 补齐仅为主链路验收所必需的测试夹具、测试脚本或已发现缺陷的最小修复。
- 本步明确不纳入：
  - 不实现文本高亮、重点挖空、可视化编辑或 AI 讲解。
  - 不实现导出、备份恢复、PWA、Cloudflare Tunnel 配置或账号登录。
  - 不改动既有资料导入协议、层级排序规则和产品范围。
- 后续归属任务：
  - 高亮、挖空与可视化编辑归属 PH3 任务。
  - AI 讲解、导出、恢复和每日备份归属 PH4 任务。
  - PWA、Tunnel、账号密码和公网验收归属 PH5 任务。

### `PH3-05` 实现多设备同步和卡片编辑锁

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：为闪卡正文编辑增加基于设备标识和后端租约令牌的单卡编辑锁，并在浏览器编辑流程中自动获取、续期和释放。
- 本步纳入：
  - 提供获取、续期、释放编辑锁的受校验 API；同一闪卡已有其他设备有效租约时返回冲突，过期租约可被新设备接管。
  - 正文保存必须校验设备标识、锁令牌和租约有效期，并在同一事务内与正文、高亮失效处理保持一致。
  - 浏览器为每个设备生成并持久化本地设备标识，进入编辑前获取锁，编辑期间定时续租，保存、取消、返回和切卡时释放。
  - 补充服务、HTTP、真实 MySQL 和桌面/390x844 移动端验收，验证双客户端竞争、续租、释放和保存拦截。
- 本步明确不纳入：
  - 不实现正文冲突合并、版本历史、离线写入队列或协同光标。
  - 不限制阅读、挖空、标注、掌握状态和资料层级操作。
  - 不实现账号登录、权限模型或跨用户设备管理。
- 后续归属任务：
  - M3 完整回归由 PH3-06 持续覆盖；公网账号保护归 PH5-04。

### `PH4-05` 验收 M4 AI 与数据治理主链路

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：用隔离的真实 MySQL、资源目录和本地 OpenAI 兼容假 Provider 串联验收 M4 主链路。
- 本步纳入：
  - 创建带结构化正文、图片资源、复习状态、高亮和 AI Provider 的最小真实夹具。
  - 通过 HTTP 验证 AI 讲解保存与读取、Markdown 导出、JSON 导出敏感字段排除、备份资源副本、JSON 恢复和回收站永久删除。
  - 验证 ISO 时间导出值可在 MySQL DATETIME(3) 严格模式下恢复。
  - 验证临时资源目录、备份目录和数据库夹具在测试结束后清理。
- 本步明确不纳入：
  - 不扩展 AI Provider、讲解、导出、恢复、备份或永久删除的业务能力；仅修复本次真实恢复验收暴露的时间格式兼容问题。
  - 不新增账号登录、Cloudflare Tunnel、PWA 或公网发布能力。
  - 不把真实 API Key、数据库密码或其他凭证写入测试输出和进度文档。
- 后续归属任务：
  - 恢复过程中的磁盘异常与数据库/资源目录一致性改进继续归属 RISK-012 后续治理。
  - iPhone、Tunnel、发布包和账号安全归属 PH5-01 至 PH5-05。

### `PH5-05` 完成 GA 全链路验收

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：修复 Windows 一键启动在应用已运行时无法补拉 Cloudflare Tunnel 的生命周期缺口，并在账号门禁已完成后验收 review.panspan.cloud HTTPS。
- 本步纳入：
  - 修复 start.ps1 在应用进程已运行但 Tunnel 未记录时的补启动逻辑，并同步更新受管进程状态与独立日志。
  - 开启本机 CLOUDFLARED_ENABLED，使用现有 config.yml、用户目录凭证和已完成的账号门禁启动连接器。
  - 运行 tunnel:check、本机健康检查和 review.panspan.cloud HTTPS 冒烟验证。
- 本步明确不纳入：
  - 不修改 Cloudflare Tunnel、DNS 路由、ingress 规则或凭证内容。
  - 不改变账号认证、Cookie 安全属性、MySQL、资料业务和移动端范围。
  - 不把本机 .env.local、Tunnel 凭证或密码写入进度源、导出文件或发布包。
- 后续归属任务：
  - iPhone Safari、备份恢复和其余 GA 回归继续归属 PH5-05 后续验收。
  - 公网长期可用性和连接器故障告警不在本最小修复内。

### `PH6-05` 实现可视化题目编辑

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：在 PH6-04 题库目录内增加题目级管理和可视化编辑，沿用题库模型与导入校验，先完成可独立验收的题目 CRUD 主链路。
- 本步纳入：
  - 提供按题库读取有效题目及回收站题目，题目新增、编辑、移动题库/章节、上下排序、软删除和恢复 API。
  - 可视化编辑题干、选项、解析和知识点；题型切换、选项动态增删（最多 A-F 六项）、答案选择复用既有严格校验，判断题固定为 A: 对、B: 错。
  - 在题库详情中展示题目列表、题型和答案摘要，并以编辑抽屉/表单完成新增和修改，不出现 Markdown 或 LaTex 源码编辑态。
  - 补充题目服务校验单测，并通过类型检查与生产构建验证前后端契约。
- 本步明确不纳入：
  - 不实现背题、检测、会话续答、统计、错题专项或题目 AI 讲解。
  - 不实现题目图片、随机抽题、部分得分、主观题 AI 评分或题目与闪卡知识点联动。
  - 不修改 PH6-03 导入文件格式、预览令牌和原子导入规则，不提供 Markdown/LaTex 源码输入模式。
- 后续归属任务：
  - PH6-06 负责题目背题、检测和会话快照。
  - PH6-07 负责统计与错题专项；PH6-08 负责题目 AI 讲解和版本历史。
  - PH6-09 负责三类题库、编辑、刷题、统计、备份恢复和浏览器端整体验收。

### `PH7-04` 重构学习洞察统计图表

- 状态：已确认
- 确认日期：2026-08-20
- 边界摘要：只重构学习洞察前端图表与统计呈现，复用既有只读数据契约。
- 本步纳入：
  - F2 Hairline Line 风格的逐日复习量图表。
  - 状态变化、题库结果和薄弱知识点的紧凑表格化呈现。
  - 桌面与 iPhone 响应式样式、文本摘要与图表无障碍标注。
- 本步明确不纳入：
  - 不写入或改写任何学习历史与题库作答快照。
  - 不改变 API 返回字段和统计结果。
- 后续归属任务：
  - 账户级报表、多课程对比与长期自定义周期。

### `PH1-06` 验收 M1 资料导入主链路

- 状态：已完成
- 确认日期：2026-08-10
- 边界摘要：用隔离的真实 MySQL、受管资源目录和真实 HTTP 导入流程完成 M1 验收；只补足验收自动化与阻碍验收的缺陷，不新增产品能力。
- 本步纳入：
  - 建立受控的真实 MySQL 验收夹具，走 /api/import/preview 到 /api/import/apply 的完整 HTTP 链路，并在结束后仅清理本次生成的数据和资源。
  - 验证合法 Markdown 与含相对图片的 ZIP 可完整写入资料、章、节、闪卡和资源记录，并核验顺序、正文、资源字节和 SHA-256。
  - 验证非法层级、缺失图片和 ZIP 路径穿越在预览阶段被阻止，且不会留下数据库或资源副作用。
  - 验证重复源文件默认跳过，导入前后源文件的字节、SHA-256 和修改时间保持不变。
  - 验证取消预览及 API 重启后的预览令牌失效提示，确认 RISK-009 当前的进程内令牌边界。
  - 通过浏览器文件选择流程检查中文导入页的预览、错误定位、应用和取消交互。
- 本步明确不纳入：
  - 不新增 PDF、多文件批量导入、解析协议或导入后的资料编辑能力。
  - 不实现正文安全渲染、公式/表格/图片的可视化无损修正、高亮挖空、复习、AI 或登录。
  - 不将预览令牌改为跨进程持久化，也不改变公网 Tunnel 安全门禁。
- 后续归属任务：
  - 正文安全渲染、复杂正文无损修正和资源访问归属 PH2-04、PH3-03 与 PH3-04。
  - 预览令牌跨重启恢复如有需要，作为后续可靠性任务另行评估。
  - 复习、标注、AI、备份与账号安全按 PH2 至 PH5 的既定任务推进。

### `PH3-06` 验收 M3 标注与编辑主链路

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：用真实 MySQL、HTTP 和两个独立客户端请求，对第三阶段已经交付的标注、挖空所需数据、可视化编辑、层级维护和软删除隔离做一次组合回归。
- 本步纳入：
  - 验证文本、表格文字和公式高亮可通过 HTTP 保存、重读，并在正文编辑后正确清理失效锚点、保留未改变锚点。
  - 验证两个独立客户端可同时读取同一张卡片，并在一次编辑后看到一致的正文和高亮结果；本步不引入设备身份协议。
  - 验证章、节、闪卡新增、改名、移动、排序、级联软删除和回收站隔离在组合流程中保持一致。
  - 保留 PH3-02 的浏览器挖空验收作为本步回归证据，并补充可重复的真实 MySQL + HTTP 自动化测试。
- 本步明确不纳入：
  - 不实现 sync_locks 的获取、续期、释放、过期和编辑接口拦截。
  - 不实现回收站恢复、永久删除、导出、备份或恢复。
  - 不改变 AI 讲解、复习筛选、资源目录迁移或账号安全。
- 后续归属任务：
  - 多设备正文编辑锁和设备身份归属 PH3-05。
  - M3 完成后的 AI 与数据治理归属 PH4。

### `PH4-06` 修复本机 AI Provider 加密配置

- 状态：进行中
- 确认日期：2026-08-11
- 边界摘要：补齐当前开发机缺失的 AI Provider 密钥加密口令，并验证新增配置流程。
- 本步纳入：
  - 在本机 .env 配置随机生成且长度不少于 32 个字符的 AI_PROVIDER_KEY_ENCRYPTION_SECRET。
  - 验证 API 配置加载和新增 Provider 的加密写入流程。
  - 同步必要的进度验证记录。
- 本步明确不纳入：
  - 不修改 AES-256-GCM 加密实现或移除未配置时的安全阻断。
  - 不把真实口令写入代码、Markdown、JSON 备份或测试夹具。
  - 不扩展 Provider 类型、模型发现或公网安全能力。
- 后续归属任务：
  - M4 全链路真实环境组合验收归属 PH4-05。
  - 生产环境密钥轮换和账号安全门禁归属后续发布与安全任务。

### `PH5-06` 实现全局秋日枫叶皮肤与专注复习反馈

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：在不改变复习信息架构的前提下，交付可切换的全局秋日枫叶皮肤、AI 请求真实取消及无文字生成反馈，并优化重点挖空遮罩。
- 本步纳入：
  - 在设置页外观分组提供简约与秋日枫叶选择，并使用本机存储持久化选择；秋日枫叶提供跟随系统的浅色枫叶与深色夜枫 token。
  - 将主题 token 应用于全局页面背景、导航、分组列表、Sheet、表单和交互细节；闪卡标题与正文保持高对比、无叶片和无纹理的阅读表面。
  - 为 AI 讲解生成建立端到端取消信号：前端停止图标可中止当前请求，后端将取消传递给 Provider；取消后保留旧讲解且不写入半成品。
  - 生成期间仅显示无文字主题化不定进度环和独立停止图标；支持 ARIA 名称、动态状态提示和减少动态效果。
  - 将文本与公式挖空优化为原文字数占位、暖灰底、枫棕细描边和极淡叶脉纹理；短词不加纹理，点击单项后以 120-180ms 淡入揭示。
  - 补充主题持久化、真实取消、取消后数据不变、文本/公式遮罩以及桌面和 iPhone 尺寸无溢出的自动化验收。
- 本步明确不纳入：
  - 不改变资料、导入、复习筛选、掌握状态、编辑、备份、认证、Tunnel 或发布流程的功能边界。
  - 不实现 AI 流式输出、批量生成、队列、费用统计、历史版本或取消后的用量退款。
  - 不新增其他季节皮肤、在线皮肤市场、跨设备皮肤同步、循环背景动画或会干扰正文的装饰素材。
- 后续归属任务：
  - 其他季节皮肤与跨设备外观同步在确认真实使用需求后另建独立任务。
  - AI 流式输出、批量生成、费用可见性和讲解历史继续按既有 AI 范围独立评估。

### `PH6-06` 实现背题与检测会话

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：在 PH6-05 题目主链路上增加可恢复的背题和检测会话，服务端持久化不可变题目快照并隔离检测答案。
- 本步纳入：
  - 按整套或章节启动 cram 背题、test 检测，创建会话和每题作答快照；章节仅限章节题库。
  - 支持未完成会话列表、继续、单题作答、检测返回修改、完成和放弃；单选/判断前端自动进入下一题，多选由用户确认后进入下一题。
  - 背题作答返回正确/错误反馈与正确答案；检测作答和会话读取不返回正确答案，完成后才返回结果摘要。
  - 补充共享契约、会话服务单测和认证 HTTP 契约，验证题目版本快照、答案隔离、事务写入和会话状态转换。
- 本步明确不纳入：
  - 不实现统计、正确率趋势、累计错题专项或答对后移出规则。
  - 不实现题目 AI 讲解、临时提示词、版本历史或题目与闪卡知识点联动。
  - 不实现随机抽题、部分得分、主观题 AI 评分、跨题库会话和修改既有题库导入格式。
- 后续归属任务：
  - PH6-07 负责完成会话后的统计、正确率、题型/模式累计和错题专项。
  - PH6-08 负责题目 AI 讲解和题目版本历史标记。
  - PH6-09 负责真实 MySQL、备份恢复与桌面/390px 刷题链路整体验收。

### `PH5-07` 建立课程、科目与资料九宫格导航及学习趋势

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：在保留资料→章→节→闪卡内容模型和 Markdown 标题协议的前提下，新增课程/科目目录层，使用逐层 URL 导航进入资料九宫格和资料详情，并为资料封面与四种掌握状态趋势建立可恢复的数据边界。
- 本步纳入：
  - 新增单层课程和课程下单归属科目，支持新增、改名、手动排序、科目移动、空节点删除确认和非空节点删除阻止。
  - 资料入口使用课程→科目→资料逐层 URL；课程/科目为单列列表，科目页资料使用桌面 3 列、iPhone 2 列的九宫格，不分页。
  - 资料格显示可选封面、资料名称和有效闪卡数量；无封面时封面区域透明；资料详情页提供名称/封面 Sheet 编辑、章/节/单卡列表和失效 URL 返回。
  - 封面只接受本地 PNG/JPEG/GIF/WebP、最大 5MB，保存原图并生成最长边 512px 缩略图；资料删除、恢复、永久删除和资源清理保持一致。
  - 导入应用前强制选择课程和科目并支持选择器内新增；相同源文件按 SHA-256 全局去重；现有资料迁移到不可删除的“待整理 / 待整理”。
  - 资料详情页显示掌握、了解、努力、未评估四状态分布和最近 30 天按 Asia/Shanghai 的每日状态快照趋势；状态变更历史长期保留，新卡导入记录初始未评估，迁移日建立既有卡片初始快照。
  - JSON 导出、每日备份和恢复包含课程、科目、排序、封面资源和状态历史；旧 JSON 缺少新增字段时自动归入默认课程/科目；Markdown 导出不加入这些元数据。
  - 补充 API、数据库迁移、导入/恢复兼容、资源清理、统计计算和桌面/iPhone 浏览器验收，覆盖刷新、前进/后退、无数据状态和无障碍图表摘要。
- 本步明确不纳入：
  - 不改变 Markdown 的 # 资料、## 章、### 节、#### 闪卡标题协议，不在前端提供 Markdown 源码编辑。
  - 不把课程或科目加入复习页筛选；从课程/科目开始复习只转换为现有资料集合筛选。
  - 不新增空白资料、PDF/OCR、多文件批量导入、网络图片 URL、在线裁剪器、课程/科目封面或资料搜索。
  - 不实现分页、拖拽跨层移动、课程多级嵌套、共享科目、实时推送或自定义时区。
  - 不改变账号门禁、Cloudflare Tunnel、AI Provider、离线复习、随机复习、间隔重复、提醒和多用户权限。
- 后续归属任务：
  - 资料名称搜索、按课程/科目筛选复习和跨资料统计归属后续独立任务。
  - 长期趋势的 7/90 天切换、更多图表交互和课程/科目视觉封面后续独立任务。
  - 课程/科目共享、批量迁移和拖拽排序在真实资料规模验证后再评估。

### `PH6-07` 实现题库统计与错题专项

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：在 PH6-06 可恢复会话基础上增加完成结果、题库统计和错题专项，统计口径固定为已完成会话。
- 本步纳入：
  - 新增题库总览、章节题章节、题型和 cram/test 模式累计统计，返回总题数、已作答、未作答、正确、错误、百分比正确率和最近完成时间。
  - 检测完成后展示本次结果，并允许从指定已完成会话启动本次错题练习；题库页可启动累计错题练习。
  - 累计错题按每题最近一次已完成结果保留，后续已完成会话答对后从累计错题中移除；错题练习继续复用不可变题目快照。
  - 补充共享契约、统计/错题服务、认证 HTTP 契约和单元测试，并接入题库工作区。
- 本步明确不纳入：
  - 不实现日期趋势图、日期筛选、部分得分、随机抽题、主观题 AI 评分或跨题库统计。
  - 不实现题目 AI 讲解、知识点联动、答对后修改题目或错题的手工移除。
  - 不在本步执行真实 MySQL、备份恢复和桌面/390px 浏览器整体验收。
- 后续归属任务：
  - PH6-08 负责题目 AI 讲解和题目版本历史标记。
  - PH6-09 负责真实 MySQL、备份恢复与桌面/390px 刷题链路整体验收。

### `PH5-08` 补齐本机深浅色显示模式切换

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：为现有外观设置补齐本机明暗模式选择，使用户能在不改变皮肤的前提下手动切换浅色或深色。
- 本步纳入：
  - 在设置页外观分组新增跟随系统、浅色、深色三档显示模式分段控件。
  - 将显示模式写入本机存储，并通过根元素主题属性让简约与秋日枫叶皮肤即时使用对应 token。
  - 补充桌面与 iPhone 视口的构建和浏览器验收，确认切换、持久化、无横向溢出和无障碍名称。
- 本步明确不纳入：
  - 不新增皮肤、跨设备同步、账户级外观偏好或服务端设置接口。
  - 不修改复习、资料、导入、AI、认证、Tunnel、备份或课程目录业务。
  - 不改变闪卡正文的纯净阅读表面、内容宽度或既有无障碍规则。
- 后续归属任务：
  - 更多皮肤及跨设备或账户级偏好在确认真实需求后另建独立任务。

### `PH6-08` 实现题目 AI 讲解与版本历史

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：在现有题目编辑和刷题会话基础上增加题目级 AI 讲解，多版本追加保存并对题目版本变化做过时提示。
- 本步纳入：
  - 新增题目 AI 讲解共享契约、历史查询和生成 API；生成只读取当前有效题目与可选本次作答上下文。
  - 复用当前启用 AI Provider 的解密、超时和取消机制；每次成功生成向 question_ai_explanations 追加一条版本，不覆盖旧记录。
  - 在题目详情提供折叠式 AI 讲解区、临时提示词、生成/停止、历史版本和安全文本渲染；题目版本变化时标注讲解可能过时。
  - 补充服务与 HTTP 契约测试，并完成类型检查、测试、构建和进度源校验。
- 本步明确不纳入：
  - 不实现题目图片理解、主观题 AI 评分、自动改题、自动重试或 Provider 管理页改造。
  - 不修改题库导入格式、题目编辑校验、刷题判分、统计口径或闪卡既有 AI 讲解存储。
  - 不在本步完成真实 MySQL 全链路和桌面/移动浏览器验收。
- 后续归属任务：
  - PH6-09 负责真实 MySQL、备份恢复和桌面/390px 浏览器整体验收。
  - 后续独立任务负责题目图片、多模态 Provider 或 AI 评分能力。

### `PH5-09` 修复 AI 讲解 Markdown 渲染

- 状态：进行中
- 确认日期：2026-08-11
- 边界摘要：将已保存 AI 讲解的 Markdown 文本在复习卡片内解析为安全、只读的结构化展示，并在本次生成成功完成时自动展开结果。
- 本步纳入：
  - 复用现有正文节点渲染规则，解析 AI 讲解中的标准 Markdown、GFM 表格和数学公式。
  - 兼容模型输出的中文引号包裹型行内强调，将 `**“强调文本”**` 与 `*“强调文本”*` 转换为只读强调节点。
  - 保留现有安全链接协议限制，禁止渲染 AI 响应中的原始 HTML，且不执行任何外部脚本。
  - 仅在当前卡片的 AI 讲解请求成功完成后自动展开讲解区，使新结果立即可见。
  - 补充构建与浏览器回归，验证标题、强调、列表和分隔线不再以 Markdown 标记原样显示。
- 本步明确不纳入：
  - 不改动 AI Provider、提示词、生成请求、保存格式或历史数据。
  - 不新增 AI 流式输出、富文本编辑、图片加载、导出格式或正文编辑能力。
  - 不改变复习卡片已有高亮、挖空和掌握状态逻辑。
  - 不改变首次进入或切换卡片时的默认折叠、用户手动展开/收起，以及生成失败或取消时的折叠状态。
- 后续归属任务：
  - AI 响应格式约束、流式输出和历史版本继续按后续独立 AI 任务评估。

### `PH6-09` 验证题库主链路与备份恢复

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：对第六阶段题库主链路执行真实 MySQL、HTTP、备份恢复和桌面/390px 浏览器的整体验收，记录环境阻塞并只修复本阶段回归。
- 本步纳入：
  - 运行题库导入、目录、题目编辑、刷题、统计错题和题目 AI 的真实 MySQL/HTTP 集成测试，并检查认证门禁。
  - 验证 JSON v2 导出/恢复包含题库、题目 AI 版本、会话和作答快照，且不暴露 API Key。
  - 启动本地 Web/API，使用桌面和 390px 视口检查题库入口、题目编辑、刷题结果、AI 历史、无横向溢出和答案隐藏。
  - 对发现的 PH6 回归补最小代码或测试修复，并同步风险、日志和自动生成进度展示层。
- 本步明确不纳入：
  - 不新增题库产品能力，不改变已冻结的题型、统计、错题、AI 版本或备份契约。
  - 不使用真实 Provider API Key 进行联网生成，不记录或导出任何密钥。
  - 不把无法启动的外部 MySQL、浏览器权限或 Provider 环境伪装为已验收。
- 后续归属任务：
  - 真实 Provider 多模型兼容和公网 Tunnel 验收另立交付任务。

### `PH5-10` 新增 JSON 与 Excel 资料导入及模板下载

- 状态：进行中
- 确认日期：2026-08-11
- 边界摘要：在保留现有 Markdown/ZIP 导入链路的前提下，新增内容级 JSON 与 Excel 单资料协议及前端模板下载，并统一进入既有预览与应用流程。
- 本步纳入：
  - 定义并校验 format/version/title/chapters 的内容级 JSON；每张闪卡的正文 body 复用 Markdown/GFM/公式安全解析。
  - 定义并校验单工作表 Excel 的“资料、章、节、闪卡、正文”五列；每行转换为一张闪卡。
  - 扩展导入预览的来源类型、可识别错误、来源哈希重复检测与原有应用事务。
  - 本次最小步骤：来源哈希去重仅检索未删除资料，回收箱中的资料不会导致新上传资料在预览或应用阶段被跳过。
  - 本次最小步骤：新增迁移，将来源哈希唯一索引改为普通查询索引，允许回收箱资料保留后重新导入相同源文件。
  - 本次最小步骤：JSON 下载模板提供同级的“第一章”和“第二章”示例；每章各保留一个最小可解析的小节和闪卡，用于展示多章节填写结构。
  - 本次最小步骤：移除对疑似 GFM 表格分隔行的猜测式预校验；只有 Markdown 解析器实际识别为表格时才校验表格结构，含 -、:、| 的普通正文必须保留并可导入。
  - 在导入页提供 Markdown、JSON、Excel 选择，以及 JSON/Excel 模板下载、格式说明和桌面/iPhone 回归。
  - 补充 JSON、Excel、模板下载、错误定位和既有 Markdown 回归测试。
- 本步明确不纳入：
  - 不改动完整 JSON 备份的导出、恢复、确认流程或历史数据格式。
  - 不实现多文件/批量导入、CSV、PDF、OCR、ZIP 内 JSON/Excel、资源文件或图片导入。
  - 不导入课程科目归属、封面、学习状态、标注、AI 讲解、备份内容、API Key 或其他设置。
  - 不改变回收箱资料的恢复、永久删除、保留期限或资料移动逻辑。
  - 不改变 JSON 导入协议、章节排序、Excel 模板或完整 JSON 恢复行为。
  - 不自动修复或改写上传文件中的 Markdown 文本；跨单元格或跨 Markdown 节点的内联高亮校验规则保持不变。
  - 不改变既有 Markdown/ZIP 标题协议、可视化编辑、复习、资料目录或账号门禁。
- 后续归属任务：
  - 批量资料导入、CSV、PDF/OCR、资源包和跨格式内容迁移在需求确认后分别建立独立任务。
  - 完整 JSON 数据备份的恢复继续归属现有数据治理流程。
  - 回收箱恢复、永久删除和保留策略继续归属 PH4-04 数据治理流程。

### `PH6-10` 重构独立刷题页与题目解析入口

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：将已完成的刷题会话能力整理为独立页面，并补齐题目级 AI 解析入口与背题手动翻题交互。
- 本步纳入：
  - 将 /review/practice/:sessionId 渲染为独立全页刷题界面，并让题库工作区启动的会话复用同一页面结构。
  - 在当前题目提供 AI 解析入口，复用既有题目 AI 历史、生成、停止和安全渲染能力，并传入当前作答上下文。
  - 背题模式作答后不自动切题，展示反馈与解析，点击下一题后才进入下一题；检测模式既有行为保持不变。
  - 补充响应式页面样式、类型检查、构建和浏览器交互验收。
  - 优化选项作答响应：前端选中和检测模式切题即时完成，持久化改为后台提交；作答接口仅返回当前题目的增量结果，避免每次选择重新传输和替换整套会话。
- 本步明确不纳入：
  - 不修改题目 AI 后端、Provider、版本存储、统计口径、错题规则，以及会话创建、完成或放弃 API。
  - 不实现随机出题、部分得分、主观题评分、题目图片或跨题库会话。
  - 不改变复习闪卡页面、题库编辑字段和现有数据备份协议。
  - 不改变作答校验、背题判分、检测答案隐藏或会话完成规则。
- 后续归属任务：
  - 真实 Provider 多模型兼容和联网生成另立任务。
  - 新题型或刷题统计可视化另立任务。

### `PH5-11` 临时屏蔽本机账号密码登录

- 状态：暂缓
- 确认日期：2026-08-11
- 边界摘要：为缩短本机状态查看流程，临时停用公网连接器并屏蔽本机账号密码门禁；保留原有凭证和认证实现，后续可通过本机配置恢复。
- 本步纳入：
  - 将本机 PUBLIC_ACCESS_READY 和 CLOUDFLARED_ENABLED 设为 false，停止受管 Cloudflare Tunnel。
  - 仅在本机忽略配置中清空 AUTH_USERNAME 与 AUTH_PASSWORD_HASH 的生效值，不删除或记录原有哈希。
  - 重启本项目受管 API 与 Web 进程，并验证认证会话接口和受保护业务接口均以已认证状态响应。
- 本步明确不纳入：
  - 不修改认证源代码、会话 Cookie、失败限速或前端登录实现。
  - 不修改 Cloudflare Tunnel、DNS、凭证、数据库、资料业务或 Windows 发布包。
  - 不把账号、密码、哈希或 Tunnel 凭证写入进度源或 Markdown 展示层。
- 后续归属任务：
  - 恢复本机账号密码门禁和公网连接器时，沿用 PH5-04 的认证实现并重新执行 PH5-05 公网安全回归。
  - 多账号、权限模型和长期公网监控不纳入本步。

### `PH6-11` 放开题干长度限制

- 状态：已完成
- 确认日期：2026-08-20
- 边界摘要：取消题干的 255 字符业务上限，同时保留内容安全边界和其他题库字段约束。
- 本步纳入：
  - 调整题库 JSON/Excel 导入校验，使题干按既有 100 万字符内容上限校验而非 255 字符。
  - 确认应用内可视化题目保存不因题干超过 255 字符被拒绝，并补充服务回归测试。
  - 补充导入回归测试，验证 256 字符以上题干可预览和应用。
- 本步明确不纳入：
  - 不移除结构化内容的 100 万字符防护上限，也不放开节点数量、层级、图片、公式或链接安全校验。
  - 不改变题库名称、章节标题、选项、知识点、解析和其他字段的既有长度限制。
  - 不修改题型、选项连续性、答案、判断题、事务写入、备份或刷题规则。
- 后续归属任务：
  - 如需调整其他字段的长度策略，另立题库输入约束任务。

### `PH5-12` 恢复公网账号门禁与 Tunnel

- 状态：进行中
- 确认日期：2026-08-11
- 边界摘要：按用户新增的对外提供要求，恢复 PH5-04 单账号门禁与既有项目 Tunnel；不扩展本机免登录例外。
- 本步纳入：
  - 恢复本机忽略配置中已注释的 AUTH_USERNAME、AUTH_PASSWORD_HASH、PUBLIC_ACCESS_READY 和 CLOUDFLARED_ENABLED 原有效值。
  - 重启受管 API、Web 和项目 Cloudflare Tunnel，并验证本机健康检查、未登录拦截和公网 HTTPS 首页。
- 本步明确不纳入：
  - 不修改认证源代码、账户、密码哈希、Cookie、失败限速、Tunnel、DNS 或凭证内容。
  - 不实现仅本机免登录例外，不改变资料、数据库、导入或发布包。
  - 不把账号、密码、哈希或 Tunnel 凭证写入进度源或 Markdown 展示层。
- 后续归属任务：
  - 兼顾本机免登录查看与公网认证的受限访问方案，待单独确认安全模型后建立新任务。
  - 其余 GA 回归继续归属 PH5-05。

### `PH6-12` 实现题目收藏与科目收藏题练习

- 状态：已完成
- 确认日期：2026-08-20
- 边界摘要：将题目收藏持久化，并以当前科目下的所有有效收藏题创建独立、可恢复的练习会话。
- 本步纳入：
  - 为有效题目新增跨设备持久化收藏状态，提供收藏切换 API、题库题目列表状态与刷题页单题操作。
  - 在课程复习工作台的每个科目题库分段汇总收藏题数量，并以固定题库排序创建该科目全部收藏题的背题或检测会话。
  - 收藏题会话保存创建时题目快照和科目范围，支持既有作答、完成、放弃、URL 恢复与继续会话；不记入任一单题库的累计统计。
  - 将收藏状态和科目范围会话纳入完整 JSON 导出恢复，补充数据库迁移、服务、HTTP 和前端类型回归。
- 本步明确不纳入：
  - 不实现收藏夹名称、标签、批量收藏、随机抽题、间隔重复、提醒或收藏统计趋势。
  - 不改变既有题库/章节/题型/模式统计、错题口径、判题逻辑、题目 AI 协议或导入文件格式。
  - 不修改闪卡收藏、资料筛选、用户权限模型或题目软删除语义。
- 后续归属任务：
  - 收藏夹分组、收藏导入导出格式、随机收藏练习和跨科目收藏练习另立后续任务。

### `PH5-13` 按资料续读并简化复习资料选择

- 状态：进行中
- 确认日期：2026-08-11
- 边界摘要：将复习续读位置从全局单张闪卡改为按资料保存，并将复习筛选收敛为资料与掌握状态；在复习首页补充仅当前复习会话生效的背默模式和当前学习资料选择。
- 本步纳入：
  - 在打开、前后切换和状态自动切卡时按资料持久化最后一张有效闪卡。
  - 复习面板提供当前学习资料选择；选择器及其选项只显示资料名称，所选资料统一决定顶部统计和快捷开始范围。
  - 为每份资料返回闪卡、掌握、未评估和努力数量，并以所选资料统计替换原有全局统计。
  - 补齐当前资料复习统计中的“了解”数量，贯通摘要查询、共享契约和界面展示。
  - 复习筛选仅保留资料选择和掌握状态多选，移除章节、节与关键词条件及其 API 契约。
  - 兼容并迁移既有全局最后闪卡设置，补充服务/API 回归测试。
  - 在复习首页提供背默模式 Switch；开启后进入闪卡时自动启用既有重点挖空，遮罩当前及后续闪卡的全部已标注重点，关闭后正常显示。
  - 将背默模式 Switch 设计为关闭近黑、开启橙色的 iOS 风格控件，并提供尊重减少动态效果偏好的滑块过渡。
  - 将背默模式控件调整为长而细的扁平圆角矩形，保持 44px 触控高度并收紧视觉行高；轨道高度与滑块保持一致，并隔离主题通用 input 规则对其尺寸的覆盖。
  - 将背默模式开关的视觉轨道宽度收紧为 30px，并同步滑块开启位移；轨道和滑块继续保持 18px 等高。
- 本步明确不纳入：
  - 不改变资料、章、节和闪卡的管理层级及阅读页上下文展示。
  - 不改变结果集固定顺序、上一张/下一张、掌握状态、复习记录、既有重点挖空或 AI 讲解。
  - 不持久化背默模式，不修改闪卡内容、重点高亮、API 或数据库。
  - 不改变资料续读位置、资料选择后的恢复逻辑，或筛选 Sheet 的资料与掌握状态条件。
  - 不实现随机复习、间隔重复、提醒、标签、离线复习或多用户设置。
- 后续归属任务：
  - 更细的章节或关键词定位需求须以独立资料浏览功能重新评估。
  - 随机与间隔重复继续归属第一版后续学习算法任务。

### `PH6-13` 为章节题增加工作台选章刷题入口

- 状态：进行中
- 确认日期：2026-08-20
- 边界摘要：补齐课程复习工作台的章节题选章入口，复用既有刷题会话范围参数。
- 本步纳入：
  - 工作台题库聚合响应为章节题附带有效章节 ID、标题和题数摘要。
  - 工作台章节题卡片提供章节选择器，按所选章节启动背题或检测，并保留整库选项。
  - 补充共享类型、工作台服务测试和前端类型/构建验证，确保章节范围沿用既有会话 API。
- 本步明确不纳入：
  - 不改变题库详情页已有的范围选择、刷题会话创建、作答判题、统计或错题专项规则。
  - 不为真题、模拟题增加章节目录或选章入口。
  - 不修改收藏题会话、独立刷题页重构、题目编辑、导入格式和数据备份协议。
- 后续归属任务：
  - 章节进度、章节续读筛选和工作台大规模列表性能另立任务。

### `PH5-14` 在资料详情提供逐卡内容编辑与排序

- 状态：进行中
- 确认日期：2026-08-11
- 边界摘要：将资料详情中的闪卡标题列表升级为逐张卡片入口，复用既有可视化编辑器、编辑锁和同层排序接口。
- 本步纳入：
  - 资料详情按章、节展示每张闪卡的独立卡片，清晰显示当前序号、标题和就近操作。
  - 在单张卡片内打开既有结构化正文编辑器，支持标题、正文、表格、公式和受管图片的可视化编辑。
  - 在闪卡编辑态的表格中支持空白单元格直接输入、添加行、删除行以及选中相邻单元格后合并；合并单元格使用结构化 rowSpan/colSpan 保存并在阅读态正确展示。
  - 复用既有编辑锁续期、保存和释放流程，避免跨设备同时编辑同一张闪卡。
  - 在同一节内提供上移和下移操作，复用既有排序接口并在刷新后显示保存后的顺序。
- 本步明确不纳入：
  - 不修改 Markdown、JSON 或 Excel 的导入协议，不提供 Markdown 或 LaTeX 源码编辑；本步不新增导入文件中的复杂 HTML 合并单元格语义。
  - 不新增拖拽排序、跨节移动、批量编辑、复习算法、状态、挖空或 AI 讲解功能。
  - 不改动闪卡数据表、已有编辑锁、排序 API 或资料目录层级。
- 后续归属任务：
  - 跨节移动继续使用既有资料管理入口；如需在资料详情提供该入口，应单独评估交互边界。
  - 拖拽排序和批量操作在确认真实效率需求后另立任务。

### `PH6-14` 重构课程复习工作台界面

- 状态：已完成
- 确认日期：2026-08-20
- 边界摘要：仅重构课程复习工作台的前端视觉层级、响应式布局与可访问交互，不改变现有复习和刷题业务语义。
- 本步纳入：
  - 整理课程/科目选择、闪卡/题库分段、继续学习和科目分组列表的视觉层级，减少重复边框和无效留白。
  - 为资料、题库、章节范围与收藏题入口统一紧凑操作区，并在窄视口改为稳定的纵向布局。
  - 修复 iPhone 底部四项主导航的 44px 触控高度和内容安全区留白，不改变入口、顺序或导航语义。
  - 补齐工作台新增或调整控件的 aria 状态、键盘焦点与深浅主题样式，并执行类型、构建和浏览器截图回归。
- 本步明确不纳入：
  - 不修改课程/科目上下文、收藏题会话、章节范围、刷题判题、统计、题目数据或 API 契约。
  - 不新增收藏夹、随机练习、搜索、筛选、分页或任何新的复习策略。
  - 不调整闪卡阅读页、独立刷题页、题库编辑页和设置页的业务流程。
- 后续归属任务：
  - 大规模工作台虚拟化、跨端上下文冲突提示和真实 iPhone Safari 设备验收沿用既有后续任务与风险项。

### `PH5-15` 实现资料九宫格长按拖入回收站快捷删除

- 状态：已完成
- 确认日期：2026-08-11
- 边界摘要：为科目页的资料九宫格增加触摸和鼠标均可用的长按拖放删除捷径，复用已有资料软删除与回收站恢复链路。
- 本步纳入：
  - 资料格在长按后进入拖动状态，顶部显示带可访问名称的垃圾桶目标区；桌面鼠标和触摸设备均可将资料拖入目标区。
  - 拖入目标区后调用既有资料软删除接口，立即从当前九宫格移除并反馈已移入回收站。
  - 短按资料格继续进入资料详情；取消长按、松开到目标区外或请求失败时保持资料不变并恢复页面状态。
  - 补充键盘无障碍替代入口、减少动态效果规则、桌面与 390px 视口无横向溢出的交互验收。
- 本步明确不纳入：
  - 不新增或修改资料删除、回收站恢复、永久删除、资源清理或资料详情 API。
  - 不实现拖拽排序、跨科目移动、多选批量删除或课程、科目和闪卡删除手势。
  - 不改变资料导入、封面、复习、认证或数据备份行为。
- 后续归属任务：
  - 资料排序、跨科目移动和批量整理在确认真实使用需求后另立任务。
  - 回收站列表和永久删除沿用既有数据治理入口。

### `PH6-15` 支持乱序、固定数量与未做筛选练习

- 状态：进行中
- 确认日期：2026-08-20
- 边界摘要：把乱序、固定数量和未做筛选作为创建会话时的显式设置，后端固化筛选结果、题目快照与顺序，前端复用现有刷题入口提供紧凑控件。
- 本步纳入：
  - 扩展题库整库/章节、科目收藏题及错题专项会话创建请求，支持数量上限和乱序开关；默认保持现有全部题目、题库排序行为。
  - 服务端在事务内完成候选题过滤、数量截取和可复现会话顺序写入 practice_attempts，get/续答只读取既有快照。
  - 在题库详情和课程复习工作台为背题、检测入口提供题目数量与乱序控件，移动端保持原生控件、44px 触控高度和无横向溢出。
  - 将题数下拉框与乱序小尺寸勾选框合并为同一设置行；整行保持足够点击区域，并在桌面端与移动端和其他设置控件对齐。
  - 在题库整库/章节和科目收藏题的普通练习入口提供“只练未做”与“不区分”双选；未做定义为该题尚不存在任何已提交的作答记录，筛选条件和最终题目集合写入会话范围快照。
  - 补充共享类型、API/服务测试，确认收藏题和章节范围仍沿用既有统计隔离与范围语义。
- 本步明确不纳入：
  - 不实现间隔重复、基于掌握度的智能随机、权重抽样、跨科目混合或进行中会话重新抽题。
  - 不修改题目导入、题目排序维护、背题/检测判题、统计口径、错题定义、AI 讲解和备注显示规则。
  - 不将未做筛选加入本次错题或累计错题入口，也不新增按正确率、错误次数或完成状态的筛选。
  - 不新增随机练习历史报表、收藏夹分组、离线缓存或新的权限模型。
- 后续归属任务：
  - 若需按掌握度、错误次数或时间间隔动态编排题目，另立间隔重复/智能出题任务。
  - 若需在进行中会话中追加、移除或重新洗牌题目，另立会话编辑任务。

### `PH5-16` 支持 JSON 内容级高亮导入

- 状态：进行中
- 确认日期：2026-08-12
- 边界摘要：扩展内容级 JSON 单资料导入的高亮协议，并将外部文本或公式标记自动解析为现有结构化高亮锚点。
- 本步纳入：
  - 为 JSON 卡片增加可选 highlights：文本使用 text，公式使用 formula，occurrence 用于指定重复项。
  - 在预览阶段校验高亮声明，并在应用时按最终正文生成文本字符范围或公式节点路径后事务写入 highlights。
  - 更新 JSON 导入模板，补充文本与公式高亮示例。
  - 为有效匹配、重复歧义、未匹配、公式匹配、正文修正和数据库写入补充自动化测试。
- 本步明确不纳入：
  - 不改变完整 JSON 备份的格式、恢复流程或内部 cardId/nodePath 锚点。
  - 不扩展 Markdown、ZIP 或 Excel 导入协议，不支持跨正文节点的文本匹配。
  - 不导入课程科目归属、封面、学习状态、AI 讲解、资源内容、密钥或其他设置。
  - 不改变既有阅读高亮、挖空、可视化编辑或高亮编辑迁移规则。
- 后续归属任务：
  - 跨节点富文本匹配、批量标注规则和从外部学习状态迁移，待确认实际数据来源后另立任务。
  - 完整 JSON 数据备份恢复继续归属现有数据治理流程。

### `PH6-16` 在学习工作区提供悬浮问答助手

- 状态：进行中
- 确认日期：2026-08-21
- 边界摘要：在既有 AI Provider 能力上，为刷题和闪卡阅读页面增加一个默认收起、可关闭的悬浮问答入口；每次请求仅携带当前学习内容和本轮提问，回答通过受控流式通道逐段显示且只保存在当前前端会话，生成完成后可一键复制回答文本。
- 本步纳入：
  - 新增无障碍可访问的悬浮助手入口、收起状态与紧凑问答面板。
  - 优化悬浮助手入口与问答面板的视觉层级、按压反馈和展开动效，弱化厚重边框与阴影；桌面和移动端保持既有位置、触控尺寸、焦点与不遮挡边界。
  - 将悬浮入口替换为项目内的小猫贴图，支持鼠标和触摸拖动调整位置并在当前设备保存；保留方向键微调与一键还原位置，避免拖动成为唯一操作方式。
  - 为生成完成的回答提供不影响阅读的一键复制按钮及成功反馈。
  - 为当前闪卡与当前刷题题目建立受控的即时流式问答 API，并复用已启用的 AI Provider。
  - 以 Server-Sent Events 转发 OpenAI 兼容 Provider 的输出分段，支持用户停止当前请求且不保存半成品。
  - 拒绝 Provider 的非流式完整 JSON 回退，按可见增量节奏呈现每个 SSE 分段；超时按首字等待和流中断等待处理，不以总时长截断持续输出。
  - 检测模式未作答题在服务端移除答案、解析和知识点，并限制模型不得给出作答结论。
  - 覆盖 API 输入边界和桌面/移动端阅读不遮挡的浏览器验收。
- 本步明确不纳入：
  - 不保存聊天记录，不新增全局聊天页或主导航入口。
  - 不改动现有 AI 讲解生成、版本历史、题目判定、作答统计或会话快照。
  - 不上传、记录或导出 API Key、Provider 密钥或完整对话内容。
- 后续归属任务：
  - 跨题目或跨资料的长期对话记忆另立后续 AI 对话任务。
  - 多角色切换、用户上传贴图和可配置贴图皮肤另立后续视觉资产任务。

### `PH5-17` 明确 JSON 高亮重复匹配判定标准

- 状态：进行中
- 确认日期：2026-08-12
- 边界摘要：补充 JSON 导入模板中高亮重复匹配的可操作判定标准，避免用户将被更长文本包含的目标或表格文本误认为唯一匹配。
- 本步纳入：
  - 在 JSON 模板的 __高亮字段规则 中明确：匹配按当前卡片 body 的连续文本子串计数，目标被更长文本包含也算一次。
  - 说明普通正文、列表和表格单元格中的文本都参与当前卡片的计数，不同卡片互不影响。
  - 在模板示例中加入包含关系导致重复的 occurrence 写法，并补充模板内容断言与解析回归。
- 本步明确不纳入：
  - 不修改高亮解析、occurrence 选择、错误提示或自动补全行为。
  - 不修改用户下载目录中的 JSON 文件，不扩展跨结构化节点文本匹配。
  - 不变更 Markdown、ZIP、Excel 或完整备份恢复协议。
- 后续归属任务：
  - 导入错误中展示每次命中的上下文、位置或提供自动补全 occurrence，需另立任务评估。

### `PH6-17` 实现 AI Provider 优先级与失败自动降级

- 状态：进行中
- 确认日期：2026-08-21
- 边界摘要：将既有唯一启用 Provider 改为有序候选池，并为 AI 讲解、题目讲解、导入格式修正和学习助手提供失败自动降级。
- 本步纳入：
  - 增加 Provider 启用状态与稳定优先级的数据迁移、管理接口和设置页排序控件。
  - 在首段有效结果前，对网络错误、超时、非成功状态和无效内容按优先级切换到下一启用 Provider。
  - 复用现有 API Key 加密存储、单配置测试与请求取消语义，不向浏览器返回密钥或渠道内部错误。
  - 学习助手流式回答在首段内容输出后不再降级，避免混拼不同渠道的回答。
  - 补充服务、HTTP、迁移和前端类型构建验证，并同步进度真相源。
- 本步明确不纳入：
  - 不新增请求重试次数、熔断器、后台健康探测、用量计费或渠道可用性历史。
  - 不修改闪卡、题目、导入修正或学习助手的提示词、业务上下文和持久化语义。
  - 不保存聊天记录，不导出 Provider 配置、API Key 或 Provider 原始错误响应。
- 后续归属任务：
  - 按 Provider 维度的熔断、冷却、可用率统计和告警另立可靠性任务。
  - 跨模型的质量评分和按任务类型路由另立 AI 策略任务。

### `PH5-18` 将 JSON 高亮去重要求改为模型可执行指令

- 状态：进行中
- 确认日期：2026-08-12
- 边界摘要：把 JSON 高亮重复规则从说明性文本升级为模型可遵循的强制生成与校验指令，减少外部模型生成无法导入的短词重复标注。
- 本步纳入：
  - 在 JSON 模板增加面向模型的逐卡生成步骤和输出前自检要求。
  - 要求优先使用唯一且语义完整的原文短语；重复时才填写实际 occurrence。
  - 明确禁止按 highlights 数组下标、猜测或默认第一个匹配确定 occurrence，并增加正反示例与模板断言。
- 本步明确不纳入：
  - 不改变 JSON 解析、重复判定、occurrence 选择或导入错误行为。
  - 不修改用户的 JSON 文件，不添加自动修复、自动扩词或模型调用能力。
  - 不修改 Markdown、ZIP、Excel 或备份恢复协议。
- 后续归属任务：
  - 在导入预览中提供逐条匹配次数、上下文定位或自动修复，需另立交互与解析任务。

### `PH6-18` 收敛复习工作台的层级与稳定交互

- 状态：进行中
- 确认日期：2026-08-21
- 边界摘要：只优化复习工作台的视觉层级、信息密度、稳定尺寸和交互反馈，保留既有四入口导航与学习流程。
- 本步纳入：
  - 整理复习页顶部工具、课程/科目选择、闪卡/题库模式、继续学习和内容分组的层级与间距。
  - 统一工作台资料、题库、章节范围和收藏题操作区的稳定按钮尺寸、对齐和展开反馈，避免状态切换造成布局跳动。
  - 补齐主导航与工作台图标按钮的可访问名称、Tooltip、选中/展开状态和键盘焦点；保持 iPhone 44px 触控目标与底部安全区。
  - 为加载、按压、展开和减少动态效果状态补充不改变布局边界的过渡，并完成 typecheck、build、progress 校验和可用环境下的桌面/390px 视觉回归。
- 本步明确不纳入：
  - 不修改课程/科目上下文、闪卡与题库会话、刷题判题、统计口径、题目数据或 API 契约。
  - 不新增主导航入口、搜索/筛选入口、复习策略、数据模型、服务端接口或新的主题皮肤。
  - 不调整闪卡阅读页、独立刷题页、题库编辑页、导入页和设置页的业务流程。
- 后续归属任务：
  - 跨页面设计系统 token 统一、真实 iPhone Safari 验收和大规模列表虚拟化另立任务或沿用既有风险项。

### `PH5-19` 固化 JSON 高亮重复判定算法与反例

- 状态：进行中
- 确认日期：2026-08-12
- 边界摘要：把 JSON 高亮重复判定从说明性文字固化为与解析器一致的可执行算法和反例，消除模型对短词、包含命中、重叠命中及跨节点文本的歧义。
- 本步纳入：
  - 在 JSON 模板增加结构化 __高亮重复判定标准，定义 text/formula 的命中单位与 occurrence 编号算法。
  - 明确文本先 trim、仅在单个解析后文本节点内连续子串匹配，包含命中和重叠命中均计数，节点之间只合并计数顺序不跨节点匹配。
  - 加入可直接照抄的短数字、包含关系、重叠、跨节点、公式和重复声明正反例，并补充模板断言。
- 本步明确不纳入：
  - 不修改当前解析器的匹配、排序、去重或错误处理算法。
  - 不修改用户 JSON 文件，不新增自动扩词、自动修复或模型调用。
  - 不改变 Markdown、ZIP、Excel 或完整备份恢复协议。
- 后续归属任务：
  - 导入预览直接展示每个命中的节点路径、字符位置和上下文，需另立诊断反馈任务。

### `PH5-20` 支持 JSON body 内联高亮标记导入

- 状态：进行中
- 确认日期：2026-08-12
- 边界摘要：以 body 内联高亮标记替代模型对重复文本 occurrence 的推算，文本位置由标记本身直接表达。
- 本步纳入：
  - JSON body 支持 [[hl:原文]] 文本高亮标记，同一文本可在每个目标位置分别标记。
  - 解析时移除标记、生成现有文本锚点，并在预览、正文入库和阅读内容中保留无标记正文。
  - 预览正文未修改时保留内联标记的精确锚点；正文被修改后仅允许能唯一重新定位的标记继续导入，否则阻止写入。
  - 保留 highlights 数组对 formula 及旧 text/occurrence 声明的兼容，模板改以内联文本标记为主并补充回归测试。
- 本步明确不纳入：
  - 不修改 Markdown、ZIP、Excel 或完整备份恢复协议。
  - 不支持跨 Markdown 节点、代码块、图片替代文本或公式内部的 [[hl:...]] 文本标记。
  - 不实现自动扩词、自动修复 occurrence、模型调用或导入预览定位 UI。
  - 不修改用户已有 JSON 文件。
- 后续归属任务：
  - 公式的 body 内联标记、预览中逐次命中定位和批量迁移旧 occurrence 文件，需要另立协议或交互任务。

### `PH5-21` 兼容表格单元格内联高亮标记

- 状态：进行中
- 确认日期：2026-08-12
- 边界摘要：修复内联标记的内部解析令牌触发表格分隔行预校验，恢复表格单元格内普通文本高亮导入。
- 本步纳入：
  - 调整 JSON 内联标记与 GFM 表格语法校验的执行顺序或输入，使单元格内普通文本标记不被误报为错误分隔行。
  - 补充表格单元格内重复文本标记、正文标记剥离和锚点路径回归。
  - 保持跨单元格、粗体等 Markdown 结构、代码、公式和未转义管道符的内联标记拒绝行为。
- 本步明确不纳入：
  - 不扩展跨单元格、跨节点、公式或代码中的内联高亮协议。
  - 不修改 Markdown、ZIP、Excel 或旧 highlights/occurrence 导入协议。
  - 不新增导入预览定位 UI 或自动修复。
- 后续归属任务：
  - 公式内联标记、跨节点高亮和预览逐次命中定位继续按后续独立任务评估。

### `PH5-22` 优化复习闪卡切换性能

- 状态：进行中
- 确认日期：2026-08-12
- 边界摘要：消除普通切卡时重复加载筛选集及单卡重复读取造成的等待。
- 本步纳入：
  - 在单张闪卡响应中提供仅含前后卡片 ID、当前序号和总数的导航信息。
  - 普通上一张、下一张只请求目标闪卡，不请求当前筛选集的任何列表。
  - 移除读取单张闪卡后为返回响应再次查询同一张卡的重复工作，并补充服务及真实 MySQL 回归。
- 本步明确不纳入：
  - 不改变复习筛选条件、固定排序、按资料续读、状态保存、复习记录或编辑锁语义。
  - 不实现预加载、离线缓存、随机复习、间隔重复或新的切换动画。
  - 不改动资料、导入、AI、认证、Tunnel 或备份功能。
- 后续归属任务：
  - 若实际内容渲染仍有性能瓶颈，再以独立任务进行长正文、复杂表格或公式的前端渲染性能分析。

### `PH5-23` 降低深色模式闪卡高亮刺眼度

- 状态：已完成
- 确认日期：2026-08-13
- 边界摘要：只调整深色模式阅读态高亮的视觉颜色，降低白色和亮橙色对正文阅读的干扰。
- 本步纳入：
  - 为简约深色模式的文本、表格和公式高亮覆盖低饱和深色背景与正文前景色。
  - 为秋日枫叶深色模式提供独立的低饱和暖色高亮背景，保持与主题环境协调。
  - 同步 DESIGN.md 深色高亮 token，确保规范与实际样式一致。
- 本步明确不纳入：
  - 不修改浅色模式高亮颜色。
  - 不修改挖空遮罩、揭示动画、高亮保存协议或高亮交互行为。
  - 不新增颜色选择、用户自定义主题或其他页面的状态色。
- 后续归属任务：
  - 若真实设备仍存在特定屏幕的对比度或色觉适配问题，另立无障碍视觉校准任务。

### `PH5-24` 导入正文格式 AI 修正

- 状态：已完成
- 确认日期：2026-08-13
- 边界摘要：在导入预览的需要处理列表中逐条新增“优化格式”，限定为可重新解析的正文格式问题，并复用设置页当前启用的 Provider；本次调整移除服务端正文保真拦截，仅保留提示词约束和格式问题重新解析。
- 本步纳入：
  - 仅对 unsafe_html、invalid_formula、invalid_table 三类正文 Markdown 格式问题提供 AI 修正入口。
  - 每次请求只发送当前一条问题和其所在闪卡的一份 Markdown 正文；不同闪卡可同时请求，服务端按修订版本合并每条结果；同一闪卡的旧结果不得覆盖已完成修正，不能把全部错误合并为一次请求。
  - Provider 的用户消息只包含目标闪卡的 body 原文，不携带标题、卡片索引、问题对象或整份 JSON；系统消息只约束格式修正边界。
  - 后端使用当前启用 Provider，密钥只在后端解密和发送；AI 返回结果必须经既有 Markdown 解析和点击的问题类型减少校验后才更新预览，正文与既有高亮只通过 Provider 提示词要求保留，不在服务端做内容或高亮匹配拦截。
  - 在每条可修正问题旁显示“优化格式”、独立生成状态、失败反馈和逐次更新的可应用预览。
- 本步明确不纳入：
  - 不修复 JSON 语法、JSON 读取错误或 JSON Schema 错误，不向该类预览显示 AI 修正。
  - 不修复资料、章、节、闪卡标题和层级结构，不修复图片路径、缺失资源、ZIP 或 Excel 文件结构。
  - 不修改原始上传文件，不自动应用导入，不保存 AI 修正历史或新增 Provider 配置。
- 后续归属任务：
  - 如需让 AI 修复 JSON 协议、标题层级、资源引用或提供逐条差异确认，另立输入协议与交互任务评估。

### `PH5-25` HTML 表格导入与格式优化诊断

- 状态：进行中
- 确认日期：2026-08-13
- 边界摘要：兼容安全 HTML 换行与表格导入，复用现有结构化内容渲染并保持不安全 HTML 拒绝。
- 本步纳入：
  - 将结构安全且可解析的 HTML table 转为内部 GFM 表格节点，不放行脚本、样式或其他原始 HTML。
  - 将 HTML br、p、div 和 table/thead/tbody/tfoot/tr/th/td 转为现有 break、paragraph、table/tableRow/tableCell 节点，并保留表头和对齐信息。
  - 补充 HTML 换行、表格、事件属性拒绝以及现有 JSON body 复用解析路径的 API 回归测试。
- 本步明确不纳入：
  - 不修复 JSON 读取、JSON 语法或 JSON Schema 错误。
  - 不放行脚本、样式、事件属性、链接和其他未列入白名单的原始 HTML，不支持跨行/跨列合并。
  - 不改变 AI 格式修正、Provider 配置、高亮协议和原始上传文件。
- 后续归属任务：
  - 复杂 HTML 跨行/跨列合并、样式语义和更多非表格 HTML 标签的转换另立任务。

### `PH5-26` 建立课程复习工作台上下文与聚合入口

- 状态：已完成
- 确认日期：2026-08-19
- 边界摘要：将复习首页从全局资料选择收敛为课程级学习工作台，保留资料页的管理职责和现有练习实现。
- 本步纳入：
  - 新增数据库持久化的课程复习上下文：当前课程、每门课程最近具体科目、每门课程最近闪卡/题库分段和最近展开资料。
  - 新增课程级复习工作台聚合契约与认证 API，提供课程/科目摘要、闪卡资料摘要、题库摘要、课程统计和最近可继续活动。
  - 前端复习页提供课程选择器、课程搜索、科目选择器、闪卡/题库分段和课程级继续入口；选择后立即保存并跨端恢复。
  - 闪卡资料按科目分组，单个资料展开后直接显示阅读/背默、状态数量和开始入口；题库按科目扁平展示并标记章节题/真题/模拟题。
  - 具体闪卡和题库会话使用可恢复 URL，继续入口遵循当前课程和科目范围；空资料和空题库保留但禁用练习。
  - 将复习上下文纳入完整 JSON 导出/恢复，失效引用自动回退；本机外观设置不纳入。
- 本步明确不纳入：
  - 不引入间隔重复、随机复习、每日任务、提醒、打卡或其他自动复习计划。
  - 不重写现有闪卡阅读、状态、高亮、编辑锁、题目编辑、刷题判题和统计业务。
  - 不把课程、科目和题库管理操作放入复习选择器；管理仍归资料页。
  - 不在课程工作台展开单张闪卡正文、题目正文或新增题目管理能力。
  - 不新增多用户权限、离线同步、实时冲突提示或本机外观跨设备同步。
- 后续归属任务：
  - 课程工作台的分页/虚拟化和大规模资料性能优化另立任务；本步聚合仅返回轻量摘要。
  - 真实 iPhone Safari 练习态和跨设备并发人工验收沿用既有发布验收任务。

### `PH5-27` 新增库洛米精致主题

- 状态：已完成
- 确认日期：2026-08-20
- 边界摘要：在既有外观选择与 iOS 风格 token 体系中新增库洛米视觉层，允许调整容器和环境装饰，不改变业务信息架构。
- 本步纳入：
  - 新增库洛米外观选项、本机 localStorage 持久化和根元素主题状态。
  - 以黑紫、薰衣草紫、库洛米粉和少量暖黄构成浅色/深色 token，覆盖页面背景、导航、容器、表单、Sheet、按钮、状态辅助和焦点。
  - 增加低面积、静态、可关闭动态影响的环境装饰与库洛米主题 swatch；保持正文和公式阅读表面纯净。
  - 验证 Web 类型检查、构建、进度生成/校验，并检查 390px 与桌面视口无横向溢出。
- 本步明确不纳入：
  - 不新增库洛米图片、品牌 Logo、角色动画、音效或联网资源。
  - 不改变复习、资料、题库、登录、AI、备份恢复和 API 数据语义。
  - 不将本机主题设置同步到服务端、账号或 JSON 备份。
- 后续归属任务：
  - 真实 iPhone Safari 设备验收沿用 GA 发布任务；若发现设备差异，仅在本主题视觉边界内修正。
  - 更多角色主题、主题市场和自定义配色另立主题能力任务。

## 11. 风险与范围变化

| ID | 日期 | 类型 | 描述 | 影响范围 | 当前处理 |
|---|---|---|---|---|---|
| `RISK-001` | 2026-08-10 | 安全风险 | Cloudflare Tunnel 会把本机应用暴露到公网，必须保证账号门禁先于连接器开放。 | PH5-02、PH5-04、GA 发布 | PH5-12 已恢复 PH5-04 单账号门禁和项目 Tunnel；未登录业务接口返回 401，review.panspan.cloud HTTPS 返回 200。任何本机免登录方案均不得绕过该公网门禁，恢复或调整后需重新执行 PH5-05 HTTPS 与账号安全回归。 |
| `RISK-002` | 2026-08-10 | 运行依赖 | 不使用 Docker 且选择现有 MySQL 8.x，目标电脑的数据库服务、专用账号和备份工具必须持续可用。 | PH1-02、PH1-03、PH4-04、Windows 发布 | 启动脚本只检查连接，不启停 MySQL；PH1-02 提供 db:check 和 storage:check，真实迁移需使用最小权限专用账号和本机凭证完成验收；后续设置页继续提供可诊断的连接和路径测试。 |
| `RISK-003` | 2026-08-10 | 数据一致性 | Markdown 只导入一次，后续数据库成为唯一真相源；若导出、备份或恢复不完整，用户无法从源文件重建学习状态。 | 资料导入、可视化编辑、PH4-03、PH4-04 | 正文使用结构化模型保存；PH4-03 已提供不改写源文件的 Markdown 内容导出和包含学习状态、资源内容且排除秘密的 JSON 恢复；PH4-04 已补齐每日数据库/资源备份、成功备份保留 7 份和恢复前留档，PH4-05 继续做完整演练。 |
| `RISK-004` | 2026-08-10 | 交互复杂度 | 文本、表格和公式高亮在可视化编辑后可能发生定位漂移，导致遮罩范围错误。 | PH3-01、PH3-02、PH3-03 | PH3-01 已按结构化内容节点路径和节点内范围保存文本高亮，公式按完整节点保存，不使用整篇纯字符偏移；PH3-03 已在事务中保留内容完全未变节点的高亮，并删除正文、公式或损坏锚点导致的无法精确定位项，且已通过服务、HTTP 与真实 MySQL 回归。PH3-06 继续覆盖标注与编辑组合回归。 |
| `RISK-005` | 2026-08-10 | 兼容性风险 | iPhone Safari 的长按选词、浮动工具栏、PWA 安全区域和公式滚动行为与桌面浏览器不同。 | PH3-01、PH5-01、GA 发布 | 已补齐 viewport-fit、iPhone PWA 元数据、主屏幕图标、静态资源 Service Worker 和带 safe-area 的底部 Tab Bar；移动端不使用左右滑动切卡，表格/公式/选词已在 390x844 视口验证无横向溢出。真实 iPhone Safari 的长按选词、浮动工具栏和 Home Indicator 仍需 PH5-01 设备验收。 |
| `RISK-006` | 2026-08-10 | 外部服务 | AI Provider 的接口格式、模型可用性、速率限制和费用可能变化。 | PH4-01、PH4-02、PH6-17、PH6-16、题目 AI、导入格式修正 | PH4-01 已在后端完成多套配置、模型选择和 API Key AES-256-GCM 加密存储，配置读取永不返回密钥；PH4-02 已完成 OpenAI 兼容请求适配、连接测试、超时与上游错误隔离，失败不会写入或改变正文、标注和复习状态。PH6-17 已增加有序 Provider 候选池，闪卡/题目讲解、导入格式修正和悬浮问答在首段有效内容前遇到网络、超时、状态或内容格式失败会自动降级到下一启用渠道；流式首段输出后不切换、不混拼。真实模型兼容性、可用模型、速率限制、费用和多渠道优先级策略仍需在认证态与发布前持续验收，Provider 熔断、冷却和可用率统计另立任务。 |
| `RISK-007` | 2026-08-10 | 数据库兼容性 | utf8mb4 下过长的唯一索引会超过 MySQL 3072 字节上限，导致初始迁移中途失败并留下部分表。 | PH1-02、后续数据库迁移 | 已将 resources.relative_path 的唯一索引字段收敛为 VARCHAR(512)，清理本次半成品数据库后重新迁移成功；后续新增索引必须按 utf8mb4 字节上限做迁移测试。 |
| `RISK-008` | 2026-08-10 | 范围与安全风险 | 本机启停脚本已经集成 cloudflared tunnel，连接器开放顺序必须继续受账号门禁约束。 | PH1-03、PH5-02、PH5-04 | PH5-12 已恢复本机 PUBLIC_ACCESS_READY、CLOUDFLARED_ENABLED 和 PH5-04 单账号门禁，项目 Tunnel 正在受管运行。后续调整必须保持门禁先于连接器的顺序，并按 PH5-05 完成 HTTPS 与账号安全回归。 |
| `RISK-009` | 2026-08-10 | 导入可靠性 | 导入预览令牌和待应用资源目前只保存在 API 进程内存中，API 重启或超过 30 分钟后预览会失效。 | PH1-05、PH1-06、Windows 运行 | 预览令牌限制 30 分钟和 20 份，应用前重新校验源文件哈希；PH1-06 已用真实 HTTP 验证取消和 API 重启后的失效提示。第一版继续接受进程内令牌边界，若需要跨重启恢复再改为受管暂存目录。 |
| `RISK-010` | 2026-08-10 | 内容保真风险 | 导入预览的正文修正使用纯文本表单；用户修改含公式、表格或图片的正文时，当前实现会将该卡片降级为纯文本段落。 | PH1-05、PH2/PH3 内容渲染与编辑 | 导入预览中的即时修正仍按纯文本应用；资料应用后可在 PH3-03 的结构化可视编辑器中无损修改正文、表格、公式和受管图片，不再依赖 Markdown 源码。 |
| `RISK-011` | 2026-08-11 | 同步一致性 | 编辑锁采用 90 秒租约；设备离线、浏览器崩溃或未执行释放时，另一设备需要等待租约过期才能编辑。同一浏览器资料中的多个标签页共享设备标识，视为同一设备。 | PH3-05、M3 | 浏览器每 30 秒续期，并在保存、取消、返回、切卡和卸载时主动释放；服务端用 MySQL 当前时间校验并清理过期锁，正文保存必须同时匹配设备标识和随机令牌。 |
| `RISK-012` | 2026-08-11 | 数据一致性风险 | 恢复流程先提交数据库事务，再写入资源文件；若恢复后的资源目录发生磁盘写入失败，数据库和资源目录可能短暂不一致。 | PH4-04、PH4-05、备份恢复 | 恢复前强制生成当前数据留档，恢复文件先校验资源哈希和路径，写入失败不删除旧资源；PH4-05 已通过真实 MySQL、资源目录和 JSON/备份恢复正常路径验收，磁盘写入异常下的事务补偿仍作为后续治理项。 |
| `RISK-013` | 2026-08-11 | 外部配置依赖 | PH5-02 的真实 HTTPS 验收依赖 Cloudflare Tunnel 连接器和 review.panspan.cloud 边缘连接状态；DNS 与凭证已配置，但公网连接器暂不能在 PH5-04 前启动。 | PH5-02、PH5-05、GA 发布 | 已创建 review-panspan-cloud Tunnel 并将 review.panspan.cloud DNS 路由到新 Tunnel；凭证只保存在用户目录，项目 config.yml 和 .env.local 被忽略。PH5-04 已完成且 PUBLIC_ACCESS_READY=true；PH5-05 已修复一键启动补拉逻辑并验证 review.panspan.cloud 返回 HTTP 200，后续继续执行其余 GA 回归。 |
| `RISK-014` | 2026-08-11 | 体验范围变更 | 全局秋日枫叶皮肤与 AI 真实取消同时触及所有前端页面、可访问性状态和 Provider 请求生命周期；若主题侵入正文或取消只停在浏览器端，会造成阅读干扰或后台继续消耗请求。 | PH5-06、复习页、设置页、AI 讲解、文本与公式挖空 | PH5-06 已完成：叶片装饰仅存在于环境层，正文维持纯净高对比；停止操作已将取消信号传递至 Provider，服务和 HTTP 测试确认取消后不写入半成品，桌面与 390x844 浏览器验收确认深浅主题、减少动态效果规则和无横向溢出。 |
| `RISK-015` | 2026-08-11 | 范围与数据一致性风险 | 课程/科目目录、资料封面和掌握状态历史会扩展资料主数据、资源生命周期、导入归属和 JSON 恢复边界；若迁移、软删除、缩略图或旧备份兼容不完整，可能造成资料丢失、分类错位或趋势失真。 | PH5-07、资料导航、导入、封面资源、状态统计、JSON/备份恢复 | PH5-07 已完成数据库迁移、目录事务 API、逐层 URL 导航、封面管理和状态趋势。完整 JSON、手动备份和每日备份均会保存课程、科目、排序、资料归属、封面关联及状态历史；恢复在同一事务中按依赖顺序写入，旧 JSON 自动生成不可删除的“待整理 / 待整理”默认目录。恢复继续复用资源路径、哈希和敏感字段校验，单测已验证封面原图/缩略图实际写回，真实 MySQL 备份回归已确认新增元数据和资源归档。残余风险仅是恢复提交后磁盘写入失败的既有补偿边界，归属 RISK-012。 |
| `RISK-016` | 2026-08-11 | 测试隔离风险 | M4 数据治理集成测试的备份断言会读取开发库当前所有受管资源；开发库已有资料时，夹具资源会混入 manifest，导致与固定预期不一致。 | PH4-05 回归、PH5-07 数据治理兼容验证 | PH5-07 已使用不依赖全库资源清单的真实复习集成回归确认旧资料写入兼容。本次不改动既有 M4 测试；后续数据治理回归应为夹具使用独立资源目录或将备份范围限定为夹具数据。 |
| `RISK-017` | 2026-08-11 | 体验范围变更 | 手动明暗模式会覆盖浏览器系统偏好；若 CSS 覆盖优先级不完整，皮肤、表单和阅读正文可能出现混合明暗色或可读性下降。 | PH5-08、设置页、全局页面与闪卡阅读 | PH5-08 以根元素 data-color-mode 统一控制深浅 token，并将跟随系统保留为默认项；2026-08-19 已使用终端 Playwright 覆盖简约与秋日枫叶两套皮肤、跟随系统/浅色/深色三档切换、刷新持久化及 390x844/1440x900 无横向溢出。后续新皮肤或跨设备偏好需单独回归深浅 token。 |
| `RISK-018` | 2026-08-11 | 工程回归阻塞 | CatalogPanel 的 route 参数排除 manage 分支，但父组件直接传入完整 MaterialsRoute，导致 App.tsx:3217 的 TypeScript 检查失败并阻塞全量前端验收。 | PH5-07、PH5-08、前端类型检查与发布构建 | PH5-07 已通过将 manage 分支从 CatalogPanel 入参显式缩窄来收敛路由类型；npm run typecheck、npm run build 与 npm test 均已通过。后续显示模式任务可基于该通过状态独立执行浏览器回归。 |
| `RISK-019` | 2026-08-11 | 内容安全与可读性风险 | AI Provider 返回 Markdown 文本；若复习页直接显示字符串，会暴露 Markdown 标记并破坏讲解可读性，若直接注入 HTML 则可能扩大不可信响应的攻击面。 | PH5-09、AI 讲解、复习页 | PH5-09 使用 remark 的 Markdown/GFM/数学解析结果复用现有只读节点渲染，安全链接规则保持不变，原始 HTML 和未受管图片均不渲染；另将模型常见的 `**“强调文本”**` 与 `*“强调文本”*` 从未解析文本节点补为强调节点，待浏览器自动化环境恢复后补齐页面元素回归。 |
| `RISK-020` | 2026-08-11 | 导入协议与安全风险 | JSON 与 Excel 内容导入扩展了不受信任输入边界；JSON 高亮还需把外部文本或公式匹配为结构化正文锚点。若将完整备份、资源路径或学习状态误当成普通资料导入，或接受歧义匹配，可能覆盖既有数据、产生错误挖空或扩大秘密泄露面。 | PH5-10、PH5-16、PH5-20、PH5-21、资料导入、数据治理 | PH5-10 固定为单资料内容协议：JSON 必须匹配 format/version 与层级字段，Excel 只读取固定工作表和五列；正文复用 Markdown 安全解析与既有预览、重复哈希和应用事务。PH5-20 将文本主协议改为 body 内 [[hl:原文]]：解析后移除标记并按所在位置生成锚点，避免重复文本 occurrence 推算；PH5-21 已修复内部令牌触发表格预校验的问题，单个 GFM 表格单元格内的普通文本标记可正常导入，跨单元格或 Markdown 节点仍阻止。正文修改后只有唯一重新定位才能应用。highlights 数组继续兼容 formula 及旧 text/occurrence 文件，完整 JSON 恢复仍只能经数据治理确认流程执行，资源、学习状态、课程科目归属与密钥不进入普通资料导入。 |
| `RISK-021` | 2026-08-11 | 验收环境阻塞 | Codex 内置浏览器连接初始化时出现 Cannot redefine property: process，无法执行资料详情页、AI 讲解页和 JSON/Excel 导入页的桌面/iPhone 浏览器交互与无横向溢出回归。 | PH5-09、PH5-10、PH5-14、PH5-24、PH5-25、前端浏览器验收 | PH5-07 已通过独立 Playwright 完成资料格与详情页的桌面/390px 回归，状态摘要、无横向溢出和 URL 返回均已验证。PH5-10 已以类型检查、生产构建和 70 项 API 测试覆盖 JSON/Excel 解析、模板下载、普通导入安全边界及既有 Markdown 回归。PH5-13 已使用独立 Playwright 的本地认证与 Dashboard 响应桩完成当前资料选择、统计切换和 390px 无横向溢出回归，不读取本机账户凭证或写入业务数据。PH5-24 的逐条优化格式按钮和 PH5-25 的 HTML 换行/表格解析均已通过类型、构建和 API 回归覆盖，但本轮内置浏览器仍以 Cannot redefine property: process 失败，未能补做实际页面验收。2026-08-19 通过 playwright-cli 在不读取本机账号凭据的旧本地服务上完成 390x844 复习页回归，确认无横向溢出、底部安全区布局和正文选词后的高亮按钮可用；仍不能替代真实登录态、iPhone Safari 原生选词浮动工具栏、主屏幕安装和 Home Indicator 验收。待运行时恢复或取得真实设备后，优先补测 AI 讲解、导入页、HTML 换行/表格阅读、资料详情逐卡编辑以及 PH5-01 的真实设备项。 |
| `RISK-022` | 2026-08-11 | 运行与安全风险 | 为缩短本机状态查看而临时屏蔽账号密码登录；若同时启动公网 Tunnel，会绕过既定的单账号安全门禁。 | PH5-11、PH5-04、PH5-05、GA 发布 | PH5-11 已因对外提供要求暂缓，PH5-12 已恢复认证配置和项目 Tunnel，并验证未登录业务接口返回 401、公网首页返回 200。本机免登录例外当前未启用，后续必须先确认隔离模型。 |
| `RISK-024` | 2026-08-11 | 工程配置阻塞 | PH5-15 引用了 progress/config.json 模块清单中不存在的 catalog-navigation，导致进度生成和校验均无法执行。 | PH5-15、进度展示层、后续任务进度同步 | PH5-15 已改用既有的 review-experience 与 data-governance 模块标识，进度生成和校验恢复可执行；未新增目录导航模块。 |
| `RISK-023` | 2026-08-11 | 工程回归阻塞 | 共享 Review API 已收敛为按资料续读与资料/掌握状态筛选，但复习页面仍读取章节、节、关键词和全局 continueCard 字段，导致全量前端 TypeScript 检查失败；服务端续读迁移 SQL 的 ON DUPLICATE KEY UPDATE 还使用未限定的 setting_value，复习面板请求返回 ER_NON_UNIQ_ERROR。 | PH5-13、PH5-14、前端构建与发布验收 | PH5-13 已收敛 ReviewPanel、筛选 Sheet 和 App 状态到新共享 API，并限定续读迁移 SQL 的 setting_value 列来源；npm run typecheck、npm run build、npm test（76 项）和真实 MySQL 复习集成均已通过。PH5-14 的逐卡编辑浏览器回归仅余既有 RISK-021 验收环境限制。 |
| `RISK-025` | 2026-08-13 | 内容兼容性风险 | 编辑器新增 rowSpan/colSpan 后，合并单元格不能无损映射回 GFM Markdown；若直接扩展导入协议或导出语义，可能造成用户以为导入导出可保留合并结构。 | PH5-14、资料编辑、Markdown 导出、JSON 备份恢复 | PH5-14 已在闪卡编辑保存的结构化正文中支持合并单元格，并同步阅读态、备份恢复白名单和服务端校验；不修改 Markdown、JSON、Excel 或 HTML 导入协议。Markdown 导出是否以展开表格、保留 HTML 或提示降级，须单独评估后实施。 |
| `RISK-026` | 2026-08-19 | 数据一致性与范围风险 | 题库引入可编辑题目、AI 多版本、进行中会话和不可变作答快照；若直接复用闪卡表或未先升级备份协议，编辑与恢复可能篡改历史统计或遗漏数据。 | PH6-02 至 PH6-09、M6、JSON 导出恢复、每日备份 | 已冻结为独立题库领域模型：题库、题目、AI 版本、会话与快照不写入现有闪卡表。PH6-02 必须先完成迁移、version 2 完整备份和 version 1 恢复兼容的真实 MySQL 回归，后续导入、编辑和刷题任务不得绕过该契约。 |
| `RISK-027` | 2026-08-19 | 范围与交互复杂度风险 | 课程复习工作台同时聚合闪卡、题库、科目筛选、继续活动和跨端上下文；若直接复用全局复习接口或在前端拼接多组目录请求，可能造成课程范围错乱、跨端状态覆盖或首页加载变慢。 | PH5-26、复习入口、课程上下文、题库入口、JSON 备份恢复 | PH5-26 已使用单一认证聚合契约和数据库复习上下文收敛范围，摘要不返回正文；URL 恢复具体工作台或练习，数据库保存跨端默认上下文，失效引用后端回退。带连字符 UUID 的 MySQL JSON 路径已改为安全引用，并兼容数据库原生映射与备份包装格式，真实 MySQL 工作台读取回归通过。后续仅保留大规模分页/虚拟化和真实跨设备并发人工验收风险。 |
| `RISK-028` | 2026-08-19 | 验收环境阻塞 | 独立刷题页、闪卡阅读页与 PH6-16 悬浮问答助手需要认证后的真实题目会话和闪卡才能验证页面层级、流式问答、复制反馈、背题手动翻题、AI 解析入口以及 390px 无遮挡布局；当前终端浏览器只能打开登录页，未取得用户登录态。 | PH6-10、PH6-16、题库入口、独立刷题页、闪卡阅读页、AI 解析与悬浮问答浏览器验收 | 不读取或猜测账号密码，不绕过认证门禁；已完成 TypeScript 类型检查、生产构建、流式服务上下文边界测试、复制控件静态构建和未登录页启动检查。取得合法登录态后补做桌面/390px 刷题与闪卡流式问答、复制反馈、键盘焦点及无横向/底部遮挡验收。 |
| `RISK-029` | 2026-08-20 | 输入规模风险 | 取消题干 255 字符业务上限后，超长题干会增加导入解析、结构化内容序列化和题目页面渲染的负担。 | PH6-11、题库 JSON/Excel 导入、题目可视化编辑与刷题页面 | 题干继续受既有 100 万字符结构化内容上限、节点数量和层级校验保护；JSON、Excel 与应用内服务回归已覆盖 256 字符题干。若出现大规模题库的性能问题，另立性能任务评估更低的体验阈值，而不恢复 255 字符业务限制。 |
| `RISK-030` | 2026-08-20 | 数据一致性与统计边界风险 | 收藏题练习可跨同一科目的多个题库；若将会话强行绑定至其中一个题库，会错误污染该题库的续答和累计统计，或在导出恢复后丢失科目范围。 | PH6-12、题目收藏、科目复习工作台、刷题会话、JSON 导出恢复 | PH6-12 已落地：收藏状态保存于题目本身；收藏题练习以独立的科目范围会话和创建时题目快照保存，不归属任一题库统计。007 迁移已在本机数据库执行；服务、HTTP、导出恢复和工作台继续会话回归已覆盖空收藏范围、跨题库排序、继续会话和恢复后的范围一致性。后续改动必须保持该边界。 |
| `RISK-031` | 2026-08-20 | 性能与内容边界风险 | 全局检索需要跨结构化闪卡正文与题干读取内容；若直接以宽泛 LIKE 扫描或返回完整正文，资料规模增长时会拖慢主库查询，并可能让结果列表泄露超出当前筛选范围的内容。 | PH7-01、MySQL 查询性能、单账号内容隔离 | GOV-003 已冻结为仅检索有效内容且始终经过单账号认证、课程/科目与类型约束的受控摘要接口。PH7-01 实施前须基于真实规模执行 EXPLAIN，定义结果上限、稳定排序和目标定位契约；全文或派生索引只能作为可重建数据实现，不进入 Markdown 或 JSON 备份真相源。 |
| `RISK-032` | 2026-08-20 | 统计语义与数据一致性风险 | 错题备注和学习洞察若混入不可变作答快照，或把进行中会话、软删除题目和样本量过小的知识点纳入统计，会造成历史被改写、正确率误导或薄弱结论失真。 | PH7-02、PH7-03、JSON 备份恢复、复练与洞察页面 | GOV-003 已明确备注与 practice_attempts 隔离，并要求随完整 JSON 和每日备份恢复；PH7-02 复用最近一次有效作答为错误的既有口径。PH7-03 已仅读取有效内容和已完成会话，按 Asia/Shanghai 计算周期；薄弱知识点最小样本量为 3，面板提供空状态与文本摘要，真实 MySQL 已验证软删除、进行中会话和边界日期。后续更多分析能力仍需保持只读历史边界。 |
| `RISK-033` | 2026-08-20 | 数据规模与答案泄露风险 | 手写笔迹若不受控会膨胀 JSON 备份和页面传输；若题目备注在检测作答中提前返回，会泄露与解析相关的学习提示。 | PH8-01、题目解析、JSON/每日备份恢复、移动端画板 | PH8-01 仅保存坐标归一化的矢量笔画，并为笔画数、点数、文字长度和序列化大小设置服务端上限；笔迹与文字备注独立于题目正文和不可变作答快照。题目备注只在背题提交后或检测会话完成后由当前备注表读取并显示，服务、HTTP、备份恢复与 390px 画板回归必须覆盖该边界。 |
| `RISK-034` | 2026-08-20 | 范围与数据一致性风险 | 乱序、固定数量和未做筛选若只在前端临时处理，刷新或续答可能重新抽题或改变候选范围，导致会话题目集合、作答快照和统计口径不一致。 | PH6-15、题库/章节/收藏题练习、会话续答与统计 | PH6-15 将数量上限、乱序开关和未做筛选作为会话创建参数，在服务端事务内先完成候选集合校验、未做过滤、抽样和排序，再把 questionIds、筛选条件与顺序写入会话范围和 practice_attempts；get/续答只读取既有快照，不再次随机。未做以是否存在已提交作答记录判断，且不适用于错题会话。智能随机算法、间隔重复和跨科目混合不纳入本步。 |
| `RISK-035` | 2026-08-21 | 体验范围变更 | 库洛米主题覆盖全局容器与控件；若视觉装饰侵入正文、浅深 token 对比度不完整或仅在设置页生效，会降低复习阅读体验并造成跨页面风格断裂。 | PH5-27、设置、复习、资料、题库、导入、Sheet、移动端与 GA 发布 | PH5-27 复用既有皮肤覆盖层，将黑紫、薰衣草紫、莓果粉与暖黄 token 应用于导航、容器、表单、按钮、分段控件、列表、检索和刷题工作区；装饰为无交互的静态环境几何，不加载角色图片、Logo、音效或联网资源。类型/构建以及 1403px 浅色、390x844 深色 Playwright 回归均通过，正文阅读面维持无纹理高对比；真实 iPhone Safari 设备回归沿用 RISK-021/RISK-028。 |
| `RISK-036` | 2026-08-21 | Provider 兼容性风险 | 学习助手依赖当前 OpenAI 兼容 Provider 真正执行 stream 参数并逐段返回 text/event-stream；部分中转服务可能忽略参数或在上游缓冲完整回答，使界面误以为不是流式输出。 | PH6-16、闪卡学习、刷题问答、AI Provider 设置 | PH6-16 已移除完整 JSON 回退，接口要求 SSE 且添加 no-transform、identity 与 X-Accel-Buffering 响应头，前端按可见增量消费每个分段。Provider 若不支持会显示明确错误而不是整段回答；需在合法认证态以当前 Provider 完成一次真实问答，必要时更换支持 stream 的模型或服务。 |

## 12. 技术债与延期能力

| ID | 事项 | 来源 | 状态 | 偿还计划 |
|---|---|---|---|---|
| `DEBT-001` | 第一版不直接解析 PDF、扫描件或 OCR | 产品范围 | `暂缓` | 核心复习链路稳定后，再独立设计 PDF 解析预览、AI 拆分和人工确认流程。 |
| `DEBT-002` | 第一版不支持离线复习和离线编辑 | 发布范围 | `暂缓` | 未来若验证存在刚需，再设计缓存范围、写入队列和冲突合并策略。 |
| `DEBT-003` | 第一版只保存当前 AI 讲解，不保留历史版本 | AI 范围 | `暂缓` | 出现版本回溯需求后再增加历史表和清理策略。 |
| `DEBT-004` | 第一版不提供自定义标签、提醒、随机复习和间隔重复算法 | 复习范围 | `暂缓` | 以真实使用数据确认价值后分别拆分，不合并为一个大功能。 |

## 13. 变更日志

### `LOG-125` 2026-08-21 - 细化学习助手小猫贴图

- 任务：`PH6-16`
- 阶段/里程碑：第六阶段 / M6
- 动作：将悬浮入口贴图从夸张表情调整为简洁圆脸小猫，补充眼睛高光、细线胡须和低饱和项圈；拖动、键盘微调、位置保存与问答面板逻辑保持不变。
- 结果：小猫贴图的表情和配色更克制，在 56px 入口尺寸下保持主体可辨识，不再使用醒目的大嘴和装饰吊饰。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；仅保留既有 Vite 大体积 chunk 提示。
  - git diff --check -- apps/web/src/App.tsx apps/web/src/styles.css apps/web/public/study-assistant-cat.svg progress/tasks/phase-6.json 通过。
  - Playwright 浏览器桥接启动失败，认证态学习工作区视觉验收未伪造通过。
- 影响文件：`apps/web/public/study-assistant-cat.svg`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：取得合法认证态后，在 1440px、390px、深色模式验证小猫贴图清晰度、拖动边界、键盘微调、面板避让和无横向溢出，再收尾 PH6-16。

### `LOG-124` 2026-08-21 - 柔化学习助手悬浮问答界面

- 任务：`PH6-16`
- 阶段/里程碑：第六阶段 / M6
- 动作：将悬浮入口调整为紧凑圆角控件，收敛面板边界与阴影，加入短促展开动效及减少动态效果降级；同步提问气泡、输入区和深色、秋日、库洛米主题的视觉层级。
- 结果：悬浮入口和问答面板降低了厚重边框、重阴影与突兀切换感，既有 SSE 流式问答、复制、停止、Esc、ARIA 和检测题内容隔离逻辑未改动。认证态真实学习工作区的桌面/390px 视觉验收仍受登录态限制，PH6-16 保持进行中。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；仅保留既有 Vite 大体积 chunk 提示。
  - git diff --check -- apps/web/src/styles.css progress/tasks/phase-6.json 通过。
  - Playwright 在 http://127.0.0.1:5173 成功加载登录页并且控制台无错误；业务工作区受登录态限制，未伪造认证态视觉验收。
- 影响文件：`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：取得合法认证态后，在 1440px、390px 和深色模式验证入口、展开面板、焦点、底部避让及横向溢出，再收尾 PH6-16。

### `LOG-123` 2026-08-21 - 实现 AI Provider 优先级与失败自动降级

- 任务：`PH6-17`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增 011_ai_provider_failover.sql，为 Provider 增加候选池启用状态和 priority；增加候选池状态/排序 API 与设置页复选框、上下移动控件；将闪卡讲解、题目讲解、导入格式修正和悬浮框流式问答统一接入按优先级的失败前自动降级。
- 结果：首选 Provider 在首段有效结果前发生网络、超时、非成功状态或无效内容时会尝试下一启用渠道；悬浮框已输出首段内容后不切换或混拼。API Key 仍只在后端解密，全部渠道失败返回明确错误。认证态设置页视觉验收仍受 RISK-021/RISK-028 环境限制。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，159 项 API 测试全部通过。
  - npm run build 通过。
  - npm run test:ai-provider:integration 通过真实 MySQL 与 HTTP 回归。
  - Provider、AI 讲解、题目 AI、悬浮问答和迁移定向回归共 23 项通过。
  - npm run db:migrate 通过，011_ai_provider_failover.sql 已登记并执行。
- 影响文件：`database/migrations/011_ai_provider_failover.sql`、`apps/api/src/ai-provider-service.ts`、`apps/api/src/ai-explanation-service.ts`、`apps/api/src/question-ai-service.ts`、`apps/api/src/import-service.ts`、`apps/api/src/study-assistant-service.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`packages/shared/src/api.ts`、`progress/tasks/phase-6.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：取得合法认证态后补做设置页 1440px 与 390px 候选池交互和错误提示视觉回归；熔断、冷却、可用率统计和跨模型策略另立任务。

### `LOG-116` 2026-08-20 - 补齐题目备注入口与解析下方展示

- 任务：`PH8-01`
- 阶段/里程碑：第八阶段 / M8
- 动作：将题目备注入口加入刷题题目工具区，背题中和检测完成后可用，检测作答中隐藏；保存后立即更新当前题目，并让文字/笔迹备注复用解析的下方内容块样式直接展示。
- 结果：题目在刷题流程中有明确的备注入口，备注展示位置与解析一致，未改变检测期间答案、解析和备注的隐藏边界。
- 验证：
  - npm run typecheck 通过。
  - npm run build:web 通过。
  - npm test 通过，145 项 API 测试全部通过。
  - 开发服务 http://127.0.0.1:5175/ 返回 200。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-8.json`、`progress/logs.json`
- 下一步：认证态真实浏览器与真实设备回归仍沿用 RISK-021/RISK-028；高级笔迹编辑能力另立任务。

### `LOG-115` 2026-08-20 - 完成闪卡与题目的文字、手写备注

- 任务：`PH8-01`
- 阶段/里程碑：第八阶段 / M8
- 动作：新增闪卡备注表和题目笔迹字段，提供认证读写 API、矢量画板 Sheet、解析后备注展示、检测隐藏、完整 JSON/每日备份恢复，并补齐迁移、服务、前端和数据治理测试。修复画板连续笔画会覆盖上一笔的问题，并将损坏笔迹映射为 409。
- 结果：PH8-01 达到验收标准：文字和手写均可保存、清除和恢复；题目备注只在背题提交后或检测完成后的解析下方显示；备注不进入不可变作答快照。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，145 项 API 测试全部通过。
  - npm run db:migrate 通过，010_phase_8_handwritten_notes.sql 已执行。
  - npm run build 通过。
  - 认证态真实浏览器与真实设备回归仍受 RISK-021/RISK-028 环境限制。
- 影响文件：`database/migrations/010_phase_8_handwritten_notes.sql`、`packages/shared/src/api.ts`、`apps/api/src/card-review-note-service.ts`、`apps/api/src/question-service.ts`、`apps/api/src/practice-service.ts`、`apps/api/src/data-governance-service.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`apps/api/src/card-review-note-service.test.ts`、`apps/api/src/question-service.test.ts`、`apps/api/src/practice-service.test.ts`、`apps/api/src/data-governance-service.test.ts`、`progress/tasks/phase-8.json`、`progress/logs.json`
- 下一步：多颜色、压感、荧光笔、区域擦除、画布背景和笔迹历史另立任务；认证态真实设备验收沿用 RISK-021/RISK-028。

### `LOG-114` 2026-08-20 - 细化学习洞察图表层级

- 任务：`PH7-04`
- 阶段/里程碑：第七阶段 / M7
- 动作：依据 UI/UX 图表与表格规范，补充折线基线、低饱和面积填充、末点标注、状态标识和移动端分组间距。
- 结果：洞察面板的视觉层级与数据锚点更清晰，仍保持颜色为辅助信息且不改变既有统计口径。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/logs.json`
- 下一步：取得认证态后补做桌面与 390px 真实视觉验收。

### `LOG-113` 2026-08-20 - 重构学习洞察统计图表

- 任务：`PH7-04`
- 阶段/里程碑：第七阶段 / M7
- 动作：登记 PH7-04 边界，沿用 F2 Hairline Line 重构逐日复习量图表，并将状态变化、题库结果和薄弱知识点改为紧凑表格与比例摘要。
- 结果：前端图表重构完成，未修改学习洞察 API、数据库统计口径或既有学习历史；待进度展示层同步后验收。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过。
  - npm run progress:check 待执行 progress:build 后复核。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/config.json`、`progress/tasks/phase-7.json`、`progress/logs.json`
- 下一步：运行 progress:build 与 progress:check，并在可用认证态后补做桌面与 390px 视觉验收。

### `LOG-112` 2026-08-20 - 冻结闪卡与题目的文字、手写备注边界

- 任务：`PH8-01`
- 阶段/里程碑：第八阶段 / M8
- 动作：新增第八阶段和 PH8-01，明确复用题目当前文字备注、为闪卡新增对应记录，并以归一化矢量笔迹支持触控与手写笔；题目备注随解析显示。
- 结果：本步开始实施。笔迹和文字均保持当前版本，不进入作答快照；检测作答期间不显示题目备注。
- 验证：
  - 任务卡、产品定位、开发方案、设计规范、风险与阶段元数据已同步。
- 影响文件：`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`、`progress/config.json`、`progress/tasks/phase-8.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：实现迁移、认证 API、画板 Sheet、题目解析展示、备份恢复与自动化验收。

### `LOG-111` 2026-08-20 - 完成周期学习洞察与薄弱知识点

- 任务：`PH7-03`
- 阶段/里程碑：第七阶段 / M7
- 动作：新增共享洞察契约、Asia/Shanghai 周期聚合服务、认证 API、服务/HTTP/真实 MySQL 回归；复习工作台工具区按需打开 7/30 天洞察面板，使用 F2 Hairline Line 点线图、可见摘要、空状态和薄弱知识点列表。
- 结果：用户可查看最近 7 天或 30 天的有效闪卡复习量、掌握状态变化、已完成题库正确率和达到 3 次样本量的薄弱知识点；不写入学习历史。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，142 项 API 测试全部通过。
  - node --import tsx --test apps/api/test/learning-insights-real-mysql-http.test.ts 通过。
  - npm run build 通过。
  - 认证态浏览器视觉回归受 RISK-021/RISK-028 环境阻塞，未伪造通过。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/learning-insights-service.ts`、`apps/api/src/learning-insights-service.test.ts`、`apps/api/src/learning-insights-http.test.ts`、`apps/api/test/learning-insights-real-mysql-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-7.json`、`progress/config.json`、`progress/logs.json`、`progress/risks.json`
- 下一步：更多图表类型、多课程比较、长期报表和真实设备认证态视觉回归另立任务。

### `LOG-110` 2026-08-20 - 完成错题备注、筛选与巩固复练收尾

- 任务：`PH7-02`
- 阶段/里程碑：第七阶段 / M7
- 动作：补充错题查询服务单元测试、认证 HTTP 契约和真实 MySQL/HTTP 回归；在错题筛选 Sheet 增加 Tab 焦点循环并保留 Esc 关闭和触发按钮焦点回归；同步任务卡与展示层。
- 结果：累计错题只保留最近一次仍为错误的有效题目，支持知识点、题型和日期筛选；当前备注与作答快照隔离并可纳入 JSON/每日备份恢复；筛选结果可创建背题或检测会话。浏览器连接因当前桌面 native pipe bridge 不可用未能建立，认证态视觉验收按既有阻塞记录保留，不伪造通过。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，139 项 API 测试全部通过。
  - node --import tsx --test apps/api/test/wrong-answer-real-mysql-http.test.ts 通过。
  - npm run build、npm run progress:build 和 npm run progress:check 通过。
- 影响文件：`apps/web/src/App.tsx`、`apps/api/src/wrong-answer-service.test.ts`、`apps/api/src/wrong-answer-http.test.ts`、`apps/api/test/wrong-answer-real-mysql-http.test.ts`、`progress/tasks/phase-7.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：PH7-03 负责周期学习洞察；随机复练、间隔重复、复杂备注历史和附件不纳入后续小步。

### `LOG-098` 2026-08-19 - 修复课程工作台资料聚合的续读查询

- 任务：`PH5-26`
- 阶段/里程碑：第五阶段 / M2、M5、M6、GA
- 动作：将材料 UUID 作为 MySQL JSON 对象键安全引用；兼容数据库原生续读映射与完整备份包装格式；在真实复习 MySQL/HTTP 夹具中新增课程、科目和工作台请求回归。
- 结果：带连字符 UUID 不再触发 ER_INVALID_JSON_PATH，/api/review/workspace 返回 200，并能恢复对应资料的最近闪卡；完整 API 测试和生产构建保持通过。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，127 项 API 测试全部通过。
  - npm run test:review:integration 通过真实 MySQL 与 HTTP 回归。
  - npm run build 通过，Web、API 和共享类型均可构建。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`apps/api/src/review-workspace-service.ts`、`apps/api/test/review-real-mysql-http.test.ts`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：继续按 PH5-26 原边界完成工作台剩余验收；性能优化和真实设备并发验收另立后续范围。

### `LOG-108` 2026-08-20 - 落地题目备注与同科目累计错题复练后端契约

- 任务：`PH7-02`
- 阶段/里程碑：第七阶段 / M7
- 动作：新增独立备注表和读写 API，扩展 JSON/每日备份恢复，增加按最近有效错误、知识点、题型和时间筛选的错题查询，并支持背题/检测会话创建。
- 结果：备注与作答快照保持隔离；迁移 009 已执行；旧题库级累计错题语义保留。前端筛选入口和完整浏览器回归仍待补齐。
- 验证：
  - npm run db:migrate 通过。
  - npm run typecheck 通过。
  - npm test 通过，136 项测试全部通过。
  - npm run build 通过。
- 影响文件：`database/migrations/009_phase_7_wrong_answer_notes.sql`、`apps/api/src/question-service.ts`、`apps/api/src/wrong-answer-service.ts`、`apps/api/src/practice-service.ts`、`apps/api/src/data-governance-service.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`packages/shared/src/api.ts`、`progress/logs.json`
- 下一步：补齐错题筛选/复练前端入口、HTTP/真实 MySQL 边界测试和备注可视化编辑后，再完成 PH7-02。

### `LOG-109` 2026-08-20 - 接入错题筛选与备注可视化入口

- 任务：`PH7-02`
- 阶段/里程碑：第七阶段 / M7
- 动作：在题库页增加同科目累计错题筛选 Sheet、知识点/题型/日期筛选、背题/检测入口和结果内备注编辑；补齐移动端单列布局、触控尺寸、关闭焦点回归和深色模式 token。
- 结果：前端类型检查和生产构建通过；浏览器可访问登录页，但当前无合法登录态，受保护题库页面的桌面/390px/横屏真实点击回归待认证后补做。
- 验证：
  - npm run typecheck 通过。
  - npm run build 通过。
  - Playwright 127.0.0.1 登录页加载通过；业务页认证态验证受登录态阻塞。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/api.ts`、`apps/web/src/styles.css`、`progress/logs.json`
- 下一步：取得合法认证态后完成 1440px、390px、375px、横屏、深色模式和 Sheet 焦点回归，再收尾 PH7-02。

### `LOG-096` 2026-08-19 - 收敛课程复习工作台到具体练习入口

- 任务：`PH5-26`
- 阶段/里程碑：第五阶段 / M2、M5、M6、GA
- 动作：在已完成数据库工作台聚合的基础上，接入闪卡阅读/背默模式、题库直接背题/检测/继续会话、工作台/闪卡/题库会话 hash URL 恢复；课程摘要同步纳入有效闪卡续读，并补充入口样式。
- 结果：课程工作台不再把题库动作导向管理页，资料行可直接进入阅读或背默，题库行可创建或恢复现有 PracticeSessionPanel；复制或刷新复习 URL 时按 URL 优先恢复，失效会话回退工作台，数据库上下文继续作为默认跨端状态。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，128 项测试全部通过。
  - 真实 ReviewWorkspaceService SQL mock、认证、复习、题库和课程工作台 HTTP 契约测试保持通过。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`apps/api/src/review-workspace-service.ts`、`apps/api/src/review-workspace-service.test.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：PH5-26 已完成；题目编辑、随机/间隔重复和大规模性能优化不纳入本步。

### `LOG-080` 2026-08-13 - 移除 AI 修正高亮匹配拦截

- 任务：`PH5-24`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：保留既有高亮数据/移除 AI 路径高亮重定位校验/增加修正后直接应用回归/同步进度
- 结果：AI 格式修正仍会重新解析正文并确认点击的问题类型减少，但不再因正文变化或既有高亮无法在新节点中重新定位而阻断预览写回；系统提示词要求 Provider 保留正文措辞、公式及既有高亮对应内容，应用时对 AI 修正卡片原样保留原高亮数据。普通手工编辑导入的高亮匹配校验保持不变。
- 验证：
  - npm test 通过，99 项测试全部通过。
  - npm run typecheck 通过。
- 影响文件：`apps/api/src/import-service.ts`、`apps/api/src/import-service.test.ts`、`apps/api/src/ingestion.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：继续观察真实 Provider 的格式修正输出；非 JSON 或格式问题未减少时仅在对应问题行提示失败，不自动重试或润色正文。

### `LOG-079` 2026-08-13 - 修复导入 AI 响应未更新预览的问题

- 任务：`PH5-24`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：兼容 JSON 代码围栏与单项 cards 响应/将失败原因显示在对应问题行/补预览正文更新回归/进度同步
- 结果：Provider 返回 JSON 代码围栏或旧版单项 cards 时仍可解析并写回服务端预览；成功响应会同步问题列表和下方闪卡正文，拒绝或请求失败会显示在对应问题下方，便于区分 Provider 输出无效、正文改写和格式未减少。
- 验证：
  - 新增测试确认代码围栏和单项 cards 响应后问题清零、预览有效且闪卡正文已更新。
- 影响文件：`apps/api/src/import-service.ts`、`apps/api/src/import-service.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：真实 Provider 仍返回非 JSON 或格式错误未减少时，优先查看对应问题行的失败原因；不自动润色正文。

### `LOG-078` 2026-08-13 - 完成导入正文格式 AI 逐条并发修正

- 任务：`PH5-24`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：登记逐条边界/改为单问题单卡 Provider 请求/保留其他卡片诊断/加入并发版本合并与前端独立状态/补 JSON 语法和正文保真回归/构建与进度同步
- 结果：需要处理列表中的每条可修正正文格式问题均有独立优化格式按钮；并发点击时每个请求只包含对应的一条 issue 和一张 card，服务端按预览修订合并结果，其他卡片错误不会被清除。AI 只能修正 Markdown、GFM 表格或 LaTeX 格式，正文文字、公式实质内容和既有高亮不变；JSON 读取、语法和 Schema 错误不显示入口且不创建预览令牌。
- 验证：
  - npm test 通过，96 项测试全部通过。
  - npm run typecheck 通过。
  - npm run build 通过；仅有既有 Vite 大 Chunk 警告。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/import-service.ts`、`apps/api/src/import-service.test.ts`、`apps/api/src/health.test.ts`、`apps/web/src/App.tsx`、`DESIGN.md`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：真实 Provider 的特定模型兼容和同一卡片并发冲突提示另立任务；本步不自动重试、不整合全部错误、不润色正文。

### `LOG-077` 2026-08-13 - 降低深色模式闪卡高亮刺眼度

- 任务：`PH5-23`
- 阶段/里程碑：第五阶段 / M2、M3、GA
- 动作：登记边界/覆盖深色文本与公式高亮色/同步设计 token/前端构建与进度校验
- 结果：深色模式普通阅读高亮（含公式整段高亮）改用低饱和深色背景与柔和的高对比正文色；秋日枫叶深色模式使用独立暖色背景，跟随系统且系统为深色时同样生效。浅色高亮、挖空遮罩、高亮持久化和交互行为保持不变。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；仅有既有 Vite 大 chunk 警告。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`apps/web/src/styles.css`、`DESIGN.md`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：若真实设备仍存在特定屏幕的对比度或色觉适配问题，另立无障碍视觉校准任务。

### `LOG-072` 2026-08-12 - 扩展 JSON 模板多章节示例

- 任务：`PH5-10`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：登记最小边界/新增第二章示例/补充双章节解析断言/进度同步
- 结果：JSON 下载模板现在以同级“第一章”和“第二章”展示多章节填写结构；第二章包含独立的第一节和示例闪卡，保持单资料内容导入协议不变。
- 验证：
  - npm --workspace @knowledge-flashcards/api run test 通过，88 项测试全部通过。
  - JSON 模板测试断言两个章节的标题和顺序，并确认第二章示例闪卡可被解析器直接读取。
  - npm run typecheck:api 通过。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：待浏览器验收环境恢复后，继续 PH5-10 的导入页格式切换和模板下载交互回归。

### `LOG-076` 2026-08-12 - 补齐复习界面的了解状态统计

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记最小边界/扩展资料摘要契约/聚合了解状态/展示五项统计/补充服务与真实 MySQL 回归/构建和进度同步
- 结果：复习首页当前资料统计已显示“了解”。资料摘要新增 familiarCount，服务端按当前资料聚合 mastery_status=familiar；统计同时保留闪卡、掌握、未评估和努力，未新增复习入口或改变筛选、续读和状态写入语义。
- 验证：
  - npm run typecheck 通过。
  - npm run build 通过；Web 构建仅保留既有大 Chunk 警告。
  - npm run test:review:integration 通过真实 MySQL + HTTP 验收，覆盖卡片改为了解后当前资料 familiarCount 为 1。
  - npm run progress:build 通过。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/api/test/review-real-mysql-http.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：PH5-13 继续按既有验收范围推进；不在本步增加了解状态的快捷复习入口或新的筛选语义。

### `LOG-075` 2026-08-12 - 优化复习闪卡切换性能

- 任务：`PH5-22`
- 阶段/里程碑：第五阶段 / M2、GA
- 动作：登记单卡导航边界/移除前端完整卡片列表状态/单卡响应提供前后 ID 和位置/保持状态后跳转与资料续读/服务和真实 MySQL 回归/构建与进度同步
- 结果：普通上一张、下一张不再请求当前筛选集，也不传输其他闪卡的正文、公式、标注或 AI 讲解。单卡响应只附带前后卡片 ID、当前序号和总数；前端直接使用 nextCardId 或 previousCardId 请求目标卡。单卡打开不再在写入浏览记录前后两次完整读取相同正文，改为轻量定位后只完整读取一次。
- 验证：
  - npm run typecheck 通过。
  - npm --workspace @knowledge-flashcards/api run test 通过，91 项测试全部通过。
  - npm run test:review:integration 通过真实 MySQL + HTTP 验收，覆盖 nextCardId 直达下一张、相邻 ID 回指、状态变更、编辑和按资料续读。
  - npm run build 通过；Web 构建仅保留既有大 Chunk 警告。
- 影响文件：`packages/shared/src/api.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/api/src/app.ts`、`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/api/test/review-real-mysql-http.test.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：正文、表格或公式本身的渲染性能问题需在实际设备复现后另立任务。

### `LOG-073` 2026-08-12 - 补全无效表格分隔行定位信息

- 任务：`PH5-10`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：登记诊断边界/在解析器统计正文表格序号/输出表头和分隔行原文/覆盖多个表格回归/类型检查与单元测试
- 结果：Markdown 与 JSON body 的无效表格分隔行报错现在显示正文内第几个表格、表头、分隔行原文和既有资料层级上下文，仍保留精确源文件行列位置。合法表格同样纳入序号，因此大量表格中可按正文出现顺序准确定位；未改变 GFM 表格校验标准或导入协议。
- 验证：
  - npm --workspace @knowledge-flashcards/api run typecheck 通过。
  - npm --workspace @knowledge-flashcards/api run test 通过，90 项测试全部通过；新增覆盖 Markdown 与 JSON body 中合法表格后的多个无效分隔行序号、所属闪卡、表头和原文。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：待浏览器验收环境恢复后，继续 PH5-10 的导入页格式切换、模板下载、合法文件预览与桌面和 390x844 无横向溢出回归。

### `LOG-074` 2026-08-12 - 移除表格分隔行猜测式预校验

- 任务：`PH5-10`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：确认正文误报范围/登记边界/删除基于字符的表格预检/保留解析后表格结构校验/补充 Markdown 与 JSON body 回归
- 结果：导入解析不再根据 -、:、| 等字符猜测表格分隔行。只有 remark-gfm 实际解析出的 table 节点才会进入既有列数一致性校验，不能组成 GFM 表格的文本完整保留为正文。
- 验证：
  - npm --workspace @knowledge-flashcards/api run typecheck 通过。
  - npm --workspace @knowledge-flashcards/api run test 通过，90 项测试全部通过，覆盖 Markdown 和 JSON body 中含 -、:、| 的普通正文。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：待浏览器验收环境恢复后，继续 PH5-10 的导入页格式切换、模板下载、合法文件预览与桌面和 390x844 无横向溢出回归。

### `LOG-071` 2026-08-12 - 兼容表格单元格内联高亮标记

- 任务：`PH5-21`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：复现实际 JSON 表格报错/定位内部令牌与 GFM 预校验冲突/替换安全令牌/补充表格单元格与跨单元格回归/真实文件解析与集成验收/进度同步
- 结果：修复了 [[hl:...]] 内部转换令牌含连字符，触发表格分隔行预校验误报的问题。现在单个 GFM 表格单元格中的普通文本标记可被移除并生成正确锚点；未转义管道符、跨单元格、粗体等跨 Markdown 节点标记继续被拒绝，原有错误表格分隔行检测保持有效。
- 验证：
  - npm --workspace @knowledge-flashcards/api test 通过，88 项测试全部通过。
  - 实际解析 deepseek_json_20260812_23f9dc.json 通过：3 张表格闪卡、21 个表格高亮、0 个问题，正文无协议标记。
  - npm run test:import:integration 与 npm run typecheck 通过。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：公式内联标记、跨节点高亮和预览逐次命中定位需要另立任务。

### `LOG-070` 2026-08-12 - 支持 JSON body 内联高亮标记导入

- 任务：`PH5-20`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：沿用已确认边界/实现内联标记解析与剥离/保留精确锚点并限制正文修正重定位/模板切换主协议/兼容旧 highlights/单元与真实 MySQL 集成验收/进度同步
- 结果：JSON body 可直接用 [[hl:原文]] 标记文本位置，同一词每个目标位置独立包裹即可，无需 occurrence。导入解析后正文自动恢复为无标记文本并生成结构化 text 锚点；预览正文未修改时沿用精确位置，修改后仅在目标唯一时重新定位，重复或缺失会阻止写入。highlights 数组继续支持公式和旧 text/occurrence 文件，下载模板已以内联文本标记为主、formula 数组声明为兼容示例。
- 验证：
  - npm --workspace @knowledge-flashcards/api test 通过，86 项测试全部通过。
  - npm run test:import:integration 通过真实 MySQL + HTTP 验收，内联文本与公式高亮均写入 highlights。
  - npm run typecheck 通过。
  - JSON 模板可由解析器直接通过，正文示例包含两处 [[hl:危险源]]，解析结果不保留协议标记。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`apps/api/src/import-service.ts`、`apps/api/src/import-service.test.ts`、`apps/api/test/import-real-mysql-http.test.ts`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：公式内联标记、预览命中定位和旧 occurrence 文件批量迁移需要另立协议或交互任务。

### `LOG-069` 2026-08-12 - 固化 JSON 高亮重复判定算法与反例

- 任务：`PH5-19`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：登记最小边界/写入结构化命中单位与编号算法/补充短数字、包含、重叠、跨节点和公式反例/模板测试与类型检查/进度同步
- 结果：模板现明确与解析器一致的重复标准：text 只在单个解析后普通文本节点内按连续子串命中，包含与重叠均计数；formula 按完整 math/inlineMath 节点值匹配；occurrence 按正文节点深度优先和节点内字符起点编号，不是 highlights 数组下标。补充了 1、10、1000 短数字、跨 Markdown 节点不可匹配及公式包装符号不计入等反例。
- 验证：
  - npm --workspace @knowledge-flashcards/api test 通过，83 项测试全部通过。
  - 模板断言覆盖连续子串、occurrence 顺序、唯一匹配门槛、短数字、跨节点和公式反例；模板可由解析器直接通过。
  - npm run typecheck 通过。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：导入预览展示节点路径、字符位置和上下文属于后续诊断反馈任务。

### `LOG-068` 2026-08-12 - 将 JSON 高亮去重要求改为模型可执行指令

- 任务：`PH5-18`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：登记最小边界/补充模型逐卡搜索与自检规则/加入禁止性约束/模板解析与 API 测试/进度同步
- 结果：JSON 模板新增面向生成模型的硬性高亮流程：每张卡独立逐字搜索目标文本并统计全部连续子串命中；唯一时才省略 occurrence，重复时先扩展为唯一的完整原文语义短语，仍重复才填写实际 occurrence。明确禁止根据 highlights 数组下标猜测序号、默认选择首个匹配，并要求输出前逐条复查。
- 验证：
  - npm --workspace @knowledge-flashcards/api test 通过，83 项测试全部通过。
  - 模板断言覆盖逐字搜索、唯一短语优先、禁止数组下标猜测和输出前逐条复查；模板可由解析器直接通过。
  - npm run typecheck、npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：自动展示匹配上下文或自动修复 occurrence 需要作为独立导入诊断任务评估。

### `LOG-067` 2026-08-12 - 明确 JSON 高亮重复匹配判定标准

- 任务：`PH5-17`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：登记模板说明边界/补充连续子串与包含关系规则/加入 occurrence 示例/模板解析与 API 测试/进度同步
- 结果：JSON 模板已明确：重复按单张卡片 body 内的连续文本子串计数，目标被更长文本包含时仍算一次；普通正文、列表与表格单元格均参与计数，卡片之间独立。示例以“10人以下”分别独立出现和包含在“3人以上10人以下”中的两次匹配，演示 occurrence: 1、occurrence: 2。
- 验证：
  - npm --workspace @knowledge-flashcards/api test 通过，83 项测试全部通过。
  - 模板断言确认重复规则包含连续文本子串与包含关系说明，并由解析器直接通过。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：导入错误若要列出匹配次数、位置和上下文，需作为新的诊断反馈任务实施。

### `LOG-066` 2026-08-12 - 支持 JSON 内容级高亮导入

- 任务：`PH5-16`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：登记协议边界/解析文本与公式声明/自动生成结构化锚点/应用时二次校验并事务写入/更新模板/全量验证/进度同步
- 结果：内容级 JSON 已支持卡片级 highlights。文本高亮先收集当前卡片完整 body 中按正文从前到后的匹配，再按 occurrence 选择第 N 次出现，计数跨普通文本、粗体等节点但单条 text 不跨节点；公式按完整公式节点匹配。未匹配、重复未指定序号或修正正文后失配均会阻止应用，成功导入在 cards 写入后于同一事务写入 highlights。JSON 模板现自带中文规则说明和可直接导入的同段多项、重复 occurrence、公式及重叠高亮示例。
- 验证：
  - npm --workspace @knowledge-flashcards/api test 通过 83 项测试，覆盖有效文本/公式匹配、body 全局 occurrence 顺序、跨节点计数、重复歧义、模板、数据库写入及修正后事务回滚。
  - npm run test:import:integration 通过真实 MySQL + HTTP 验收，JSON 文本与公式高亮均写入 highlights，夹具数据在测试结束后清理。
  - JSON 模板可被自身完整解析，覆盖同段多项、重复文本 occurrence、公式和重叠高亮示例。
  - npm run typecheck 通过。
  - npm run build 通过；仅有既有 Vite 大 chunk 警告。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/import-service.ts`、`apps/api/src/ingestion.test.ts`、`apps/api/src/import-service.test.ts`、`apps/api/test/import-real-mysql-http.test.ts`、`项目开发方案.md`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：跨节点匹配、批量标注及外部学习状态迁移需要根据实际数据来源另立任务；完整 JSON 备份保持现有恢复流程。

### `LOG-065` 2026-08-12 - 生成完成后展开 AI 讲解

- 任务：`PH5-09`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记最小边界/调整成功回调面板状态/前端类型与生产构建验证/进度同步
- 结果：AI 讲解请求成功保存并刷新当前卡片后，讲解折叠区会保持展开，新结果可立即阅读。首次进入和切换卡片仍默认折叠；用户手动切换、生成失败和停止生成均不改变现有折叠状态。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过，转换 2000 个模块；仅有既有大 chunk 警告。
  - 成功回调仅将 setExplanationOpen 状态设为 true；失败与取消分支没有面板状态更新。
- 影响文件：`apps/web/src/App.tsx`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：继续 PH5-09 既有的 AI 讲解页面浏览器回归；不扩展自动展开到首次进入、切卡、失败或取消场景。

### `LOG-064` 2026-08-12 - 回收箱资料不参与导入去重

- 任务：`PH5-10`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/过滤软删除资料/来源哈希索引迁移/单元与真实 MySQL 回归/进度同步
- 结果：导入预览和应用阶段的来源哈希去重现仅匹配未删除资料。移入回收箱的同源资料不会触发重复跳过，可保留原记录并重新导入为新资料；活动资料的重复跳过行为保持不变。
- 验证：
  - npm run typecheck:api 通过。
  - 导入服务单元测试 6 项通过，覆盖活动资料重复跳过与回收箱同源资料重新导入。
  - npm run db:check 与 npm run db:migrate 通过，004_allow_reimport_trashed_materials.sql 已执行。
  - npm run test:import:integration 通过真实 MySQL + HTTP 验收；软删除资料后重新导入同一 ZIP 成功。
- 影响文件：`apps/api/src/import-service.ts`、`apps/api/src/import-service.test.ts`、`apps/api/test/import-real-mysql-http.test.ts`、`database/migrations/004_allow_reimport_trashed_materials.sql`、`项目开发方案.md`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：继续 PH5-10 既有的导入页浏览器验收；不改动回收箱恢复、永久删除或资料移动流程。

### `LOG-063` 2026-08-11 - 实现资料九宫格长按拖入回收站

- 任务：`PH5-15`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/Pointer Events 长按/顶部垃圾桶目标区/复用软删除/键盘替代入口/桌面与移动端回归/进度同步
- 结果：科目页资料九宫格支持长按拖动。触发后显示固定顶部垃圾桶，资料跟随指针；进入目标区时垃圾桶切换危险态，松开后调用既有资料软删除并将资料即时移出网格。目标区外松开不改变资料，短按仍进入详情；Delete 键提供无障碍替代入口。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build 通过；仅有既有 Vite 大 chunk 警告。
  - 独立 Playwright 使用隔离认证与资料接口夹具验证桌面长按、顶部垃圾桶激活、DELETE /api/hierarchy/material/:id、删除反馈、目标区外取消与短按详情跳转；未读取账户凭证或写入业务资料。
  - 独立 Playwright 在 390x844 视口验证长按删除前后 body 与 document 宽度均为 390。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：资料排序、跨科目移动和批量删除如有需求，须以独立任务评估。

### `LOG-062` 2026-08-11 - 实现当前学习资料选择与局部统计

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/资料级状态汇总/当前资料名称选择器/快捷入口收敛/类型服务浏览器回归/进度同步
- 结果：复习首页以只显示资料名称的当前资料选择器替换原资料列表。每份资料返回闪卡、掌握、未评估和努力数量；选择资料后，顶部统计与全部、未评估、努力快捷入口均只使用该资料数据。资料刷新后会保留有效选择，失效时回退到首份资料。
- 验证：
  - npm run typecheck 通过。
  - npm --workspace @knowledge-flashcards/api run test 通过 76 项。
  - npm run build:web 通过；仅有既有大 chunk 警告。
  - npm run test:review:integration 通过真实 MySQL + HTTP 验收。
  - 独立 Playwright 使用本地认证与 Dashboard 响应桩验证资料名称选择、统计/快捷入口切换、390x844 无横向溢出和零控制台错误。
  - npm run progress:build 与 npm run progress:check 均因 PH5-15 引用未知 catalog-navigation 模块失败，详见 RISK-024；未手改自动生成 Markdown。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：待 PH5-15 恢复进度生成器后，重新执行 progress:build 和 progress:check；不扩展当前资料选择、续读或筛选范围。

### `LOG-061` 2026-08-11 - 收紧背默模式开关宽度

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/将轨道宽度调整为 30px/同步滑块行程/前端类型检查与构建
- 结果：背默模式开关保持 18px 轨道与滑块等高，视觉轨道由 68px 收紧至 30px；滑块开启状态的横向位移同步调整为 12px，仍可准确停靠在右端。黑橙色状态、180ms 过渡和 44px 触控行高均保持不变。
- 验证：
  - npm run typecheck:web 通过。
  - npm --workspace @knowledge-flashcards/web exec vite build 通过；仅有既有大 chunk 警告。
  - npm run progress:build 与 npm run progress:check 均因既有 PH5-15 引用未知 catalog-navigation 模块失败；本次未改动 PH5-15 或自动生成的 Markdown 展示层。
- 影响文件：`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`progress/risks.json`
- 下一步：继续 PH5-13 的已登录桌面与 390px 页面交互回归；不扩展背默模式为持久化设置或修改既有重点挖空。

### `LOG-060` 2026-08-11 - 修复背默模式轨道被主题输入样式放大

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：根据截图定位 CSS 层叠/提升主题专用选择器优先级/重置高度与内边距/类型构建与进度同步
- 结果：定位到秋日主题后段的通用 input 规则以相同优先级覆盖了开关轨道的 height、min-height 和 padding，导致实际容器仍高于滑块。开关规则现绑定 data-appearance 主题范围并显式重置最小高度、内边距和边框，优先级高于通用规则；轨道与滑块将实际同为 18px 高。
- 验证：
  - npm run typecheck:web 通过。
  - npm --workspace @knowledge-flashcards/web exec vite build 通过；仅有既有大 chunk 警告。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：继续 PH5-13 的已登录桌面与 390px 页面交互回归；不扩展背默模式为持久化设置或修改既有重点挖空。

### `LOG-059` 2026-08-11 - 将背默模式轨道与滑块等高

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/轨道高度收紧/滑块垂直对齐/开启位移与阴影同步/类型构建与进度同步
- 结果：背默模式轨道高度由 24px 调整为与 18px 滑块相同的高度，滑块取消上下留白并以 50px 横向位移完成开启状态；阴影同步减弱以保持扁平观感。轨道长度、黑橙状态、动画时长和 44px 触控行高保持不变。
- 验证：
  - npm run typecheck:web 通过。
  - npm --workspace @knowledge-flashcards/web exec vite build 通过；仅有既有大 chunk 警告。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：继续 PH5-13 的已登录桌面与 390px 页面交互回归；不扩展背默模式为持久化设置或修改既有重点挖空。

### `LOG-058` 2026-08-11 - 收紧背默模式开关比例

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/压缩设置行/长条轨道与方形滑块/保持黑橙状态与过渡/类型构建与进度同步
- 结果：背默模式的设置行收紧为 44px 高，轨道调整为 68 x 24px 的扁平圆角矩形，滑块缩为 18 x 18px 小圆角方块，开启位移同步为 44px。关闭近黑、开启橙色、按压反馈和减少动态效果逻辑保持不变。
- 验证：
  - npm run typecheck:web 通过。
  - npm --workspace @knowledge-flashcards/web exec vite build 通过；仅有既有大 chunk 警告。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：继续 PH5-13 的已登录桌面与 390px 页面交互回归；不扩展背默模式为持久化设置或修改既有重点挖空。

### `LOG-057` 2026-08-11 - 优化背默模式开关视觉

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/专用 Switch 样式/主题覆盖隔离/黑橙状态/滑块过渡与减少动态效果/类型构建与进度同步
- 结果：背默模式控件改为紧凑的 iOS 风格设置行。关闭时为近黑轨道，开启时为橙色轨道；白色滑块以 180ms 缓动位移，并补齐悬停、按压和键盘焦点状态。专用选择器覆盖了通用 input 的主题样式，避免深浅色模式下轨道形态或色彩错乱；用户启用减少动态效果时不播放切换动画。
- 验证：
  - npm run typecheck:web 通过。
  - npm --workspace @knowledge-flashcards/web exec vite build 通过；仅有既有大 chunk 警告。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：继续 PH5-13 的已登录桌面与 390px 页面交互回归；不扩展背默模式为持久化设置或修改既有重点挖空。

### `LOG-056` 2026-08-11 - 完成复习首页背默模式

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/首页背默 Switch/复用重点挖空/自动启用与切卡重置/类型构建与进度同步
- 结果：复习首页新增背默模式 Switch。开启后，进入任意资料的闪卡会自动启用既有挖空模式，当前及后续闪卡的全部已标注重点均会隐藏；点击遮罩可逐项显示，“显示全部”可展示当前卡的所有标注，切换卡片后重新遮挡。关闭开关后闪卡正常显示。该模式不会持久化，也不改动重点高亮、闪卡内容、API 或数据库。
- 验证：
  - npm run typecheck:web 通过。
  - npm --workspace @knowledge-flashcards/web exec vite build 通过；仅有既有大 chunk 警告。
  - 独立 Playwright 可打开本机登录页；未读取账号凭证，已登录复习页面的桌面和 390px 交互回归仍受 RISK-021 限制。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：继续 PH5-13 的已登录桌面与 390px 页面交互回归；不扩展背默模式为持久化设置或修改既有重点挖空。

### `LOG-055` 2026-08-11 - 完成按资料续读与复习资料选择收敛

- 任务：`PH5-13`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/资料进度映射/旧设置迁移/筛选契约收敛/JSON 备份兼容/真实 MySQL 回归/构建与进度同步
- 结果：复习续读从全局单张卡片改为按资料保存。打开、切卡和状态自动前进均会保存当前资料的最后卡片；面板逐项显示资料及可继续卡片，进入资料会直接恢复保存位置。筛选仅保留资料与掌握状态，章节、节和关键词选择已从共享契约、服务和页面移除。完整 JSON 导出与恢复支持资料进度映射，并兼容读取旧的全局位置。PH5-13 另有背默模式范围尚未实现，因此任务保持进行中。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 76 项 API 与服务测试，包含资料进度导出与恢复。
  - npm run test:review:integration 通过真实 MySQL + HTTP 验收，验证两份随机资料的续读位置互不覆盖，清理夹具后恢复原有进度设置。
  - npm run build 通过，只有既有 Vite 大 chunk 警告。
  - 独立 Playwright 确认本机登录页可达；未读取本机凭证，登录后桌面和 390px 页面回归继续受 RISK-021 限制。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/review-service.ts`、`apps/api/src/app.ts`、`apps/api/src/data-governance-service.ts`、`apps/api/src/review-service.test.ts`、`apps/api/src/data-governance-service.test.ts`、`apps/api/test/review-real-mysql-http.test.ts`、`apps/api/test/m4-real-mysql-http.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/api.ts`、`apps/web/src/styles.css`、`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`、`progress/config.json`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：继续 PH5-13 的背默模式和已登录浏览器回归；不改写已完成的资料续读与筛选契约。

### `LOG-054` 2026-08-11 - 实现资料详情逐卡内容编辑与同节排序

- 任务：`PH5-14`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记执行边界/独立闪卡编辑卡/复用正文编辑锁和资源上传/接入同节上下排序/主题适配/构建与进度校验
- 结果：资料详情的单行闪卡标题已替换为独立编辑卡。用户可展开一张卡，直接使用既有结构化正文编辑器修改标题、正文、表格、公式与受管图片；编辑卡取得、续期并释放已有 90 秒租约。每张卡提供上移和下移图标，复用既有排序接口后按返回层级立即更新顺序。全量类型和浏览器交互回归仍受 PH5-13 未收敛的续读迁移阻塞，PH5-14 保持进行中。
- 验证：
  - npm --workspace @knowledge-flashcards/web exec vite build 通过，完成 2000 个模块转换与生产产物生成；仅有既有大 chunk 警告。
  - http://127.0.0.1:5173/ 返回 HTTP 200 且包含 root 容器。
  - npm run progress:build 与 npm run progress:check 通过。
  - npm run typecheck 被 PH5-13 的共享 Review 类型与旧复习页不一致阻塞；本机服务日志同时显示续读迁移 SQL 的 setting_value 列歧义。
  - 应用内浏览器连接初始化报 Cannot redefine property: process，无法补做桌面与 390px 逐卡交互回归。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：由 PH5-13 修复共享 Review 契约与续读 SQL 后，重新执行全量 typecheck/build，并补做资料详情逐卡编辑、保存、排序和移动端无横向溢出回归。

### `LOG-053` 2026-08-11 - 完成 JSON 与备份恢复兼容

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：登记边界/完整 JSON 快照/目录与封面恢复校验/旧 JSON 默认归属/资源哈希写回/真实 MySQL 备份回归/进度同步
- 结果：完整 JSON、手动备份和每日备份现在包含课程、科目、排序、资料归属、封面关联和掌握状态历史。恢复会先校验秘密字段、路径和资源哈希，再在同一事务中按课程、科目、资料、资源、封面和状态历史顺序写入；旧 JSON 缺少目录字段时自动补入不可删除的“待整理 / 待整理”。封面原图与缩略图会随资源正文写回。PH5-07 的全部验收项已满足。
- 验证：
  - npm run typecheck、npm run build 和 npm test 通过，后端单测共 76 项。
  - npm run test:backup:integration 通过真实 MySQL、HTTP 和受管资源目录验收，课程、科目排序、资料归属、封面原图/缩略图和状态历史均存在于 data.json 与资源归档。
  - 数据治理单测验证新快照的事务写入顺序、旧 JSON 默认目录回退，以及封面原图和缩略图的实际文件恢复。
- 影响文件：`apps/api/src/data-governance-service.ts`、`apps/api/src/data-governance-service.test.ts`、`apps/api/test/backup-real-mysql-http.test.ts`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：PH5-07 已完成。发布主线继续由 PH5-01、PH5-02 和 PH5-05 验收；资料跨科目移动、按课程/科目筛选复习和长期趋势交互需另立任务。

### `LOG-052` 2026-08-11 - 完成导入归属选择与选择器内新增

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：登记边界/导入归属契约/事务内课程科目校验/归属 Sheet 与内置新增/真实 MySQL 导入回归/桌面和移动浏览器验收/进度同步
- 结果：导入应用请求现强制携带课程和科目，服务端在同一事务内确认所选科目属于课程后才写入 materials.subject_id，缺失或错配归属均返回 400。导入预览新增归属 Sheet，可逐级选择并在 Sheet 内新增课程或当前课程下的科目，创建后立即选中；每次新文件、格式切换、取消或应用完成均清空归属，避免旧选择使后续文件绕过必选门槛。PH5-07 总任务仍进行中，JSON/备份恢复兼容留待下一小步。
- 验证：
  - npm run typecheck:shared、npm run typecheck:api、npm run typecheck:web、npm run build、npm test 通过，后端单测共 75 项。
  - npm run test:import:integration 通过真实 MySQL + HTTP 回归，确认导入资料的 subject_id 等于请求指定科目。
  - 隔离 Playwright 在桌面验证初始应用禁用，选择器内新增课程和科目后自动选中，应用请求包含 courseId=course-new 与 subjectId=subject-new。
  - 390x844 视口复验新文件会清空归属、Sheet 选择后才启用应用，documentElement.scrollWidth 与 clientWidth 均为 390，控制台无错误；截图已保存。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/import-service.ts`、`apps/api/src/import-service.test.ts`、`apps/api/test/import-real-mysql-http.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`output/playwright/ph5-07-import-destination-desktop.png`、`output/playwright/ph5-07-import-destination-mobile.png`
- 下一步：待确认 PH5-07 下一小步：补齐 JSON 导出、每日备份和恢复中的课程、科目、排序、封面资源与状态历史兼容；不扩展资料跨科目移动或复习筛选。

### `LOG-051` 2026-08-11 - 完成移动端页首与账号入口压缩

- 任务：`PH5-01`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记边界/移除全局账号工具栏/设置页账号管理/页首图标化/深浅色可读性回归/构建与浏览器验收/进度同步
- 结果：已移除已登录状态下每页顶部的账号与退出条，将当前账号和退出操作放入设置页的账号分组。复习、资料、设置、管理与导入页首的常用操作已使用带 Tooltip 和无障碍名称的 Lucide 图标；移动端标题缩小为 22px，页首保持 44px 高度且标题与操作区同一行。深色模式下账号信息保持高对比可读。认证接口、会话、密码策略和主导航业务均未改动。
- 验证：
  - npm run typecheck:web 与 npm run build:web 通过；生产构建仅有既有大 chunk 警告。
  - 隔离 Playwright 在 390x844 模拟已登录会话下验证全局账户工具栏不存在、设置账号分组可见、页首高度为 44px、图标按钮具有 aria-label 与 Tooltip，documentElement 宽度与滚动宽度均为 390。
  - 深色模式视觉截图复核通过，账号名称计算色为 rgb(242, 242, 247)。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`output/playwright/ph5-01-mobile-header-settings.png`、`output/playwright/ph5-01-mobile-header-settings-dark.png`
- 下一步：PH5-01 继续完成真实 iPhone Safari 的添加到主屏幕、长按选词和 Home Indicator 安全区验收；本次不扩展认证或离线业务。

### `LOG-050` 2026-08-11 - 完成课程与科目低频管理

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：登记边界/前端目录 API 封装/课程和科目更多操作 Sheet/科目移动选择/删除确认和阻止反馈/深色与桌面手机浏览器验收/进度同步
- 结果：课程和科目单列行右侧新增带无障碍名称的更多操作入口。课程可改名、上移、下移和删除；科目额外可在 Sheet 选择其他课程移动。删除均经确认，服务端拒绝系统或非空节点时，Sheet 保持打开并显示明确原因；成功操作会刷新当前列表。PH5-07 总任务仍进行中，导入归属、资料跨科目移动和 JSON/备份兼容留待后续小步。
- 验证：
  - npm run typecheck、npm run build 和 npm test 通过，后端单测共 74 项。
  - 独立 Playwright 在桌面和 390x844 视口验证课程/科目更多入口、操作 Sheet、科目移动候选、排序边界和删除确认；非空课程删除返回 409 后 Sheet 保持并显示阻止原因。
  - 390x844 视口 documentElement.scrollWidth 与 clientWidth 均为 390；手动深色模式下操作项计算色为 rgb(242, 242, 247)，遮罩层级为 40。
- 影响文件：`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：待确认 PH5-07 下一小步：导入应用前强制选择课程和科目，并支持选择器内新增；不扩展资料跨科目移动、JSON/备份兼容或复习筛选。

### `LOG-049` 2026-08-11 - 完成资料详情名称与封面管理

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：登记边界/资料改名与封面 API/原图和缩略图存储/资源生命周期修复/深色可读性修复/桌面和手机浏览器验收/真实 MySQL 回归/进度同步
- 结果：资料详情页现提供 iOS 风格编辑 Sheet，可改名并上传、替换或移除本地 PNG、JPEG、GIF、WebP 封面。服务端校验 5MB 上限与文件签名，保留原图并生成最长边 512px 的 WebP 缩略图；替换、移除和资料永久删除均会清理封面专属资源，永久删除也会删除关联状态历史。简约深色模式下封面字段标签采用系统辅助文字色，Sheet 内保持可读。PH5-07 总任务仍进行中，课程/科目低频操作、导入归属与 JSON/备份兼容留待后续小步。
- 验证：
  - npm run typecheck、npm run build 和 npm test 通过，后端单测共 74 项。
  - npm run test:catalog:integration 通过真实 MySQL + HTTP + 临时资源目录验收，覆盖资料改名、封面原图与缩略图写入、详情读取和移除后的数据库及文件清理。
  - 独立 Playwright 在桌面和 390x844 视口验证资料详情编辑 Sheet、无封面透明区域、文件选择入口与无横向溢出；移动端 Sheet 层级为 40，控制台无错误。
  - npm run package:windows 通过，Windows 发布包使用固定 Node.js v24.18.0 并安装 178 个生产依赖，包含封面缩略图所需的 sharp。
  - 手动深色模式复核确认资料编辑 Sheet 中封面字段标签计算色为 rgb(152, 152, 157)，与系统深色表面保持可读。
- 影响文件：`apps/api/src/catalog-service.ts`、`apps/api/src/catalog-service.test.ts`、`apps/api/src/catalog-http.test.ts`、`apps/api/src/app.ts`、`apps/api/src/data-governance-service.ts`、`apps/api/src/data-governance-service.test.ts`、`apps/api/test/catalog-real-mysql-http.test.ts`、`apps/api/package.json`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`package-lock.json`、`progress/tasks/phase-5.json`、`progress/logs.json`、`output/playwright/ph5-07-material-editor-desktop.png`、`output/playwright/ph5-07-material-editor-mobile.png`、`output/windows-release/`
- 下一步：待确认 PH5-07 下一小步：在课程与科目单列中接入现有低频改名、排序、科目移动和删除操作；不扩展资料跨科目移动、导入归属或 JSON/备份兼容。

### `LOG-048` 2026-08-11 - 恢复公网账号门禁与 Tunnel

- 任务：`PH5-11`、`PH5-12`
- 阶段/里程碑：第五阶段 / GA
- 动作：按新增对外提供要求回退本机免登录配置/重启受管服务/本机未登录拦截与公网首页验收/进度同步
- 结果：用户确认必须对外提供后，已恢复本机认证配置、PUBLIC_ACCESS_READY 和 CLOUDFLARED_ENABLED；项目 Tunnel 正在运行。免登录模式未保留为当前配置，未登录业务接口恢复为 401，公网首页与登录表单均可访问。
- 验证：
  - /api/auth/session 未登录返回 authenticated=false，/api/catalog/courses 未携带 Cookie 返回 401。
  - 项目 Tunnel PID 已写入运行记录且对应进程正在运行；review.panspan.cloud 返回 200、text/html 且包含 root 容器。
  - Playwright 公网首页快照显示用户名、密码和登录按钮。
- 影响文件：`.env.local（本机忽略配置）`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：继续 PH5-05 其余 GA 回归。本机免登录查看需先设计不绕过公网认证的独立安全模型。

### `LOG-047` 2026-08-11 - 临时屏蔽本机账号密码登录

- 任务：`PH5-11`
- 阶段/里程碑：第五阶段 / GA
- 动作：登记安全边界/停用公网开关/保留本机哈希注释/重启受管服务/本机与公网状态验证/进度同步
- 结果：本机已切换为免登录查看模式：AUTH_USERNAME 和 AUTH_PASSWORD_HASH 不再生效，PUBLIC_ACCESS_READY 与 CLOUDFLARED_ENABLED 均为 false；原有哈希未写入进度源，仍仅在忽略的本机配置中以注释保留。受管 Tunnel 已停止，公网域名返回 502。认证实现和所有业务数据均未修改。
- 验证：
  - /api/auth/session 返回 authenticated=true，未携带 Cookie 的 /api/catalog/courses 返回 200。
  - 本机 Web 首页返回 200；.runtime/process.json 不含 Tunnel PID 或公网 URL。
  - review.panspan.cloud 返回 502，未再提供本机应用。
- 影响文件：`.env.local（本机忽略配置）`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：恢复登录时先取消 PH5-11 的本机配置注释并重启服务；恢复公网前必须重新执行 PH5-05 账号安全和 HTTPS 回归。

### `LOG-046` 2026-08-11 - 补齐资料目录的课程与科目新增入口

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：执行边界登记/目录新增请求封装/课程与科目名称 Sheet/桌面和手机夹具浏览器验收/构建验证/进度同步
- 结果：资料首页现提供“新增课程”图标，进入课程后提供“新增科目”图标。两处均以名称 Sheet 调用既有目录 API，空名称不可提交；成功后关闭 Sheet、刷新当前列表并显示简短反馈。手机 Sheet 层级高于底部导航，不会遮挡取消或添加操作。PH5-07 总任务仍进行中，资料封面、导入归属及 JSON/备份兼容留待后续小步。
- 验证：
  - npm run typecheck、npm run build 和 npm test 通过；后端单测共 70 项。
  - 独立 Playwright 使用本地响应夹具验证课程、科目新增按钮、空名称禁用、名称提交、成功反馈与当前列表刷新；未向真实资料库写入测试课程或科目。
  - 390x844 视口验证 Sheet 层级为 40、高于底部导航 30，documentElement.scrollWidth 与 clientWidth 均为 390；截图保存至 output/playwright。
- 影响文件：`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`、`output/playwright/ph5-07-catalog-create-desktop.png`、`output/playwright/ph5-07-catalog-create-mobile.png`
- 下一步：待确认 PH5-07 下一小步：实现资料名称和封面 Sheet 管理、封面缩略图及资源清理；不扩展导入归属、JSON/备份兼容或资料跨科目移动。

### `LOG-045` 2026-08-11 - 完成资料详情、状态历史与趋势回归

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：独立浏览器验收/状态写入事务/上海日界修复/服务与真实 MySQL 回归/进度同步
- 结果：已完成资料格进入详情、刷新、浏览器前进/后退、失效地址返回，以及桌面三列和 390px 两列资料格、详情无横向溢出的页面验收。发现当前状态分布与当天趋势不一致后，补齐状态更新的 review_status_history 事务写入；趋势读取保留 MySQL DATETIME 墙上时间并按 Asia/Shanghai 计算，当前和最新快照现一致。PH5-07 总任务仍进行中，封面管理、导入归属及 JSON/备份兼容留待后续小步。
- 验证：
  - npm run typecheck、npm run build 和 npm test 通过，后端单测共 70 项。
  - npm run test:catalog:integration 通过真实 MySQL + HTTP 验收，确认状态事件、当天趋势与当前分布一致，且夹具已清理。
  - 独立 Playwright 在 1440x900 与 390x844 验证详情 URL、返回、刷新、前进/后退、失效地址、3/2 列网格、图表摘要和无横向溢出；截图保存至 output/playwright。
- 影响文件：`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/api/src/catalog-service.ts`、`apps/api/src/catalog-service.test.ts`、`apps/api/test/catalog-real-mysql-http.test.ts`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`output/playwright/ph5-07-detail-desktop.png`、`output/playwright/ph5-07-detail-mobile.png`
- 下一步：待确认 PH5-07 下一小步：实现资料名称和封面 Sheet 管理、封面缩略图及资源清理；不扩展导入归属、JSON/备份兼容或资料跨科目移动。

### `LOG-044` 2026-08-11 - 完成 JSON、Excel 内容导入与模板下载的代码及自动化验收

- 任务：`PH5-10`
- 阶段/里程碑：第五阶段 / M5
- 动作：执行边界登记/JSON 与 Excel 解析/模板下载 API/导入页分段选择/安全与回归测试/类型构建与进度同步
- 结果：已在保留 Markdown/ZIP 链路的前提下，新增单资料 JSON 与 Excel 导入。JSON 使用固定 format/version/层级协议，Excel 只读取“闪卡”工作表及五列；两种正文均进入原有安全 Markdown 解析、预览、重复哈希检测和应用事务。前端导入页支持三种格式选择，并可直接下载 JSON/Excel 模板。完整 JSON 备份未被接入普通导入。浏览器连接环境仍阻塞交互验收，PH5-10 保持进行中。
- 验证：
  - npm --workspace @knowledge-flashcards/api run test 通过，70 项测试全部通过，覆盖 JSON/Excel、模板端点、完整备份拦截、上传原始 JSON 与既有 Markdown 回归。
  - npm run typecheck 和 npm run build 通过；生产构建仅有既有 Vite 大 chunk 警告。
  - npm run progress:build 与 npm run progress:check 通过。
  - Codex 内置浏览器初始化报 Cannot redefine property: process，暂不能完成格式切换、模板下载和桌面/390x844 页面回归。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/app.ts`、`apps/api/src/ingestion.test.ts`、`apps/api/src/health.test.ts`、`apps/api/src/xlsx-populate.d.ts`、`packages/shared/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/api.ts`、`apps/web/src/styles.css`、`apps/api/package.json`、`apps/web/package.json`、`package-lock.json`、`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：待浏览器连接恢复后完成前端导入路径与移动布局验收；未通过前不提前完成 PH5-10。

### `LOG-043` 2026-08-11 - 实现资料详情读取与四状态分布、30 天趋势

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：执行边界登记/详情读取 API/上海自然日快照/详情 Hash 路由/分布与趋势可视化/服务和真实 MySQL HTTP 验收/构建验证/进度同步
- 结果：已新增 GET /api/catalog/materials/:materialId，返回资料信息、有效章/节/单卡层级、当前掌握/了解/努力/未评估分布及按 Asia/Shanghai 每日结束状态计算的连续 30 天快照。资料格可进入详情 URL，详情页按资料信息、横向堆叠分布、30 天趋势和内容层级单列展示，并提供图表无障碍摘要；无闪卡和无章节均有空状态。浏览器自动化连接初始化受运行时 process 属性冲突阻断，尚未完成页面交互和 390px 无溢出回归，当前步骤保持进行中。
- 验证：
  - npm run typecheck:api、npm run typecheck:web、npm run build 和 npm test 通过；后端单测共 70 项。
  - npm run test:catalog:integration 通过真实 MySQL + HTTP 验收，详情接口返回有效层级、当前四状态分布和最后一天快照；状态历史夹具已按外键顺序清理。
  - 本机 API 健康检查返回 HTTP 200，现有 Vite 前端返回 HTTP 200；无会话访问业务目录端点返回 401。
  - Codex 内置浏览器初始化报 Cannot redefine property: process，无法执行桌面与 390x844 页面回归。
- 影响文件：`apps/api/src/catalog-service.ts`、`apps/api/src/catalog-service.test.ts`、`apps/api/src/catalog-http.test.ts`、`apps/api/test/catalog-real-mysql-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：待 Codex 内置浏览器连接恢复后，补做资料格进入详情、刷新、返回、失效 URL、桌面和 390x844 无横向溢出以及图表摘要回归；未通过前保持本步骤进行中。

### `LOG-042` 2026-08-11 - 补齐 AI 讲解中文引号强调兼容

- 任务：`PH5-09`
- 阶段/里程碑：第五阶段 / GA
- 动作：复现 CommonMark 边界行为/任务范围更新/中文引号强调节点转换/截图语法回归/类型构建验证/进度同步
- 结果：发现模型输出 `**“不符合规定”**` 时，CommonMark 会因强调符紧贴中文引号而将整段视为普通文本。AI Markdown 渲染器现仅对这类未解析文本补充转换，生成保留中文引号的 strong/emphasis 节点，不改变已合法解析的 Markdown 或安全规则。PH5-09 仍待浏览器环境恢复后完成页面回归。
- 验证：
  - 截图同构语法 `事故隐患的本质是**“不符合规定”**——即生产经营活动中。` 经解析和转换后为 text + strong + text，strong 内容为 `“不符合规定”`。
  - npm run typecheck:web 通过。
  - npm run build:web 通过，转换 2000 个模块；仅有既有大 chunk 警告。
- 影响文件：`apps/web/src/App.tsx`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：在可用浏览器环境完成 AI 讲解面板的结构化 DOM、原始 HTML 抑制和桌面/移动无溢出回归后，更新 PH5-09 验收状态。

### `LOG-041` 2026-08-11 - 完成课程、科目与资料九宫格 URL 导航

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：执行边界登记/目录读取客户端/Hash URL 路由/课程科目单列/资料九宫格/编辑入口保留/桌面与 iPhone 浏览器验收/进度同步
- 结果：资料页已使用 #/materials、课程与科目子路径表达逐层导航。课程与科目以单列呈现，资料以桌面 3 列、390px 2 列九宫格展示名称、闪卡数量和可用缩略图；无封面资料保留透明封面区。既有层级编辑改为 #/materials/manage 低频入口，并可由目录返回。失效目录地址会显示可识别错误并提供返回路径，目录请求失败时会核验会话失效。
- 验证：
  - npm run typecheck、npm run build 和 npm test 通过；后端回归共 64 项。
  - Playwright 临时本机目录数据在桌面和 390x844 视口验证课程→科目→资料格、刷新、浏览器前进/后退、管理入口返回及移动端无横向溢出；documentElement.scrollWidth 与 clientWidth 均为 390。
  - 目录页面浏览器控制台无错误；用于验收的本地 mock/Vite 进程和临时脚本已停止、移除。
- 影响文件：`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`output/playwright/ph5-07-catalog-desktop.png`、`output/playwright/ph5-07-catalog-mobile.png`
- 下一步：进入 PH5-07 下一小步，补齐资料详情读取 API 与详情页四状态分布、最近 30 天趋势；不实现封面上传、资料改名/跨科目移动、导入归属选择或数据治理扩展。

### `LOG-040` 2026-08-11 - 修复 AI 讲解 Markdown 直出

- 任务：`PH5-09`
- 阶段/里程碑：第五阶段 / GA
- 动作：执行边界登记/Markdown GFM 数学解析/复用只读内容节点/原始 HTML 抑制/类型构建与解析回归/进度同步
- 结果：已将 AI 讲解由按空行拆分的纯文本段落改为安全的 Markdown AST 渲染。标题、强调、列表、分隔线、表格和公式将显示为结构化内容；复用既有链接协议限制，忽略原始 HTML，讲解公式保持只读。浏览器自动化连接被运行时 process 属性冲突阻断，PH5-09 保持进行中，待补页面回归。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过，转换 222 个模块；仅有既有大 chunk 警告。
  - Node 解析回归确认三级标题、粗体、分隔线、列表、GFM 表格、公式和 HTML 节点的 AST 类型符合预期。
  - 浏览器自动化初始化被 Codex 运行时 process 属性冲突阻断，未将该环境问题误记为产品回归。
- 影响文件：`apps/web/package.json`、`package-lock.json`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：在可用浏览器环境完成 AI 讲解面板的结构化 DOM、原始 HTML 抑制和桌面/移动无溢出回归后，更新 PH5-09 验收状态。

### `LOG-039` 2026-08-11 - 接入本机深浅色显示模式切换

- 任务：`PH5-08`
- 阶段/里程碑：第五阶段 / GA
- 动作：范围更新/三档分段控件/本机持久化/根元素主题状态/简约与秋日枫叶 token 覆盖/构建验证/进度同步
- 结果：已在设置页外观分组加入跟随系统、浅色、深色显示模式，并以本机存储和根元素 data-color-mode 应用于简约及秋日枫叶皮肤。任务因目录页既有 TypeScript 类型错误和浏览器自动化环境故障而保持进行中。
- 验证：
  - npm --workspace @knowledge-flashcards/web exec vite build 通过，生成生产前端产物。
  - http://127.0.0.1:5173/ 返回 HTTP 200。
  - npm run typecheck:web 被 App.tsx:3217 的既有 MaterialsRoute/manage 类型不匹配阻断。
  - 浏览器自动化初始化被 process 属性冲突阻断，尚缺桌面与 iPhone 交互回归。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：PH5-07 修复资料目录路由类型后，完成 PH5-08 的桌面与 iPhone 显示模式持久化、皮肤适配和无横向溢出验收。

### `LOG-038` 2026-08-11 - 完成课程、科目与资料九宫格读取/维护 API

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：目录服务/受保护 HTTP 路由/课程科目新建改名排序移动/空节点删除限制/资料格聚合查询/封面元数据读取/单元 HTTP 真实 MySQL 验收/进度同步
- 结果：已提供课程/科目读取、新建、改名、上下排序和科目跨课程移动 API；系统“待整理”和非空节点不能删除。指定科目 API 返回资料格、有效闪卡数量和已有封面原图/缩略图元数据，供后续逐层导航页面直接使用。
- 验证：
  - npm run typecheck、npm run build 和 npm test 通过，共 64 项测试。
  - npm run test:catalog:integration 通过真实 MySQL + HTTP 验收，覆盖课程/科目读取、资料格封面与闪卡数、科目排序/移动、新建和空/非空删除边界。
  - HTTP 单测覆盖目录路由成功响应、服务调用和可识别 404；全局账号门禁已覆盖所有 /api/ 业务路由。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/catalog-service.ts`、`apps/api/src/catalog-service.test.ts`、`apps/api/src/catalog-http.test.ts`、`apps/api/test/catalog-real-mysql-http.test.ts`、`apps/api/src/app.ts`、`apps/api/package.json`、`package.json`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：将目录 API 接入资料页，实现课程→科目→资料九宫格逐层 URL 导航与桌面/iPhone 布局；不在该步实现封面上传、资料详情统计、导入归属选择或数据治理扩展。

### `LOG-037` 2026-08-11 - 完成秋日枫叶皮肤、AI 真实取消与挖空反馈

- 任务：`PH5-06`
- 阶段/里程碑：第五阶段 / GA
- 动作：外观本机持久化/主题 token/静态叶片环境层/Provider 取消信号/无文字进度与停止图标/文本公式遮罩/服务与浏览器验收/进度同步
- 结果：PH5-06 已完成。设置页可切换简约和秋日枫叶并保存到本机；秋日浅色/深色将主题限制在环境层，闪卡正文保持纯净高对比。AI 停止图标可取消在途请求，API 将取消传递至 Provider，取消不覆盖旧讲解也不保存半成品；长重点遮罩采用暖灰底、枫棕细描边和叶脉纹理，短词无纹理并以 160ms 淡入揭示。
- 验证：
  - npm run typecheck、npm --workspace @knowledge-flashcards/api run test（55 项）和 npm run build 通过。
  - npm run test:ai-explanation:integration 通过 3 项，服务单测确认 Provider 请求中止且 ai_explanations 未写入。
  - Playwright 模拟 API 验证主题 localStorage 持久化、桌面和 390x844 无横向溢出、文本/公式挖空及夜枫阅读面；截图已保存至 output/playwright。
  - npm run progress:check 通过。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/api.ts`、`apps/web/src/styles.css`、`apps/api/src/app.ts`、`apps/api/src/ai-explanation-service.ts`、`apps/api/src/ai-explanation-service.test.ts`、`apps/api/test/ai-explanation-http.test.ts`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`output/playwright/ph5-06-autumn-settings-desktop.png`、`output/playwright/ph5-06-autumn-cloze-desktop.png`、`output/playwright/ph5-06-autumn-cloze-mobile.png`、`output/playwright/ph5-06-autumn-cloze-mobile-dark.png`
- 下一步：PH5-06 已完成；GA 剩余验收继续归属 PH5-05，课程与资料导航继续按 PH5-07 任务卡推进。

### `LOG-036` 2026-08-11 - 完成 PH5-07 数据迁移与共享 API 契约第一小步

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：课程/科目表/默认归属回填/资料封面关系/掌握状态历史/旧写入默认值兼容/目录与趋势共享类型/迁移单测/真实 MySQL 验收/进度同步
- 结果：已新增课程、科目、资料封面关系和卡片掌握状态历史；旧资料统一归入系统保留的“待整理 / 待整理”，既有卡片写入迁移日初始快照。兼容迁移固定系统默认归属并为 materials.subject_id 配置默认值，使既有写入继续可用。共享契约覆盖目录导航、封面、四状态分布、30 天趋势和导出恢复兼容字段。
- 验证：
  - npm run typecheck:shared、npm run typecheck:api 和 npm test 通过，共 57 项测试。
  - npm run db:migrate 成功应用 002_phase_5_07_catalog.sql 与 003_phase_5_07_legacy_material_default.sql；npm run db:check 通过。
  - npm run test:review:integration 通过，确认既有省略 subject_id 的资料写入路径可用。
  - 真实 MySQL 查询确认固定默认课程/科目、materials.subject_id 默认值、2 份资料、603 条状态历史和 3 个关键外键。
  - npm run progress:build 与 npm run progress:check 待本次结构化源更新后执行。
- 影响文件：`database/migrations/002_phase_5_07_catalog.sql`、`database/migrations/003_phase_5_07_legacy_material_default.sql`、`packages/shared/src/api.ts`、`apps/api/src/catalog-migration.test.ts`、`apps/api/src/catalog-legacy-default.test.ts`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：进入 PH5-07 下一小步，实现课程/科目目录与资料九宫格读取/维护 API；本步不扩展前端页面、封面上传处理或统计趋势计算。

### `LOG-035` 2026-08-11 - 确认课程、科目、资料九宫格与学习趋势开发方案

- 任务：`PH5-07`
- 阶段/里程碑：第五阶段 / M5
- 动作：逐项确认目录层级/归属/导航/封面/统计/迁移/导出边界，并登记执行边界、风险和后续任务
- 结果：已确认课程→科目→资料逐层 URL 导航；课程/科目单列、资料九宫格桌面 3 列与移动 2 列；资料封面可选且无封面透明；资料详情页显示掌握/了解/努力/未评估分布和最近 30 天 Asia/Shanghai 状态快照趋势；课程、科目、封面和状态历史进入 JSON/备份，Markdown 协议保持不变。PH5-07 保持未开始，等待代码实现。
- 验证：
  - 用户已确认 PH5-07 的层级、导入归属、迁移默认值、封面规则、排序/移动/删除、统计历史和响应式边界。
  - 已更新项目产品定位.md、项目开发方案.md、DESIGN.md、progress/config.json、progress/tasks/phase-5.json 和 progress/risks.json。
- 影响文件：`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`、`progress/config.json`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：代码变更前先按 PH5-07 边界完成数据库迁移和共享 API 契约设计；不扩展复习筛选、Markdown 协议或离线能力。

### `LOG-034` 2026-08-11 - 确认秋日枫叶皮肤与专注反馈实施边界

- 任务：`PH5-06`
- 阶段/里程碑：第五阶段 / GA
- 动作：确认全局主题范围、简约回退、浅深色支持、AI 真实取消与无文字反馈、挖空遮罩规则，并写入开发方案、设计规范和任务卡。
- 结果：已建立 PH5-06 唯一 P1 任务，状态保持未开始。任务将全局秋日枫叶皮肤、AI 取消与生成反馈、挖空美化作为一个可验收交付，不拆分子任务；尚未开始代码实现。
- 验证：
  - 用户已确认全局范围、简约回退、浅色/深色模式、正文专注边界、AI 真实取消和挖空视觉方向。
  - npm run progress:build 已通过，已生成项目开发进度.md 与当前执行看板.md。
  - npm run progress:check 已通过，结构化进度源与自动生成展示层一致。
- 影响文件：`项目开发方案.md`、`DESIGN.md`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：在 PH5-06 内一次性实现主题 token、设置持久化、真实取消、无文字生成反馈和挖空遮罩优化；不拆分为子任务。

### `LOG-033` 2026-08-11 - 修复一键启动补拉 Cloudflare Tunnel

- 任务：`PH5-05`
- 阶段/里程碑：第五阶段 / GA
- 动作：执行边界登记/复现应用已运行时 Tunnel 未启动/修复受管 Tunnel 补启动/开启本机连接器开关/HTTPS 冒烟验收/进度同步
- 结果：已修复 start.ps1 在应用已运行时无条件提前退出的问题：当 Tunnel 缺失且 CLOUDFLARED_ENABLED=true 时，脚本会补启动受管 Tunnel、回写进程状态和独立日志；同时将 Vite 源站固定监听 127.0.0.1，消除 cloudflared 访问 IPv4 源站时的 502。
- 验证：
  - 使用已有应用进程执行 start.ps1，成功补启动 Cloudflare Tunnel 并写入 .runtime/process.json。
  - 停止后重新执行启动脚本，应用和 Tunnel 均成功启动；再次执行启动脚本不会重复拉起进程。
  - npm run tunnel:check、npm run typecheck 和 start.ps1 PowerShell 语法解析通过。
  - review.panspan.cloud 返回 HTTP 200，页面包含 root；Tunnel 最近日志无源站连接错误。
- 影响文件：`start.ps1`、`apps/web/vite.config.ts`、`.env.local（本机忽略配置）`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：继续 PH5-05 的 Windows、iPhone、备份恢复和账号安全完整回归；公网监控和其他 Cloudflare 配置不在本步范围。

### `LOG-032` 2026-08-11 - 完成单账号密码门禁和会话恢复

- 任务：`PH5-04`
- 阶段/里程碑：第五阶段 / GA
- 动作：执行边界登记/scrypt 密码哈希/登录会话 Cookie/失败限速/API 拦截/前端登录与退出/本机凭证哈希配置/HTTP 与自动化回归/进度同步
- 结果：PH5-04 已完成。后端使用 scrypt 哈希校验单账号密码，登录成功设置 30 天 HttpOnly、SameSite=Lax 会话 Cookie，失败登录按客户端指纹限速；未登录业务 API 统一返回 401。前端先恢复会话，刷新同一浏览器无需重复登录；密码明文未进入代码、进度源或导出边界。
- 验证：
  - npm --workspace @knowledge-flashcards/api run test 通过 54 项，新增认证测试覆盖健康检查例外、401 拦截、Cookie 会话、退出和 429 限速。
  - npm run typecheck:api、npm run typecheck:web、npm run build:api、npm run build:web 通过。
  - 本机 HTTP 冒烟验证正确凭据登录 200、错误凭据 401、带 Cookie 访问业务接口 200；Cookie 含 HttpOnly、SameSite=Lax 和 30 天 Max-Age。
  - PUBLIC_ACCESS_READY=true 已写入本机忽略配置，CLOUDFLARED_ENABLED=false 保持连接器关闭。
- 影响文件：`apps/api/src/auth.ts`、`apps/api/src/auth.test.ts`、`apps/api/src/app.ts`、`apps/api/src/config.ts`、`apps/api/src/server.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`packages/shared/src/api.ts`、`.env.example`、`.env.local（本机忽略配置）`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/config.json`、`progress/logs.json`
- 下一步：进入 PH5-05，执行 GA 全链路发布验收；Cloudflare Tunnel 仍需在已完成门禁基础上单独启动并验证 HTTPS。

### `LOG-031` 2026-08-11 - 完成 Windows 固定 Node.js 发布包

- 任务：`PH5-03`
- 阶段/里程碑：第五阶段 / GA
- 动作：执行边界登记/生产态同源托管/发布目录组装/生产依赖安装/固定 Node.js/配置与迁移入口/包内启动停止验收/进度同步
- 结果：已生成 output/windows-release：生产 API 同源托管 Web 静态文件，发布包携带固定 Node.js v24.18.0、167 个 API 生产依赖、共享包产物、.env.example、数据库迁移入口和启停入口。包内启动脚本只依赖 MySQL 外部服务，不复制本机秘密或实际资料。
- 验证：
  - npm run package:windows 通过。
  - npm run typecheck 通过；npm test 通过 54 项。
  - 使用包内 runtime/node/node.exe 和真实 MySQL 配置启动，/api/health 与首页均返回 200；stop-release.ps1 成功停止服务。
  - 使用包内 runtime/node/node.exe 执行 migrate-release.ps1，真实 MySQL 已迁移状态下重复执行正常退出。
  - 发布包根目录未发现 .env、.env.local、Cloudflare 凭证或 PEM 文件。
- 影响文件：`apps/api/src/app.ts`、`apps/api/src/config.ts`、`package.json`、`scripts/package-windows.ps1`、`scripts/run-powershell.mjs`、`scripts/start-release.ps1`、`scripts/stop-release.ps1`、`scripts/migrate-release.ps1`、`output/windows-release/`、`progress/tasks/phase-5.json`
- 下一步：进入 PH5-04 单账号密码登录和公网安全门禁；本步不提前启动 Cloudflare Tunnel。

### `LOG-030` 2026-08-11 - 启动 PH5-02 并补齐 Cloudflare Tunnel 运行契约

- 任务：`PH5-02`
- 阶段/里程碑：第五阶段 / GA
- 动作：配置模板/秘密边界/ingress 预检/启停状态/本地诊断/进度同步
- 结果：已提供 review.panspan.cloud 到本机 Web 源站的 cloudflared 配置模板，创建 review-panspan-cloud Tunnel 并将 DNS 路由指向新 Tunnel；启动脚本会在应用启动前校验 HTTPS 地址、凭证文件和 ingress 语法，并在受管状态与输出中记录 Tunnel 公网地址。凭证保留在用户目录，PUBLIC_ACCESS_READY=false 时连接器不会启动，PH5-02 保持进行中。
- 验证：
  - PowerShell 解析 start.ps1 和 stop.ps1 通过。
  - cloudflared tunnel --config cloudflared/config.example.yml ingress validate 返回 OK。
  - 使用隔离临时配置和空凭证文件运行 npm run tunnel:check 通过，并明确报告 PUBLIC_ACCESS_READY=false；临时文件已清理。
  - 缺少实际 config.yml、credentials.json 或非 HTTPS 公网地址时 npm run tunnel:check 按预期失败且不输出凭证内容。
  - npm run typecheck、npm run build 和 npm run progress:check 通过。
  - cloudflared tunnel route dns --overwrite-dns 1aa1536f-878f-4843-ae58-677b5222f68a review.panspan.cloud 成功，review.panspan.cloud 当前返回 530，表明 DNS 已到 Cloudflare 但连接器尚未启动。
- 影响文件：`cloudflared/config.example.yml`、`cloudflared/README.md`、`cloudflared/config.yml（本机忽略配置）`、`.env.example`、`.env.local（本机忽略配置）`、`.gitignore`、`package.json`、`scripts/check-tunnel.mjs`、`start.ps1`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：完成 PH5-04 单账号密码门禁后，将 PUBLIC_ACCESS_READY 设为 true，运行启动/停止脚本并验收 review.panspan.cloud HTTPS 稳定访问；在此之前保持连接器关闭。

### `LOG-029` 2026-08-11 - 补齐 PWA 安装入口与移动端安全区域布局

- 任务：`PH5-01`
- 阶段/里程碑：第五阶段 / GA
- 动作：执行边界登记/manifest 与 iPhone 元数据/PNG 图标/静态资源 Service Worker/底部 Tab Bar/dvh 与 safe-area/真实数据库移动浏览器回归/进度同步
- 结果：已补齐可安装 PWA 所需的 manifest、iPhone Safari 元数据、PNG 图标和生产环境 Service Worker 注册；移动端主导航改为带安全区域的底部 Tab Bar，表格、公式和正文选词保持在 390x844 视口内。Service Worker 不缓存 /api 数据。真实 iPhone Safari 添加到主屏幕仍待设备验收，因此 PH5-01 保持进行中。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；生产预览中 manifest、sw.js 和两个 manifest 图标均返回 200，Service Worker 已接管页面。
  - Playwright 桌面与 390x844 移动视口验证无横向溢出、底部导航固定位置和无控制台错误。
  - 真实数据库页面验证表格宽度 324px、公式节点渲染、选中“海因”后高亮按钮可用；截图已保存到 output/playwright。
- 影响文件：`apps/web/index.html`、`apps/web/public/manifest.webmanifest`、`apps/web/public/sw.js`、`apps/web/public/icon.svg`、`apps/web/public/icon-192.png`、`apps/web/public/icon-512.png`、`apps/web/public/apple-touch-icon.png`、`apps/web/src/main.tsx`、`apps/web/src/styles.css`、`apps/web/src/vite-env.d.ts`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：在真实 iPhone Safari 上完成添加到主屏幕、长按选词浮动工具栏和 Home Indicator 安全区验收；本步不扩展离线业务。

### `LOG-028` 2026-08-11 - 完成 M4 AI 与数据治理主链路真实环境验收

- 任务：`PH4-05`
- 阶段/里程碑：第四阶段 / M4
- 动作：真实 MySQL 与资源目录夹具/本地 OpenAI 兼容假 Provider/AI 讲解与高亮复习/Markdown 与 JSON 导出/JSON 与备份恢复/回收站永久删除/时间格式兼容修复/全量验证/进度同步
- 结果：PH4-05 已完成。组合验收验证 Provider 密钥只在后端解密并发送给假 Provider，卡片讲解、当前结果、高亮和复习状态可保存并恢复；Markdown、JSON、资源备份、JSON 恢复、备份恢复和永久删除闭环通过。期间发现 ISO 时间直接写入 MySQL DATETIME(3) 严格模式失败，已统一转换为 MySQL UTC DATETIME 格式。
- 验证：
  - npm run test:m4:integration 通过 1 项真实 MySQL + 资源目录 + HTTP 组合验收，假 Provider 收到 2 次正确鉴权请求，夹具清理完成。
  - npm test 通过 51 项；npm run typecheck、npm run build、npm run db:check 和 npm run storage:check 通过。
  - 导出内容未命中 API Key、ciphertext、password、tunnel 或 secret；恢复后的卡片正文、AI 讲解与高亮可读，永久删除后卡片和资源文件均不存在。
  - npm run progress:check 通过。
- 影响文件：`apps/api/src/data-governance-service.ts`、`apps/api/test/m4-real-mysql-http.test.ts`、`apps/api/package.json`、`package.json`、`progress/config.json`、`progress/tasks/phase-4.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：进入 PH5-01，继续 iPhone Safari 响应式与 PWA 主屏幕安装；本步不扩展 Cloudflare Tunnel、账号登录或 Windows 发布包。

### `LOG-027` 2026-08-11 - 完成每日备份、恢复和回收站永久删除

- 任务：`PH4-04`
- 阶段/里程碑：第四阶段 / M4
- 动作：备份快照与资源副本/保留 7 份/每日调度/恢复前留档/永久删除事务/API 与资料页/真实 MySQL HTTP 与浏览器验收
- 结果：资料页现可查看备份、立即备份和恢复备份；API 启动时检查当天备份并按日调度，成功备份默认保留 7 份。恢复 JSON 或备份均先留档当前数据，回收站永久删除会清理层级关联和不再引用的资源文件。
- 验证：
  - npm test 通过 51 项。
  - npm run test:backup:integration 通过真实 MySQL + HTTP 验收，覆盖资源副本和永久删除。
  - npm run test:data-governance:integration、npm run typecheck、npm run build、npm run db:check、npm run storage:check 通过。
  - Playwright 桌面与 390x844 移动端验证备份 Sheet、成功备份和恢复前确认；bodyWidth/documentWidth=375，控制台无错误。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/package.json`、`apps/api/src/app.ts`、`apps/api/src/data-governance-service.test.ts`、`apps/api/src/data-governance-service.ts`、`apps/api/src/server.ts`、`apps/api/test/backup-real-mysql-http.test.ts`、`apps/api/test/data-governance-http.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`package.json`、`output/playwright/ph4-04-backups-desktop.png`、`output/playwright/ph4-04-backups-mobile.png`
- 下一步：进入 PH4-05，验收 M4 AI 与数据治理主链路；本步不提前实现账号登录或公网发布。

### `LOG-026` 2026-08-11 - 修复本机 AI Provider 加密配置

- 任务：`PH4-06`
- 阶段/里程碑：第四阶段 / M4
- 动作：本机随机加密口令配置/API 重启/真实 HTTP 新增清理/Provider 集成验收/进度同步
- 结果：当前开发机已配置 AI_PROVIDER_KEY_ENCRYPTION_SECRET。API 重启后新增 Provider 请求返回 201 且只返回密钥存在状态，临时夹具已清理；未配置口令时的安全阻断保持有效。
- 验证：
  - 配置冒烟确认读取到长度 64 的口令且未输出口令内容。
  - npm run test:ai-provider:integration 通过 1 项真实 MySQL + HTTP 验收。
  - 真实运行 API 新增 Provider 返回 201，hasApiKey=true，随后删除临时配置。
  - npm run progress:check 通过；全量测试、类型检查和构建受 PH4-04 既有编译阻塞影响。
- 影响文件：`.env`、`progress/tasks/phase-4.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：继续 PH4-04，补齐备份与回收站永久删除实现后再进行 PH4-05 M4 全链路验收。

### `LOG-025` 2026-08-11 - 完成 Markdown 内容导出与 JSON 完整恢复

- 任务：`PH4-03`
- 阶段/里程碑：第四阶段 / M4
- 动作：数据治理服务/结构化 Markdown 序列化/GFM 表格与公式/JSON 资源与学习状态/敏感字段校验/事务恢复/资料页下载与确认 Sheet/HTTP、单元和浏览器验收
- 结果：已完成活动资料 Markdown 导出和完整学习状态 JSON 导出/恢复。JSON 保留正文、掌握状态、复习记录、高亮、当前 AI 讲解、回收站、资源元数据和资源内容，不导出 AI Provider 配置及密钥；恢复前校验版本、层级关系、正文节点、资源路径和哈希，事务替换可恢复数据。资料页已提供 Markdown 导出、JSON 导出和带确认的 JSON 恢复入口。
- 验证：
  - npm run typecheck、npm test（49 项）、npm run build 和 npm run test:data-governance:integration 均通过。
  - Playwright 隔离服务验证 Markdown 和 JSON 下载成功；JSON 实测约 4.1MB，包含 2 份资料、603 张闪卡、3 条高亮和 116 条复习记录，敏感字段检查未命中。
  - Playwright 桌面及 390x844 移动视口验证资料页导出/恢复入口、恢复确认 Sheet、bodyWidth/documentWidth 无横向溢出，浏览器无控制台错误。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/package.json`、`apps/api/src/app.ts`、`apps/api/src/data-governance-service.ts`、`apps/api/src/data-governance-service.test.ts`、`apps/api/test/data-governance-http.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`package.json`、`package-lock.json`、`output/playwright/ph4-03-materials-desktop.png`、`output/playwright/ph4-03-materials-mobile.png`
- 下一步：进入 PH4-04，实现每日备份、恢复和回收站永久删除；本步不扩展账号登录或公网发布。

### `LOG-024` 2026-08-11 - 完成卡片 AI 讲解与当前结果保存

- 任务：`PH4-02`
- 阶段/里程碑：第四阶段 / M4
- 动作：OpenAI 兼容请求适配/密钥解密/连接测试/默认与临时提示词/当前结果覆盖/卡片折叠区/HTTP 与浏览器验收/进度同步
- 结果：卡片页现可使用设置页当前启用 Provider 生成 AI 讲解，设置页也可对单个 Provider 发起最小连接测试。系统默认提示词与仅本次生效的临时提示词会组成请求，后端解密 API Key 并将当前讲解、Provider、模型、提示词和生成时间保存到 ai_explanations；重新生成覆盖旧结果。讲解结果默认收起且只读，生成失败不会改变正文、标注或复习状态。
- 验证：
  - npm test 通过 45 项 API 与服务单元测试，覆盖提示词、密钥解密、Provider 请求、错误隔离和覆盖保存。
  - npm run test:ai-explanation:integration 通过 2 项 HTTP 契约验收，覆盖卡片讲解和 Provider 连接测试。
  - npm run typecheck、npm run build 通过。
  - Playwright 使用本地拦截 API 验证桌面与 390x844 移动端折叠、只读、重新生成覆盖和无横向溢出。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/ai-provider-service.ts`、`apps/api/src/ai-explanation-service.ts`、`apps/api/src/ai-explanation-service.test.ts`、`apps/api/src/review-service.ts`、`apps/api/src/app.ts`、`apps/api/test/ai-explanation-http.test.ts`、`apps/api/package.json`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`package.json`、`output/playwright/ph4-02-card-mobile.png`、`progress/config.json`、`progress/tasks/phase-4.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：进入 PH4-03，实现 Markdown 内容导出与 JSON 完整恢复；本步不扩展每日备份或回收站永久删除。

### `LOG-023` 2026-08-11 - 完成多 Provider 与模型配置

- 任务：`PH4-01`
- 阶段/里程碑：第四阶段 / M4
- 动作：加密配置服务/API 契约/启用项事务锁/iOS 设置页/真实 MySQL HTTP 验收/桌面与移动浏览器回归
- 结果：设置页现可维护 OpenAI、DeepSeek、OpenRouter 或自定义 OpenAI 兼容配置，并切换当前启用项。API Key 只在后端经 AES-256-GCM 加密后保存，任何读取接口仅返回密钥存在状态。未配置本机加密口令时，保存会被安全阻止且不会写入数据库。
- 验证：
  - npm run test:ai-provider:integration 通过 1 项真实 MySQL + HTTP 验收，随机命名夹具在 finally 中清理。
  - npm test 通过 43 项单元测试；npm run typecheck 和 npm run build 通过。
  - Playwright 验证桌面设置页、移动端 390x844 设置页和新增表单；documentWidth=390、bodyWidth=390，浏览器无控制台错误。
- 影响文件：`.env.example`、`package.json`、`packages/shared/src/api.ts`、`apps/api/src/ai-provider-service.ts`、`apps/api/test/ai-provider-real-mysql-http.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/config.json`、`progress/tasks/phase-4.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：进入 PH4-02，实现 Provider 请求适配、连接测试、卡片 AI 讲解和当前结果保存；本步不得提前实现导出、恢复或备份。

### `LOG-022` 2026-08-11 - 完成多设备正文编辑锁并达成 M3

- 任务：`PH3-05`
- 阶段/里程碑：第三阶段 / M3
- 动作：编辑租约 API/保存事务校验/浏览器设备标识与续期/真实 MySQL 双客户端验收/桌面与移动浏览器回归
- 结果：闪卡正文编辑现由后端 90 秒租约保护。首个设备进入编辑后，其他设备仍能阅读和复习，但无法进入同一闪卡正文编辑；保存、取消、返回和切卡会释放租约，过期后可由另一设备接管。第三阶段全部任务已达到验收标准，M3 标注与编辑主链路完成。
- 验证：
  - npm run test:edit-lock:integration 通过真实 MySQL + HTTP 验收，覆盖竞争、保存拦截、续期、释放和过期接管；夹具已清理。
  - npm test（40 项）、npm run test:m3:integration、npm run test:review:integration、npm run typecheck、npm run build、npm run db:check 和 npm run storage:check 通过。
  - Playwright 桌面与 390x844 移动会话验证首端编辑、第二端冲突提示、首端释放后的移动端接管，且无横向溢出。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/review-service.ts`、`apps/api/test/edit-lock-real-mysql-http.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`output/playwright/ph3-05-desktop.png`、`output/playwright/ph3-05-mobile.png`、`progress/config.json`、`progress/tasks/phase-3.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：进入 PH4-01，实现多 Provider 与模型配置；本步不得将 API Key 下发浏览器。

### `LOG-021` 2026-08-11 - 完成 M3 已交付主链路组合回归

- 任务：`PH3-06`
- 阶段/里程碑：第三阶段 / M3
- 动作：真实 MySQL + HTTP 组合验收/双客户端读取一致性/高亮与编辑回归/层级软删除隔离/全量构建检查
- 结果：新增 PH3-06 隔离集成测试，将第三阶段已经完成的标注、编辑和层级能力串联验证。测试确认正文编辑仅清理会漂移的高亮，并让两个独立客户端读取到一致的持久化内容和高亮；层级操作与回收站隔离正常。挖空行为沿用 PH3-02 的浏览器验收结果。正文编辑锁未提前实现。
- 验证：
  - npm run test:m3:integration 通过 1 项真实 MySQL + HTTP 组合回归，随机 UUID 夹具已清理。
  - npm run typecheck、npm test（40 项）、npm run build、npm run db:check 和 npm run storage:check 通过。
  - PH3-02 已有的真实 MySQL + Playwright 桌面和 390x844 移动端挖空验收继续有效。
- 影响文件：`package.json`、`apps/api/package.json`、`apps/api/test/m3-real-mysql-http.test.ts`、`progress/tasks/phase-3.json`、`progress/logs.json`
- 下一步：进入 PH3-05，实现多设备同步和闪卡正文编辑锁；本步不扩展回收站恢复、永久删除、导出或备份。

### `LOG-020` 2026-08-11 - 完成层级维护和回收站入口

- 任务：`PH3-04`
- 阶段/里程碑：第三阶段 / M3
- 动作：层级维护服务与事务/API 契约/资料树/软删除回收站/真实 MySQL HTTP 验收/桌面与移动浏览器回归
- 结果：资料页现可维护章、节和闪卡：新增、改名、移动和同层排序即时持久化；软删除会级联隐藏下级内容并逐项写入回收站。资料树与复习查询只返回活动内容，回收站保持只读。为适配大资料，闪卡默认按节收起，展开后才显示卡片级操作。
- 验证：
  - npm run typecheck、npm test（40 项）、npm run build、npm run db:check 和 npm run storage:check 通过。
  - npm run test:hierarchy:integration 通过 1 项真实 MySQL + HTTP 验收，覆盖新增、重命名、排序、跨章移动、级联软删除和回收站记录读取；随机 UUID 夹具已清理。
  - Playwright 桌面与 390x844 移动端验证资料页、按节展开、删除确认与回收站入口；移动端 bodyWidth=390、documentWidth=390，展开 29 张卡后无横向溢出，浏览器无控制台错误。
- 影响文件：`package.json`、`apps/api/package.json`、`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/hierarchy-service.ts`、`apps/api/src/hierarchy-service.test.ts`、`apps/api/test/hierarchy-real-mysql-http.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`output/playwright/ph3-04-materials-desktop.png`、`output/playwright/ph3-04-materials-collapsed-mobile.png`、`progress/config.json`、`progress/tasks/phase-3.json`、`progress/logs.json`
- 下一步：进入 PH3-05，实现多设备同步和闪卡正文编辑锁；本步不扩展回收站恢复或永久删除。

### `LOG-019` 2026-08-11 - 完成结构化可视化卡片编辑

- 任务：`PH3-03`
- 阶段/里程碑：第三阶段 / M3
- 动作：结构化编辑 API/受管图片上传/MathLive 公式字段/高亮失效处理/真实 MySQL 集成验收/桌面与移动浏览器回归
- 结果：用户可在闪卡阅读页原位编辑标题、正文和表格单元格，新增段落、表格、公式或受管图片；公式使用排版数学字段，未出现 Markdown 或 LaTeX 源码编辑态。内容保存以事务同步正文和高亮，未改变节点的高亮保留，可能漂移或损坏的锚点删除并向用户反馈。
- 验证：
  - npm run typecheck、npm test（36 项）、npm run test:review:integration、npm run build、npm run db:check 和 npm run storage:check 通过。
  - 真实 MySQL + HTTP 验证结构化正文、表格、公式和受管图片引用的保存与重读；验收夹具和上次位置已清理恢复。
  - Playwright 桌面及 390x844 移动端验证编辑入口、表格、公式字段和取消草稿；移动端无横向溢出，浏览器无控制台错误。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/review-service.ts`、`apps/api/src/resource-service.ts`、`apps/api/test/review-real-mysql-http.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`apps/web/src/main.tsx`、`apps/web/package.json`、`package-lock.json`、`output/playwright/ph3-03-desktop.png`、`output/playwright/ph3-03-mobile.png`、`progress/config.json`、`progress/tasks/phase-3.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：进入 PH3-04，实现章、节、闪卡维护和回收站入口；本步不实现多设备编辑锁或协同冲突处理。

### `LOG-018` 2026-08-11 - 完成重点挖空与揭示重置

- 任务：`PH3-02`
- 阶段/里程碑：第三阶段 / M3
- 动作：阅读态遮罩渲染/逐项揭示与显示全部/切卡重置/桌面和移动浏览器验收/隔离夹具清理
- 结果：复习阅读页现可切换挖空模式：文本和表格文字以原文字占位长度显示灰色可点击遮罩，已高亮公式以完整 KaTeX 布局遮罩。揭示状态只存在当前页面内存；连续切卡时保留挖空模式但重置每张卡的已揭示项，退出闪卡页会关闭模式。
- 验证：
  - npm test 通过 32 项测试。
  - npm run typecheck 和 npm run build 通过。
  - Playwright 在真实 MySQL 两卡夹具中验证文本、表格、公式、逐项揭示、显示全部和切卡重置。
  - 390x844 视口 bodyWidth=390、tableWidth=324、tableContainerWidth=324，无横向溢出；桌面和移动截图已人工检查。
  - 夹具的 2 张卡、4 条高亮已事务清理，fixtureCount=0，原上次闪卡设置已恢复。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`output/playwright/ph3-02-desktop.png`、`output/playwright/ph3-02-mobile.png`、`progress/config.json`、`progress/tasks/phase-3.json`、`progress/logs.json`
- 下一步：进入 PH3-03，实现正文、表格、公式和图片的结构化可视化编辑；本步必须同时设计编辑后的高亮迁移或失效处理。

### `LOG-017` 2026-08-10 - 完成文本、表格和公式重点高亮

- 任务：`PH3-01`
- 阶段/里程碑：第三阶段 / M3
- 动作：结构化锚点契约/高亮读写 API/阅读态标记/公式整段切换/服务与 HTTP 测试/浏览器验收
- 结果：已在阅读态建立可持久化的重点高亮。文本和表格文字以内容节点路径与节点内字符范围保存，跨多个富文本或表格节点的选区会拆为多个高亮即时写入；公式按完整节点标记或取消。服务端校验卡片归属、节点类型和范围，图片及无效位置无法被标记。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 32 项测试，覆盖高亮校验、去重、公式限制、删除和 HTTP 路由。
  - npm run build 通过。
  - 真实 MySQL + Playwright 验证表格选词即时保存与阅读态渲染，验收生成的一条测试高亮已精确删除。
  - Playwright 390x844 复测跨两个表格节点的选区；bodyWidth=390、tableWidth=324，无横向溢出，桌面和移动截图已人工检查。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/api/src/health.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`output/playwright/ph3-01-desktop.png`、`output/playwright/ph3-01-mobile.png`、`progress/config.json`、`progress/tasks/phase-3.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：进入 PH3-02，实现基于现有高亮的重点挖空与离开卡片后的揭示重置；本步不实现正文编辑或层级维护。

### `LOG-016` 2026-08-10 - 完成 M2 连续复习主链路验收

- 任务：`PH2-05`
- 阶段/里程碑：第二阶段 / M2
- 动作：真实 MySQL 隔离夹具/HTTP 集成验收/Playwright 桌面与移动端回归/用户设置恢复/进度同步
- 结果：M2 复习主链路已闭环：用户可从面板进入，以资料、章、节、状态和关键词组合筛选，阅读安全富内容，保存掌握状态并在结果集内连续切换；末张卡片不会循环。真实数据库验收夹具仅使用随机 UUID 数据，结束后清理并恢复用户原先的上次闪卡设置。
- 验证：
  - npm run test:review:integration 通过 1 项真实 MySQL + HTTP 测试。
  - Playwright 验证桌面筛选、1 / 2 到 2 / 2 连续切换和两端按钮边界；390x844 视口 bodyWidth=390，无横向溢出。
  - npm run typecheck、npm test（30 项）、npm run build、npm run db:check、npm run storage:check 和 npm run progress:check 通过。
- 影响文件：`apps/api/test/review-real-mysql-http.test.ts`、`apps/api/package.json`、`package.json`、`output/playwright/ph2-05-desktop.png`、`output/playwright/ph2-05-mobile.png`、`progress/config.json`、`progress/tasks/phase-2.json`、`progress/logs.json`
- 下一步：进入 PH3-01，实现普通文本与表格文字高亮及其即时保存；本步不实现挖空、公式遮罩或可视化编辑。

### `LOG-015` 2026-08-10 - 完成正文、表格、公式、代码和图片安全渲染

- 任务：`PH2-04`
- 阶段/里程碑：第二阶段 / M2
- 动作：富内容契约/节点白名单/KaTeX 渲染/资源受限路由/响应式排版/安全与页面验收
- 结果：复习卡片已从纯文本升级为安全的结构化阅读态。API 仅传递允许节点和安全属性；原始 HTML、未知节点、javascript 链接和资源路径穿越均被拒绝。图片通过资源 UUID 读取，公式使用 KaTeX 渲染。用户明确要求表格不允许滚动后，移动端改为固定布局和自动换行，页面与表格均无横向滚动。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 30 项测试，覆盖富内容过滤、资源服务和 HTTP 路由。
  - npm run build 通过。
  - 真实 MySQL + HTTP 筛选并打开包含表格、引用和代码的语法闪卡，阅读态正确显示。
  - Playwright 桌面及 390x844 移动视口截图通过；移动端 bodyWidth=390，表格及其容器均为 324px，无横向滚动。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/review-service.ts`、`apps/api/src/resource-service.ts`、`apps/api/src/app.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-2.json`、`progress/config.json`、`progress/logs.json`
- 下一步：进入 PH2-05，验收从面板、筛选、富内容阅读、状态保存到结果集连续切换的完整 M2 主链路。

### `LOG-014` 2026-08-10 - 完成掌握状态、进度和复习记录

- 任务：`PH2-03`
- 阶段/里程碑：第二阶段 / M2
- 动作：状态更新 API/复习记录持久化/三段式控件/自动下一张/自动化与真实数据库验收
- 结果：已完成掌握、了解、努力的即时保存，打开或切换闪卡会写入首次查看、最近查看和查看次数。保存状态后自动进入当前结果集的下一张，最后一张不循环；面板统计与筛选结果会同步刷新。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 26 项测试，覆盖状态更新、查看记录、异常输入和 HTTP 路由。
  - npm run build 通过。
  - 真实 MySQL 事务验收确认状态及复习记录字段正确写入，并已回滚验收数据。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/health.test.ts`、`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-2.json`、`progress/logs.json`
- 下一步：进入 PH2-04，实现正文、表格、公式、代码和图片安全渲染；本步不提前实现高亮挖空。

### `LOG-013` 2026-08-10 - 完成组合筛选、搜索和顺序切卡

- 任务：`PH2-02`
- 阶段/里程碑：第二阶段 / M2
- 动作：筛选契约/层级筛选 API/稳定结果集/Sheet 交互/前后切卡/移动端溢出修正/自动化验收
- 结果：已建立以资料、章、节、掌握状态和关键词为条件的组合筛选。后端按资料层级与排序字段返回稳定结果集，前端在 iOS 风格 Sheet 中联动选择条件，并在卡片页显示位置和前后切换；边界卡片禁用对应方向。移动端长路径经过截断，不再产生横向滚动。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 22 项测试，新增组合筛选、当前结果位置和无效状态测试。
  - npm run build 通过。
  - 真实 MySQL API 组合资料、章、节、未评估和关键词后返回 1 张匹配卡片。
  - Playwright 验证筛选联动、1 / 1 边界、1 / 29 到 2 / 29 顺序切卡以及 390x844 移动视口无横向溢出。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-2.json`、`progress/logs.json`
- 下一步：进入 PH2-03，实现掌握状态、进度和复习记录；本步不提前实现富内容渲染或高亮挖空。

### `LOG-012` 2026-08-10 - 完成复习面板与继续上次闪卡

- 任务：`PH2-01`
- 阶段/里程碑：第二阶段 / M2
- 动作：复习查询 API/上次位置持久化/默认复习首页/快捷入口/导入导航保留/响应式验收
- 结果：复习首页已成为默认入口，展示真实资料数量、闪卡数量和状态数量；全部、未评估、努力和选章节可打开各自的首张闪卡，最近打开卡片通过 app_settings 持久化并在返回面板后显示。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 20 项 API 测试。
  - npm run build 通过。
  - 真实数据库页面显示 1 份资料、99 张闪卡、99 张未评估；点击全部打开首张闪卡，返回后上次闪卡出现。
  - Playwright 桌面和 390x844 移动视口快照通过，导入页入口可切换。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-2.json`、`progress/config.json`、`progress/logs.json`
- 下一步：进入 PH2-02，实现资料、章、节、状态和关键词组合筛选与结果集内顺序切卡。

### `LOG-011` 2026-08-10 - 完成 M1 资料导入主链路验收

- 任务：`PH1-06`
- 阶段/里程碑：第一阶段 / M1
- 动作：真实 MySQL/HTTP 集成验收/浏览器文件选择/非法输入/重复跳过/源文件不变/进程内令牌失效/进度同步
- 结果：真实 MySQL 与 HTTP 导入闭环通过：合法 Markdown 和含图片 ZIP 完整入库，非法层级、缺图和路径穿越在预览阶段阻止，重复源文件跳过且无新增数据；浏览器导入页的预览、应用、错误定位和取消流程通过。验收夹具只使用随机源 SHA、临时资源目录，并在结束时清理本次资料。
- 验证：
  - npm run test:import:integration 通过 1 项真实数据库集成测试。
  - Playwright 实测合法资料应用成功、非法资料错误定位与应用禁用、取消回到空状态。
  - npm run typecheck、npm test（17 项）、npm run build、npm run db:check、npm run storage:check 和 npm run progress:check 均通过。
- 影响文件：`apps/api/test/import-real-mysql-http.test.ts`、`apps/api/package.json`、`apps/web/src/App.tsx`、`package.json`、`progress/tasks/phase-1.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：进入 PH2-01，实现复习面板与继续上次闪卡；本步不提前实现组合筛选、掌握状态、高亮或 AI。

### `LOG-010` 2026-08-10 - 补充 Windows 双击启停入口

- 任务：`PH1-03`
- 阶段/里程碑：第一阶段 / M1
- 动作：CMD 包装/Windows PowerShell 5 UTF-8 BOM 兼容/双击流程回归/进度同步
- 结果：已新增启动应用.cmd 和停止应用.cmd，用户可双击启动或停止应用，无需手工打开终端。启动完成后窗口保留访问状态，关闭窗口不会停止后台服务；错误时保留窗口和日志路径。
- 验证：
  - Windows PowerShell 5 可正确解析带 UTF-8 BOM 的 start.ps1 和 stop.ps1。
  - 通过启动应用.cmd 以自定义空闲端口启动，API 与 Web 均返回 200。
  - 通过停止应用.cmd 停止受管应用进程，MySQL 保持运行。
- 影响文件：`启动应用.cmd`、`停止应用.cmd`、`start.ps1`、`stop.ps1`、`progress/tasks/phase-1.json`、`progress/logs.json`
- 下一步：继续 PH1-06，完成 M1 资料导入主链路验收。

### `LOG-009` 2026-08-10 - 完成导入预览、修正和数据库应用

- 任务：`PH1-05`
- 阶段/里程碑：第一阶段 / M1
- 动作：共享导入契约/预览令牌/事务写入/源文件去重/ZIP 资源托管/中文导入页面/自动化验证
- 结果：已完成单个 Markdown/ZIP 的选择、预览、错误定位、标题与正文修正、应用和取消闭环；合法资料写入 MySQL 四级层级，ZIP 资源复制到按资料 ID 隔离的受管目录，同源文件默认按 SHA-256 跳过。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 17 项测试，覆盖事务应用、重复跳过、资源清理和解析边界。
  - npm run build 通过。
  - 本地 API 烟测验证健康检查、合法 Markdown 预览和取消预览。
  - 本地 Web DOM 快照验证导入页面和选择文件入口；Playwright 截图因文件选择器模态状态未执行。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/import-service.ts`、`apps/api/src/import-service.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`apps/web/index.html`、`progress/tasks/phase-1.json`、`progress/risks.json`
- 下一步：进入 PH1-06，使用合法和非法样本完成 M1 资料导入主链路验收；重点验证真实 MySQL、源文件不变、资源完整和重复导入行为。

### `LOG-008` 2026-08-10 - 完成单 Markdown/ZIP 解析与严格校验

- 任务：`PH1-04`
- 阶段/里程碑：第一阶段 / M1
- 动作：unified/remark/GFM/KaTeX 解析/层级校验/图片资源校验/ZIP 路径安全/自动化测试
- 结果：解析器已输出资料、章、节、闪卡和结构化正文节点；严格识别标题层级、未归属内容、公式、表格、图片和 ZIP 路径错误，并返回可供后续导入应用的资源字节。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 13 项测试。
  - npm run build 通过。
- 影响文件：`apps/api/package.json`、`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`package-lock.json`、`progress/tasks/phase-1.json`、`progress/config.json`、`progress/logs.json`
- 下一步：进入 PH1-05，实现导入预览、修正和数据库应用；本步解析结果作为预览输入，不在解析器中写入数据库。

### `LOG-007` 2026-08-10 - 完成 Windows 启停脚本与受控 Tunnel 集成

- 任务：`PH1-03`
- 阶段/里程碑：第一阶段 / M1
- 动作：启停脚本/进程托管/端口预检/健康检查/Cloudflare Tunnel 门禁/进度同步
- 结果：已建立 start.ps1 和 stop.ps1。启动前检查 MySQL、三类存储路径和共享类型，启动后端与 Web 并记录进程和日志；停止只处理本项目受管进程树。脚本包含 cloudflared tunnel 的配置化启停，但默认关闭，未完成单账号密码保护时强制拒绝公网 Tunnel。
- 验证：
  - start.ps1 和 stop.ps1 PowerShell 语法解析通过。
  - 自定义空闲端口实测 API/Web 均返回 200，stop.ps1 成功停止应用进程树且未停止 MySQL。
  - 默认端口冲突会在启动前报告未受管进程号，不会误杀其他进程。
  - CLOUDFLARED_ENABLED=true 且 PUBLIC_ACCESS_READY=false 时拒绝启动 Tunnel。
  - npm run typecheck、npm test（4 项）和 npm run build 均通过。
- 影响文件：`start.ps1`、`stop.ps1`、`.env.example`、`.gitignore`、`apps/web/vite.config.ts`、`progress/tasks/phase-1.json`、`progress/risks.json`
- 下一步：进入 PH1-04，实现单 Markdown/ZIP 解析与严格校验；本步不实现导入预览和数据库应用。

### `LOG-006` 2026-08-10 - 完成 MySQL 数据模型、迁移和路径配置验收

- 任务：`PH1-02`
- 阶段/里程碑：第一阶段 / M1
- 动作：本机数据库创建/应用账号授权/索引兼容修正/真实迁移/重复迁移验收
- 结果：已创建 knowledge_flashcards 数据库和独立应用账号；修正 utf8mb4 唯一索引长度后，首次迁移和重复迁移均通过。
- 验证：
  - npm run db:check 通过。
  - npm run db:migrate 首次执行成功。
  - npm run db:migrate 第二次执行跳过 001_initial_schema.sql。
  - 数据库包含 14 张表，schema_migrations 已记录 001_initial_schema.sql。
- 影响文件：`database/migrations/001_initial_schema.sql`、`apps/api/src/migration.test.ts`、`progress/tasks/phase-1.json`、`progress/risks.json`
- 下一步：进入 PH1-03，建立 Windows 一键启动与关闭脚本。

### `LOG-005` 2026-08-10 - 建立 MySQL 数据模型、迁移和路径配置

- 任务：`PH1-02`
- 阶段/里程碑：第一阶段 / M1
- 动作：数据库连接池/初始迁移/核心表结构/路径读写探针/自动化测试
- 结果：已建立 001_initial_schema.sql、迁移版本校验、数据库连接检查和数据/资源/备份路径检查；代码与自动化测试已通过，真实迁移等待本机独立数据库账号凭证。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过 4 个测试。
  - npm run build 通过。
  - npm run storage:check 通过。
  - 3306 端口正在监听，但默认应用账号和 root 均未提供可用密码；npm run db:check 和 npm run db:migrate 均在连接阶段返回 ER_ACCESS_DENIED_ERROR，未执行建表语句。
- 影响文件：`apps/api/src/config.ts`、`apps/api/src/database.ts`、`apps/api/src/migrate.ts`、`apps/api/src/storage.ts`、`database/migrations/001_initial_schema.sql`、`package.json`、`package-lock.json`、`.env.example`
- 下一步：配置本机独立 MySQL 数据库和应用账号后执行 npm run db:migrate，确认 schema_migrations 与 13 张核心表创建成功；随后进入 PH1-03。

### `LOG-004` 2026-08-10 - 完成 React + Node.js + TypeScript 工程骨架

- 任务：`PH1-01`
- 阶段/里程碑：第一阶段 / M1
- 动作：workspace/共享类型/Web/API/环境配置/健康检查/日志/错误处理/最小测试
- 结果：已完成可安装、可启动、可类型检查和可构建的 npm workspace。API 仅监听本机回环地址并提供健康检查，Web 提供简体中文工程状态页，前后端通过共享类型包保持契约一致。
- 验证：
  - npm install 安装成功，审计结果为 0 vulnerabilities。
  - npm run typecheck 通过。
  - npm test 通过 2 个 API 测试，覆盖健康检查和统一 404 响应。
  - npm run build 通过，Vite 和 TypeScript API 构建成功。
  - npm run dev 启动后，http://127.0.0.1:8787/api/health、http://localhost:5173/ 和 Web 的 /api/health 代理均返回 200。
- 影响文件：`package.json`、`package-lock.json`、`.gitignore`、`.env.example`、`tsconfig.base.json`、`scripts/dev.mjs`、`packages/shared/package.json`、`packages/shared/tsconfig.json`、`packages/shared/src/api.ts`、`packages/shared/src/index.ts`、`apps/api/`、`apps/web/`
- 下一步：进入 PH1-02，建立 MySQL 数据模型、迁移和路径配置；本步不改变数据库范围。

### `LOG-003` 2026-08-10 - 完成 iOS 风格前端设计规范收口

- 任务：`GOV-002`
- 阶段/里程碑：第零阶段 / M0
- 动作：文件名纠正/iOS 视觉系统/页面规范/控件规范/文案压缩/进度同步
- 结果：已将前端规范文件统一为 DESIGN.md，并补齐 iOS 式页面层级、系统色、排版、分组列表、Sheet、Tab Bar、分段控件、触控尺寸、iPhone 安全区域和响应式规则；同时建立主界面无说明段落、按钮原则上不超过 4 个汉字的硬约束。
- 验证：
  - DESIGN.md 已提供标准按钮词表和禁止文案清单。
  - DESIGN.md 已覆盖复习面板、闪卡、筛选、导入、资料管理和设置页面。
  - AGENTS.md 与 progress/config.json 已改为引用 DESIGN.md。
  - DSEIGN.md 已移除，避免存在两个设计规范真相源。
- 影响文件：`DESIGN.md`、`DSEIGN.md（已移除）`、`AGENTS.md`、`progress/config.json`、`progress/tasks/phase-0.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：PH1-01 及后续前端任务必须以 DESIGN.md 作为唯一前端设计基线。

### `LOG-002` 2026-08-10 - 建立 layout3.0 风格结构化进度系统

- 任务：`GOV-001`
- 阶段/里程碑：第零阶段 / M0
- 动作：Agent 规则/进度真相源/阶段任务/风险技术债/生成校验脚本
- 结果：已建立 progress/config.json、分阶段任务卡、风险、技术债和日志作为唯一真相源，并通过脚本生成项目开发进度与当前执行看板。
- 验证：
  - AGENTS.md 已规定任务边界、状态、日志和自动生成规则。
  - npm run progress:build 可生成两份展示文档。
  - npm run progress:check 可验证展示层与 JSON 真相源一致。
- 影响文件：`AGENTS.md`、`package.json`、`progress/config.json`、`progress/tasks/*.json`、`progress/risks.json`、`progress/tech-debts.json`、`progress/logs.json`、`scripts/generate-progress.mjs`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：开始 PH1-01 前再次确认具体工程目录和基础依赖，确认后先更新执行边界再改代码。

### `LOG-001` 2026-08-10 - 完成产品、开发与设计基线

- 任务：`GOV-001`
- 阶段/里程碑：第零阶段 / M0
- 动作：需求收敛/产品定位/技术方案/交互设计/范围冻结
- 结果：已将逐项确认的产品决策落实为项目产品定位、项目开发方案和 DESIGN 设计规范，明确 Markdown 一次性导入、MySQL 主数据、可视化编辑、手动掌握状态、高亮挖空、多 Provider AI、Windows Web 应用和 Cloudflare Tunnel 等边界。
- 验证：
  - 项目产品定位.md 已覆盖目标用户、核心价值、成功标准和第一版非目标。
  - 项目开发方案.md 已覆盖架构、数据边界、导入、复习、AI、备份、部署和测试策略。
  - DESIGN.md 已覆盖桌面/iPhone 布局、主题、字体、高亮、挖空和可视化编辑规则。
- 影响文件：`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`
- 下一步：按 PH1-01 已确认边界建立 TypeScript Web 工程骨架。

### `LOG-081` 2026-08-13 - 兼容安全 HTML 换行与表格导入

- 任务：`PH5-25`
- 阶段/里程碑：第五阶段 / M5、GA
- 动作：引入 parse5 白名单转换/扩展共享表格节点表头契约/补充 API 回归与构建检查
- 结果：HTML br、p/div 和 table/thead/tbody/tfoot/tr/th/td 会转换为结构化正文；显式表头和单元格对齐信息在复习页保留。脚本、样式、事件属性、链接和其他未支持 HTML 继续阻止导入。
- 验证：
  - npm --workspace @knowledge-flashcards/api test 通过，101 项测试全部通过。
  - npm run typecheck 通过。
  - npm run build:web 与 npm run build:api 通过；Web 仅有既有 Vite 大 chunk 警告。
  - 内置浏览器仍因 Cannot redefine property: process 无法启动，页面回归待运行时恢复后补测。
- 影响文件：`apps/api/src/ingestion.ts`、`apps/api/src/ingestion.test.ts`、`apps/api/src/review-service.ts`、`apps/web/src/App.tsx`、`packages/shared/src/api.ts`、`apps/api/package.json`、`package-lock.json`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：先补做实际页面换行与表格回归；复杂 HTML 合并单元格、样式语义和更多标签另立任务。

### `LOG-082` 2026-08-13 - 扩展闪卡编辑表格操作

- 任务：`PH5-14`
- 阶段/里程碑：第五阶段 / M3、GA
- 动作：登记表格操作边界/实现空白单元格输入、按选中单元格加删行与横纵合并拆分/扩展跨度存储、阅读渲染和恢复校验/服务回归与构建/进度同步
- 结果：资料详情的闪卡编辑器中，表格单元格可直接输入；点击单元格后可在紧凑工具区加行、删行、横向合并、纵向合并或拆分。合并内容会保留在起始单元格并以 rowSpan/colSpan 保存，阅读态正确渲染；跨度进入服务端编辑校验和 JSON 恢复白名单。
- 验证：
  - npm --workspace @knowledge-flashcards/api test 通过，102 项测试全部通过，覆盖合并跨度写入和非法跨度拒绝。
  - npm run typecheck 通过。
  - npm run build 通过；Web 仅有既有 Vite 大 chunk 警告。
  - 内置浏览器连接仍因 Cannot redefine property: process 无法启动，桌面与 390px 实际交互回归待运行时恢复后补测。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`packages/shared/src/api.ts`、`apps/api/src/ingestion.ts`、`apps/api/src/review-service.ts`、`apps/api/src/review-service.test.ts`、`apps/api/src/data-governance-service.ts`、`DESIGN.md`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：待浏览器运行时恢复后，补做表格编辑、保存和阅读态的桌面与 390px 交互回归；Markdown 导出对合并单元格的保真策略另立任务。

### `LOG-083` 2026-08-13 - 修复空白表格单元格不可编辑

- 任务：`PH5-14`
- 阶段/里程碑：第五阶段 / M3、GA
- 动作：复现空单元格仅显示选中边框/为无子节点单元格渲染输入框/首字符写入 text 节点/类型检查、构建和进度校验
- 结果：空白表格单元格现在会显示可直接输入的文字框；输入内容后自动建立 text 子节点，后续编辑和保存沿用既有结构化正文链路。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；仅有既有 Vite 大 chunk 警告。
  - npm run progress:check 通过。
- 影响文件：`apps/web/src/App.tsx`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：用户可在闪卡编辑态重新点击空白单元格并输入内容；若发现特定合并跨度下的焦点问题，再单独补充交互回归。

### `LOG-084` 2026-08-19 - 冻结题库领域协议与阶段拆分

- 任务：`PH6-01`
- 阶段/里程碑：第六阶段 / M6
- 动作：逐项确认题库层级、导入模板、校验、背题/检测、统计、错题专项、可视化编辑、AI 历史和备份兼容；同步产品、开发、设计和结构化进度源。
- 结果：题库固定在课程→科目下的章节题、真题、模拟题三个入口。章节题由导入文件章节建树，真题和模拟题不设章节；题干、题型、连续选项和答案强制校验，解析/知识点可空。刷题、统计、错题、AI 版本和备份均已拆成独立可验收任务，题目与闪卡联动、图片、随机、部分得分和主观题评分明确不在本阶段。
- 验证：
  - 已将确认结论写入 项目产品定位.md、项目开发方案.md 和 DESIGN.md。
  - npm run progress:build 通过，生成展示层。
  - npm run progress:check 通过，进度真相源引用和状态一致。
- 影响文件：`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`、`progress/config.json`、`progress/tasks/phase-6.json`、`progress/risks.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：PH6-02 先建立题库独立数据模型、共享契约和备份恢复兼容，再开始题库导入或界面实施。

### `LOG-085` 2026-08-19 - 建立题库数据模型与备份恢复兼容

- 任务：`PH6-02`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增独立题库迁移与回收站枚举迁移/扩展共享契约/将完整 JSON 导出升级为 v2/兼容 v1 恢复/覆盖题目 AI 版本和作答快照恢复。
- 结果：题库数据不复用闪卡表。question_banks、question_chapters、questions、question_ai_explanations、practice_sessions 和 practice_attempts 已可持久化，题目版本和完整快照使后续编辑不篡改旧作答。完整备份 v2 包含题库数据，v1 闪卡备份仍可恢复，且所有路径继续拒绝敏感字段。
- 验证：
  - npm run typecheck 通过。
  - npm --workspace @knowledge-flashcards/api test 通过，105 项测试全部通过。
  - npm run db:migrate 在真实 MySQL 成功执行 005 和 006 迁移。
  - npm run test:data-governance:integration 通过。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`database/migrations/005_phase_6_question_bank.sql`、`database/migrations/006_phase_6_question_trash.sql`、`apps/api/src/migration.test.ts`、`apps/api/src/data-governance-service.ts`、`apps/api/src/data-governance-service.test.ts`、`packages/shared/src/api.ts`、`progress/tasks/phase-6.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：PH6-03 实现章节题、真题和模拟题的 JSON/Excel 模板、预览校验与写入接口。

### `LOG-086` 2026-08-19 - 实现题库 JSON/Excel 导入与模板

- 任务：`PH6-03`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增独立题库 JSON/Excel 模板、原始文件预览、取消和预览令牌应用接口；接入认证门禁、课程科目归属与重名校验；补充单元、HTTP 和真实 MySQL 回归。
- 结果：章节题可从 JSON chapters 或 Excel 章节列建立题库章节树，真题和模拟题以整套题库导入且无章节。题干、题型、选项和答案强制非空；选项限 A-F 连续的 2-6 项，判断题固定为 A: 对、B: 错，多选答案会标准化为字母数组。应用使用服务端预览令牌，在一个事务内写入题库、章节和题目，并阻止同科目同类型重名题库。
- 验证：
  - npm run typecheck 通过。
  - npm --workspace @knowledge-flashcards/api test 通过，110 项测试全部通过。
  - npm run test:question-import:integration 通过真实 MySQL 与 HTTP 导入回归。
  - npm run db:migrate 通过，题库迁移已保持应用状态。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/question-import-service.ts`、`apps/api/src/question-import-service.test.ts`、`apps/api/src/question-import-http.test.ts`、`apps/api/test/question-import-real-mysql-http.test.ts`、`apps/api/package.json`、`package.json`、`progress/tasks/phase-6.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：PH6-04 接入题库目录管理及导入页面入口；题目可视化编辑、刷题、统计和 AI 讲解继续分别由后续任务承担。

### `LOG-087` 2026-08-19 - 实现课程科目下的题库目录管理

- 任务：`PH6-04`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增独立题库目录服务、认证 API 和科目页题库入口；接入三类题库分组、题库与章节的目录操作、回收站恢复，以及带当前课程科目上下文的题库导入页。
- 结果：章节题、真题和模拟题在科目下固定分组。空题库可新建、改名、上下排序、软删除和恢复；删除题库会同时软删除其章节和题目。章节题支持新增、改名、上下排序、同科目章节题库间移动、删除约束和恢复，移动时同步题目归属。导入页复用 PH6-03 的 JSON/Excel 模板、预览、取消和原子应用 API。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，113 项 API 测试全部通过。
  - npm run test:question-bank:integration 通过真实 MySQL 与 HTTP 回归。
  - npm run build 通过。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/question-bank-service.ts`、`apps/api/src/question-bank-service.test.ts`、`apps/api/test/question-bank-real-mysql-http.test.ts`、`apps/api/package.json`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`package.json`、`progress/tasks/phase-6.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：PH6-05 实现题目可视化编辑及题目级新增、移动、排序和软删除；刷题、统计和题目 AI 继续分别由后续任务承担。

### `LOG-088` 2026-08-19 - 实现可视化题目编辑

- 任务：`PH6-05`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增题目服务、共享契约和认证 API；在题库页接入题目工作区、结构化内容编辑、题型答案控件、题库/章节移动、排序、软删除和恢复。
- 结果：题干、选项、解析和知识点均通过可视化节点编辑，选项限制 A-F 连续且最多六项，判断题固定为对/错；保存会严格校验答案并递增题目版本，题目可在同科目题库间移动。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，115 项 API 测试全部通过。
  - npm run build 通过。
  - npm run progress:build 与 npm run progress:check 通过。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/app.ts`、`apps/api/src/question-service.ts`、`apps/api/src/question-service.test.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：PH6-06 实现背题与检测会话；统计、错题专项和题目 AI 继续由 PH6-07、PH6-08 负责。

### `LOG-089` 2026-08-19 - 实现背题与检测会话

- 任务：`PH6-06`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增共享会话契约、PracticeService 和认证路由；持久化题目/版本快照与作答；接入题库范围选择、背题/检测面板、继续和放弃；补服务层及 HTTP 契约测试。
- 结果：题库支持整套或章节启动背题、检测。背题提交即时返回正确/错误与解析；检测在完成前不返回正确答案或解析，完成时计算已作答结果并揭示快照答案。未完成会话可继续或放弃，单选/判断自动前进，多选确认后前进。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，120 项 API 测试全部通过。
  - npm run build 通过；Web 构建仅保留既有大 chunk 警告。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/practice-service.ts`、`apps/api/src/practice-service.test.ts`、`apps/api/src/practice-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：PH6-07 实现完成会话统计、正确率和错题专项；PH6-08 实现题目 AI 讲解与版本历史。

### `LOG-090` 2026-08-19 - 实现题库统计与错题专项

- 任务：`PH6-07`
- 阶段/里程碑：第六阶段 / M6
- 动作：扩展完成会话结果摘要和错题来源；新增题库统计服务、统计认证路由和前端统计区；支持本次错题与累计错题会话；补统计、错题、结果和 HTTP 契约测试。
- 结果：已完成会话显示总题数、已作答、未作答、正确、错误和一位小数正确率。题库工作区展示总览、章节、题型、背题/检测累计统计和累计错题数。累计错题以每题最近一次已作答完成结果为准，答对后立即移出；未作答不会覆盖既有答题状态。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，122 项 API 测试全部通过。
  - npm run build 通过；Web 构建仅保留既有大 chunk 警告。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/practice-service.ts`、`apps/api/src/practice-service.test.ts`、`apps/api/src/practice-statistics-service.ts`、`apps/api/src/practice-statistics-service.test.ts`、`apps/api/src/practice-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：PH6-08 实现题目 AI 讲解与版本历史；真实 MySQL、备份恢复和桌面/390px 刷题整体验收留待 PH6-09。

### `LOG-091` 2026-08-19 - 实现题目 AI 讲解与版本历史

- 任务：`PH6-08`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增题目 AI 共享契约、Provider 生成与取消服务、历史版本查询和认证 HTTP 路由；在题目详情接入折叠讲解区、临时提示词、历史过时标记和安全渲染；补服务与 HTTP 测试。
- 结果：题目 AI 仅发送当前题干、选项、答案、解析、知识点和可选本次作答上下文，成功生成追加 question_ai_explanations 版本，取消或 Provider 错误不写入；题目版本变化后历史讲解显示可能过时且保留。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，126 项 API 测试全部通过。
  - npm run build 通过；Web 构建仅保留既有大 chunk 警告。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/question-ai-service.ts`、`apps/api/src/question-ai-service.test.ts`、`apps/api/src/question-ai-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：进入 PH6-09，执行真实 MySQL、备份恢复以及桌面/390px 浏览器端题库主链路验收。

### `LOG-092` 2026-08-19 - 完成题库主链路与备份恢复验收

- 任务：`PH6-09`
- 阶段/里程碑：第六阶段 / M6、GA
- 动作：串行运行题库导入、目录、数据治理和备份真实 MySQL/HTTP 回归；清理两条已确认无引用的历史夹具资源；启动隔离本地 API/Web，使用 Playwright 验证题库入口、检测结果、AI 历史区、桌面/390px 无横向溢出和控制台错误。
- 结果：真实 MySQL/HTTP 回归全部通过，备份验证题库与资源归档；临时题库夹具验证检测前隐藏答案、完成后显示结果和统计错题；桌面与 390px 页面无横向溢出且控制台无错误。真实 Provider 联网生成未执行，避免使用真实 API Key。
- 验证：
  - npm run test:question-import:integration 通过。
  - npm run test:question-bank:integration 通过。
  - npm run test:data-governance:integration 通过。
  - npm run test:backup:integration 通过。
  - npm run db:check、npm run storage:check 通过。
  - Playwright 1440x900 与 390x844 截图和宽度检查通过，控制台错误数为 0。
- 影响文件：`progress/tasks/phase-6.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`、`output/playwright/ph6-09-question-desktop.png`、`output/playwright/ph6-09-question-mobile.png`、`output/playwright/ph6-09-question-mobile-workspace.png`、`output/playwright/ph6-09-practice-mobile.png`
- 下一步：第六阶段主链路完成；真实 Provider 多模型兼容、公网 Tunnel 或新需求另立任务。

### `LOG-093` 2026-08-19 - 补充移动端 PWA 与选词回归证据

- 任务：`PH5-01`
- 阶段/里程碑：第五阶段 / GA
- 动作：在不读取本机账号凭据的前提下，使用终端 Playwright 旧本地服务执行 390x844 复习页回归，检查页面宽度、底部固定导航、安全区内边距、PWA 元数据，并拖选正文确认高亮操作状态。
- 结果：390x844 页面 bodyWidth/documentWidth 均为 390，底部导航为 fixed 且主内容保留底部内边距，Manifest、apple-mobile-web-app-capable 和 viewport-fit=cover 均生效；拖选正文后高亮按钮可用。真实 iPhone Safari 的主屏幕安装、原生长按浮动工具栏和 Home Indicator 仍待设备验收，因此 PH5-01 保持进行中。
- 验证：
  - Playwright viewportWidth=390、bodyWidth=390、documentWidth=390。
  - 底部导航 position=fixed、height=61px、padding-bottom=8px，主内容 padding-bottom=96px。
  - 正文拖选得到可识别文本，高亮按钮由 disabled 变为可用。
  - 截图保存为 output/playwright/ph5-01-mobile-20260819.png。
  - 内置浏览器仍因 Cannot redefine property: process 无法连接，未执行真实 iPhone Safari 验收。
- 影响文件：`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`output/playwright/ph5-01-mobile-20260819.png`
- 下一步：取得真实 iPhone Safari 或可用设备桥接后，完成添加到主屏幕、长按选词原生浮动工具栏和 Home Indicator 安全区验收；设备差异只在 PH5-01 范围内修正。

### `LOG-094` 2026-08-19 - 完成本机深浅色模式浏览器验收

- 任务：`PH5-08`
- 阶段/里程碑：第五阶段 / GA
- 动作：修复后的前端类型检查通过后，使用终端 Playwright 在设置页依次验证跟随系统、浅色和深色模式，检查本机持久化、简约与秋日枫叶皮肤 token、刷新恢复以及 390x844/1440x900 宽度。
- 结果：显示模式会写入 knowledge-flashcards-color-mode 并映射至 data-color-mode；深色刷新后仍保持选中。简约与秋日枫叶皮肤都即时使用各自的浅深 token，移动和桌面均未产生横向溢出，PH5-08 达到验收标准。
- 验证：
  - npm run typecheck:web 通过。
  - 390x844：深色模式 mode/stored=dark、documentWidth=390；刷新后深色 radio 仍选中。
  - 秋日枫叶深色背景/正文为 rgb(23, 21, 18)/rgb(243, 238, 232)，浅色模式写入 light，跟随系统写入 system。
  - 1440x900：documentWidth=1440；浏览器控制台仅有 React DevTools 开发提示。
- 影响文件：`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`output/playwright/ph5-08-mobile-autumn-dark-20260819.png`、`output/playwright/ph5-08-desktop-autumn-dark-20260819.png`
- 下一步：PH5-08 已完成；继续选择其余进行中的独立验收任务。

### `LOG-097` 2026-08-19 - 启动独立刷题页与题目解析入口改造

- 任务：`PH6-10`
- 阶段/里程碑：第六阶段 / M6、GA
- 动作：登记独立刷题页面、题目 AI 解析入口和背题手动下一题的边界；保留现有会话、判分、统计、错题与 AI 服务契约。
- 结果：任务进行中，尚未完成代码与浏览器验收。
- 验证：
  - 已在 PH6-10 任务卡登记执行边界，任务保持进行中。
- 影响文件：`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：先改造刷题页面渲染与交互，再运行类型检查、构建和响应式验收。

### `LOG-099` 2026-08-19 - 完成独立刷题页、AI 解析入口和紧凑选项交互实现

- 任务：`PH6-10`
- 阶段/里程碑：第六阶段 / M6、GA
- 动作：将题库与复习入口统一到独立 /review/practice/:sessionId 页面；移除刷题遮罩弹窗；增加题目 AI 解析历史/生成/停止入口；背题作答后改为等待用户点击下一题；将选项行压缩到 28px 级并修正序号垂直居中；运行类型检查、构建和未登录本地页面检查。
- 结果：代码实现和构建验证通过。终端浏览器被认证门禁挡在登录页，未执行真实题目会话的桌面/390px 验收，PH6-10 保持进行中。
- 验证：
  - npm run typecheck 通过。
  - npm run build:web 通过；仅保留 Vite 既有大 chunk 警告。
  - playwright-cli 打开 http://127.0.0.1:5173 成功，页面显示登录门禁；未读取或绕过账号密码。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/risks.json`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：取得合法登录态后补做独立刷题页真实交互和响应式验收。

### `LOG-100` 2026-08-19 - 压缩移动端底部导航高度

- 任务：`PH5-01`
- 阶段/里程碑：第五阶段 / GA
- 动作：在 640px 以下视口将底部导航上下内边距、导航项高度与字号压缩至原有约三分之一，并将主内容底部预留从 96px 同步收紧到 32px；不改桌面端、safe-area 语义或导航业务逻辑。
- 结果：移动端底部导航占用空间已显著降低，保留固定定位和安全区内边距；PH5-01 仍等待真实 iPhone Safari 的主屏幕安装、原生选词浮动工具栏和 Home Indicator 验收。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；Vite 仅报告既有大 chunk 警告。
  - npm run progress:check 通过；移动端 640px 以下规则将导航内边距、项目最小高度和字号压缩至原有约三分之一，并移除固定导航的额外底部间距。
  - 真实 iPhone Safari 添加到主屏幕、长按选词原生浮动工具栏和 Home Indicator 安全区仍未完成，PH5-01 保持进行中。
- 影响文件：`apps/web/src/styles.css`、`progress/tasks/phase-5.json`、`progress/logs.json`
- 下一步：完成移动端导航高度与无横向溢出回归；真实 iPhone Safari 验收仍归属 PH5-01。

### `LOG-101` 2026-08-20 - 取消题干 255 字符业务限制

- 任务：`PH6-11`
- 阶段/里程碑：第六阶段 / M6
- 动作：登记输入规模边界；将题干导入校验改为复用既有 100 万字符内容上限；补 JSON、Excel 与应用内保存服务回归；执行类型检查、全量 API 测试和进度同步。
- 结果：题干不再受 255 字符限制。JSON 和 Excel 导入可预览 256 字符题干，JSON 导入可事务应用；应用内题目保存也可通过 256 字符题干的输入校验。其他字段约束与题型校验未变。
- 验证：
  - 题库导入与题目保存定向测试 9 项全部通过。
  - npm run typecheck 通过。
  - npm test 通过，131 项 API 测试全部通过。
- 影响文件：`apps/api/src/question-import-service.ts`、`apps/api/src/question-import-service.test.ts`、`apps/api/src/question-service.test.ts`、`progress/risks.json`、`progress/tasks/phase-6.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：其他题库字段的长度策略继续维持现状；需要变更时单独拆分并验收。

### `LOG-102` 2026-08-20 - 优化刷题答案选择响应

- 任务：`PH6-10`
- 阶段/里程碑：第六阶段 / M6、GA
- 动作：登记快速作答边界；检测模式选项先本地更新并立即切题，答案后台保存并在失败时回滚；作答 API 改为按当前题返回增量结果，服务端避免读取和序列化整套会话题目；补充 HTTP、服务回归并执行类型检查和 Web 构建。
- 结果：答案选择不再等待整套会话回包；检测模式即时切题，背题仍等待判分反馈，答案校验、统计和完成规则保持不变。
- 验证：
  - npm run typecheck 通过。
  - npm run build:web 通过；仅保留 Vite 既有大 chunk 警告。
  - 刷题 HTTP、服务定向回归通过，作答响应不含 questions 整套题目数组。
  - npm test 通过，131 项 API 测试全部通过。
- 影响文件：`apps/api/src/practice-service.ts`、`apps/api/src/practice-service.test.ts`、`apps/api/src/practice-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`packages/shared/src/api.ts`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：取得合法登录态后补做独立刷题页桌面/390px 真实交互验收；性能优化代码已完成。

### `LOG-103` 2026-08-20 - 为章节题增加工作台选章刷题入口

- 任务：`PH6-13`
- 阶段/里程碑：第六阶段 / M6
- 动作：扩展工作台题库摘要返回章节 ID、标题和题数；在章节题卡片增加章节选择器，并将所选范围传给既有背题/检测会话；补充工作台服务回归、类型检查和 Web 构建。
- 结果：章节题可在课程复习工作台按章节启动背题或检测，也可保留整库刷题；真题和模拟题入口不变。
- 验证：
  - node --import tsx --test apps/api/src/review-workspace-service.test.ts 通过。
  - node --import tsx --test apps/api/src/review-workspace-http.test.ts 通过。
  - npm run typecheck 通过。
  - npm run build:web 通过；仅有既有 Vite 大 chunk 警告。
  - 全量 npm test 仍受 PH6-10/PH6-12 既有 Fake 数据库字段不同步影响，4 项刷题服务测试失败。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/review-workspace-service.ts`、`apps/api/src/review-workspace-service.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：章节进度、章节续读筛选和工作台大规模列表性能另立任务。

### `LOG-104` 2026-08-20 - 完成题目收藏与科目收藏题练习

- 任务：`PH6-12`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增题目收藏迁移、共享契约和收藏切换 API；将科目内跨题库收藏题保存为独立 subjectId 范围会话；在题目工作区、独立刷题页和课程复习工作台接入收藏操作、背题、检测和继续入口；同步 JSON 导出恢复，并补全服务、HTTP、工作台与数据治理回归。
- 结果：用户可收藏或取消收藏单题，并在每个科目题库分段对全部收藏题背题、检测或继续。收藏会话按创建时快照保存，不污染任何单题库的累计统计；本机数据库已执行 007 收藏迁移，复习页服务器 500 已恢复。
- 验证：
  - npm run db:migrate 通过，已执行 007_phase_6_question_favorites.sql。
  - npm run typecheck 通过。
  - npm test 通过，133 项 API 测试全部通过。
  - npm run build 通过；仅有既有 Vite 大 chunk 警告。
  - Playwright 临时无认证验收实例中，复习 > 题库工作台加载正常且无接口错误。
- 影响文件：`database/migrations/007_phase_6_question_favorites.sql`、`packages/shared/src/api.ts`、`apps/api/src/question-service.ts`、`apps/api/src/practice-service.ts`、`apps/api/src/review-workspace-service.ts`、`apps/api/src/data-governance-service.ts`、`apps/web/src/App.tsx`、`apps/web/src/api.ts`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：收藏夹分组、随机收藏练习和跨科目收藏练习继续按后续独立任务处理。

### `LOG-105` 2026-08-20 - 完成课程复习工作台 UI 重构

- 任务：`PH6-14`
- 阶段/里程碑：第六阶段 / M6、GA
- 动作：重组课程/科目选择、闪卡/题库分段、继续学习和科目分组列表；为选择 Sheet 补充展开状态、Esc 关闭和焦点回归；统一章节范围、收藏题与刷题操作区的响应式布局；修复移动端固定主导航的 44px 触控目标与正文安全区。
- 结果：复习工作台在桌面与 390px 视口均保持清晰的信息层级和无横向溢出。闪卡、题库、章节范围、收藏题与继续入口维持既有业务语义；移动端焦点不会被固定导航遮挡。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；仅保留既有 Vite 大 chunk 警告。
  - npm run progress:check 通过。
  - Playwright 1440px 与 390x844 受控工作台回归通过，闪卡、题库、收藏题和章节范围均无横向溢出。
  - Playwright 确认课程 Sheet 可用 Esc 关闭并将焦点还给触发控件；聚焦页面末尾操作不会被 53px 底部导航遮挡，控制台无运行时错误。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`、`output/playwright/ph6-14-workspace-desktop.png`、`output/playwright/ph6-14-workspace-mobile.png`、`output/playwright/ph6-14-workspace-questions-mobile.png`、`output/playwright/ph6-14-workspace-questions-mobile-bottom.png`、`output/playwright/ph6-14-workspace-mobile-nav.png`、`output/playwright/ph6-14-workspace-questions-mobile-dark.png`
- 下一步：大规模工作台虚拟化、跨端上下文冲突提示和真实 iPhone Safari 设备验收另立任务或沿用既有风险项。

### `LOG-106` 2026-08-20 - 冻结全局检索、错题巩固与学习洞察的 P1 范围

- 任务：`GOV-003`
- 阶段/里程碑：第七阶段 / M7
- 动作：将用户确认的三项能力写入产品定位、开发方案和设计规范；新增第七阶段、M7、三个业务模块、GOV-003 与 PH7-01 至 PH7-03 任务卡，并登记性能、统计语义和备份边界风险。
- 结果：三项能力已按全局检索、错题巩固、学习洞察的顺序拆分为独立任务。所有实现均以前置的 GA 发布和题库 P0 主链路稳定为条件；本步未改动应用、数据库或既有业务语义。
- 验证：
  - npm run progress:build 通过，展示层由结构化源重新生成。
  - npm run progress:check 通过，阶段、里程碑、模块、任务、风险和日志引用一致。
- 影响文件：`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`、`progress/config.json`、`progress/tasks/phase-7.json`、`progress/risks.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：GA 发布验收与题库 P0 主链路稳定后，按 PH7-01 实现全局检索；PH7-02、PH7-03 不提前启动。

### `LOG-107` 2026-08-20 - 完成跨课程全局检索与目标定位

- 任务：`PH7-01`
- 阶段/里程碑：第七阶段 / M7
- 动作：新增 MySQL ngram 全文索引、认证检索服务和共享契约；在复习页接入检索弹层、去抖、受控摘要、课程/科目/类型筛选、空状态、深链定位和桌面/移动端无障碍交互；补充服务、HTTP、性能 EXPLAIN 与浏览器回归。
- 结果：用户可跨课程检索有效资料、闪卡和题目；结果按内容类型分组并限制数量，资料、闪卡和题目可直接定位，题目刷新后仍会滚动并聚焦目标。390px、375px 横屏和深色模式均无横向溢出，筛选 Sheet 的触控行与焦点约束符合 UI 规范。
- 验证：
  - npm run db:migrate、npm run typecheck、npm run build 通过。
  - 全局检索服务与 HTTP 定向测试通过，三类 EXPLAIN 均命中 ngram 全文索引。
  - Playwright 1440px、390x844、844x375 与深色模式通过；控制台无新增错误。
  - 进度展示层由 npm run progress:build 生成，并由 npm run progress:check 校验。
- 影响文件：`database/migrations/008_phase_7_global_search.sql`、`packages/shared/src/api.ts`、`apps/api/src/global-search-service.ts`、`apps/api/src/global-search-service.test.ts`、`apps/api/src/global-search-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-7.json`、`progress/logs.json`
- 下一步：PH7-02 在题库 P0 主链路稳定后启动。

### `LOG-117` 2026-08-21 - 完成库洛米精致主题

- 任务：`PH5-27`
- 阶段/里程碑：第五阶段 / GA
- 动作：复用现有本机外观持久化与皮肤覆盖层，新增黑紫、薰衣草紫、莓果粉、暖黄 token 及静态几何环境装饰；系统化细化导航、按钮、输入、分段控件、容器、列表、Sheet、检索与刷题工作区，并在受控认证页面回归浅深色和手机宽度。
- 结果：库洛米主题不再只是配色切换：主控件采用黑紫实体层与莓粉细节，容器使用薰衣草边界和轻量内高光，背景只保留低对比度静态耳饰、蝴蝶结、骷髅与星芒；设置页可选并本机持久化，闪卡正文和公式未引入装饰或纹理。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；仅保留既有大体积 chunk 提示。
  - 终端 Playwright 受控认证态验证 1403px 设置页和 390x844 深色设置页：库洛米选项、显示模式、导航、容器与装饰均已渲染；390px 无横向溢出，深色背景/正文为 rgb(23, 18, 27)/rgb(247, 240, 248)。
  - 测试浏览器仅拦截 auth session 读取且未调用写接口；所有路由与本机主题偏好已在关闭前撤销。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`项目产品定位.md`、`项目开发方案.md`、`DESIGN.md`、`progress/tasks/phase-5.json`、`progress/risks.json`、`progress/logs.json`、`项目开发进度.md`、`当前执行看板.md`
- 下一步：更多角色主题、主题市场和自定义配色另立主题能力任务；真实 iPhone Safari 设备回归沿用既有 GA 发布验收。

### `LOG-118` 2026-08-21 - 补充只练未做的练习筛选

- 任务：`PH6-15`
- 阶段/里程碑：第六阶段 / M6
- 动作：扩展刷题创建选项和会话范围快照；题库、章节和科目收藏题入口增加“不区分 / 只练未做”分段控件。服务端在候选题查询阶段排除已有提交答案的题目，再执行既有题数截取和乱序；错题入口保持不展示且拒绝该筛选。
- 结果：只练未做与不区分均能创建可恢复的固定题目会话，刷新和续答不会重新筛选。未做定义为该题不存在任何已提交答案，已开始但未作答的会话不影响其未做状态。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，149 项 API 测试全部通过。
  - npm run build:web 通过；仅保留既有大体积 chunk 提示。
  - 开发服务 http://127.0.0.1:5173/ 返回 200；内置浏览器当前无法建立本机连接，认证态视觉回归仍沿用 RISK-021/RISK-028。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/practice-service.ts`、`apps/api/src/practice-service.test.ts`、`apps/api/src/practice-http.test.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：认证态真实设备视觉验收沿用 RISK-021/RISK-028；按掌握度、错误次数或时间间隔动态编排题目另立任务。

### `LOG-119` 2026-08-21 - 合并题数与乱序设置行

- 任务：`PH6-15`
- 阶段/里程碑：第六阶段 / M6
- 动作：将题数原生下拉框与乱序选项合并到同一设置行；乱序改为小尺寸原生勾选框，外层标签保留 44px 最小点击高度，并补齐桌面与移动端响应式对齐样式。
- 结果：题库详情和课程复习工作台的刷题设置均以“题数下拉框 + 乱序勾选项”呈现，乱序不再单独显示大号 Switch；作答筛选、会话参数和服务端语义保持不变。
- 验证：
  - npm run typecheck:web 通过。
  - npm run build:web 通过；仅保留既有 Vite 大体积 chunk 提示。
  - 未改变背默模式继续使用的 Switch 或刷题会话 API。
- 影响文件：`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`DESIGN.md`、`progress/tasks/phase-6.json`、`progress/logs.json`
- 下一步：PH6-15 继续沿用既有认证态真实设备视觉验收风险；间隔重复和进行中会话重新洗牌另立任务。

### `LOG-120` 2026-08-21 - 实现刷题与闪卡悬浮问答助手

- 任务：`PH6-16`
- 阶段/里程碑：第六阶段 / M6
- 动作：新增受控即时问答服务和认证 API；前端以默认收起的 Bot 悬浮入口接入闪卡与刷题页面，回答仅保存在当前页面会话。检测会话未完成时服务端仅发送题干和选项，并限制模型不得给出答案、选项、解析或作答结论。
- 结果：实现与自动化验证完成，刷题入口预留固定底部栏空间，面板支持 Esc 收起、停止请求、可访问名称与安全 Markdown 只读渲染。未取得合法认证态，真实桌面/390px 业务页面视觉验收按 RISK-028 保持待办，PH6-16 未提前完成。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，151 项 API 测试全部通过。
  - npm run build 通过；仅保留既有 Vite 大体积 chunk 提示。
  - Playwright 登录页启动检查和控制台检查通过；认证态工作区视觉验收受 RISK-028 阻断。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/study-assistant-service.ts`、`apps/api/src/study-assistant-service.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：取得合法认证态后补做桌面与 390px 的悬浮入口、展开问答、焦点和底部无覆盖验收，再收尾 PH6-16。

### `LOG-121` 2026-08-21 - 完成悬浮问答流式输出与一键复制

- 任务：`PH6-16`
- 阶段/里程碑：第六阶段 / M6
- 动作：将悬浮问答 Provider 请求改为 stream，并通过 SSE 向浏览器转发分段；前端逐段显示回答。为生成完成的回答增加带无障碍名称、悬停提示和成功确认的一键复制图标，生成中保持禁用。
- 结果：流式输出、网络分段拼接与检测模式内容隔离均已通过自动化验证；复制不会改变学习内容、聊天持久化、AI 讲解历史或作答数据。认证态真实工作区的桌面/390px 视觉与复制反馈验收仍受 RISK-028 阻塞，PH6-16 保持进行中。
- 验证：
  - npm run typecheck 通过。
  - npm test 通过，152 项 API 测试全部通过，覆盖 stream 标记、跨分段 SSE 拼接、认证后的 SSE 事件序列及检测题答案/解析隔离。
  - npm run build 通过；仅保留既有 Vite 大体积 chunk 提示。
- 影响文件：`packages/shared/src/api.ts`、`apps/api/src/study-assistant-service.ts`、`apps/api/src/study-assistant-service.test.ts`、`apps/api/src/study-assistant-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：取得合法认证态后补做桌面与 390px 的流式输出、复制反馈、悬浮入口、键盘焦点和底部无覆盖验收，再收尾 PH6-16。

### `LOG-122` 2026-08-21 - 收紧悬浮问答的真实流式输出与超时策略

- 任务：`PH6-16`
- 阶段/里程碑：第六阶段 / M6
- 动作：移除 Provider 完整 JSON 的兼容回退，要求 text/event-stream；接口增加 SSE 预热注释与防缓冲头，前端逐段按可见增量呈现。新增首字与流空闲双超时配置，持续输出不受总时长限制。
- 结果：非流式 Provider 现在会返回明确兼容性错误，不再整段显示为成功回答；定向测试确认首段可先于后段抵达客户端，首字后持续输出不会被原 30 秒总时长截断。默认超时为首字 30 秒、连续空闲 45 秒，可用 AI_STUDY_ASSISTANT_FIRST_TOKEN_TIMEOUT_SECONDS 与 AI_STUDY_ASSISTANT_STREAM_IDLE_TIMEOUT_SECONDS 调整。
- 验证：
  - npm run typecheck 通过。
  - node --import tsx --test apps/api/src/study-assistant-service.test.ts apps/api/src/study-assistant-http.test.ts 通过，6 项测试全部通过。
  - npm test 通过，155 项 API 测试全部通过。
  - npm run build、npm run progress:build 与 npm run progress:check 通过；仅保留既有 Vite 大体积 chunk 提示。
- 影响文件：`.env.example`、`apps/api/src/config.ts`、`apps/api/src/study-assistant-service.ts`、`apps/api/src/study-assistant-service.test.ts`、`apps/api/src/study-assistant-http.test.ts`、`apps/api/src/app.ts`、`apps/web/src/api.ts`、`apps/web/src/App.tsx`、`apps/web/src/styles.css`、`progress/tasks/phase-6.json`、`progress/risks.json`、`progress/logs.json`
- 下一步：启动本机 API 后在合法认证态以当前 Provider 完成一次真实流式问答；若返回不支持 stream，改用支持 SSE 的模型或服务，再补做桌面与 390px 验收。
