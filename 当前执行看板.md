# 当前执行看板

> 本文件由结构化进度源自动生成，请勿直接修改。

## 当前焦点

- 项目：知识闪卡复习工具
- 阶段：第五阶段：移动端与发布（`进行中`）
- 里程碑：`M5` M5 资料可组织（`进行中`）

## 进行中与阻塞

| 任务 | 标题 | 状态 | 验收标准 |
|---|---|---|---|
| `PH5-01` | 完成 iPhone Safari 响应式与 PWA 主屏幕安装 | `进行中` | iPhone 可添加到主屏幕，选词、高亮、表格、公式和底部安全区域通过验收。 |
| `PH5-02` | 接入 Cloudflare Tunnel 与 review.panspan.cloud | `进行中` | 项目脚本可启动/关闭 cloudflared，域名 HTTPS 稳定访问且凭证不入库。 |
| `PH5-05` | 完成 GA 全链路验收 | `进行中` | Windows 本机、iPhone、MySQL、Tunnel、备份恢复和账号安全完整通过发布验收。 |
| `PH5-09` | 修复 AI 讲解 Markdown 渲染 | `进行中` | AI 讲解中的 Markdown 标题、强调、列表、分隔线、表格、公式和安全链接以只读结构化内容显示，不出现 Markdown 标记原文；兼容模型常见的 `**“强调文本”**` 与 `*“强调文本”*` 行内强调写法。 |
| `PH5-10` | 新增 JSON 与 Excel 资料导入及模板下载 | `进行中` | 用户能在导入页选择 Markdown、JSON 或 Excel 单文件，下载 JSON/Excel 模板，并将符合模板的资料预览、校验、重复检测后应用为层级闪卡。 |
| `PH5-13` | 按资料续读并简化复习资料选择 | `进行中` | 每份资料独立保存并恢复最后一张闪卡；当前学习资料选择器仅显示资料名称，并决定顶部统计与快捷开始范围；复习页筛选只保留资料和掌握状态，不再提供章节、节或关键词选择。 |
| `PH5-14` | 在资料详情提供逐卡内容编辑与排序 | `进行中` | 资料详情按章、节展示独立闪卡编辑卡；用户可在卡内可视化编辑正文，表格支持空白单元格输入、加删行、相邻单元格横纵合并与拆分，并可在同一节内上移或下移，刷新后顺序和表格跨度保持一致。 |
| `PH5-25` | HTML 表格导入与格式优化诊断 | `进行中` | Markdown、ZIP 和内容级 JSON 正文中的安全 HTML 换行与表格可解析为结构化正文并正常阅读；脚本、样式、事件属性和其他未支持 HTML 继续被拒绝。 |

## 下一步

当前阶段没有待开始任务。

## 已确认待开工边界

当前没有已确认但尚未开始的任务边界。

## 当前风险

| ID | 类型 | 描述 | 当前处理 |
|---|---|---|---|
| `RISK-001` | 安全风险 | Cloudflare Tunnel 会把本机应用暴露到公网，必须保证账号门禁先于连接器开放。 | PH5-12 已恢复 PH5-04 单账号门禁和项目 Tunnel；未登录业务接口返回 401，review.panspan.cloud HTTPS 返回 200。任何本机免登录方案均不得绕过该公网门禁，恢复或调整后需重新执行 PH5-05 HTTPS 与账号安全回归。 |
| `RISK-002` | 运行依赖 | 不使用 Docker 且选择现有 MySQL 8.x，目标电脑的数据库服务、专用账号和备份工具必须持续可用。 | 启动脚本只检查连接，不启停 MySQL；PH1-02 提供 db:check 和 storage:check，真实迁移需使用最小权限专用账号和本机凭证完成验收；后续设置页继续提供可诊断的连接和路径测试。 |
| `RISK-003` | 数据一致性 | Markdown 只导入一次，后续数据库成为唯一真相源；若导出、备份或恢复不完整，用户无法从源文件重建学习状态。 | 正文使用结构化模型保存；PH4-03 已提供不改写源文件的 Markdown 内容导出和包含学习状态、资源内容且排除秘密的 JSON 恢复；PH4-04 已补齐每日数据库/资源备份、成功备份保留 7 份和恢复前留档，PH4-05 继续做完整演练。 |
| `RISK-004` | 交互复杂度 | 文本、表格和公式高亮在可视化编辑后可能发生定位漂移，导致遮罩范围错误。 | PH3-01 已按结构化内容节点路径和节点内范围保存文本高亮，公式按完整节点保存，不使用整篇纯字符偏移；PH3-03 已在事务中保留内容完全未变节点的高亮，并删除正文、公式或损坏锚点导致的无法精确定位项，且已通过服务、HTTP 与真实 MySQL 回归。PH3-06 继续覆盖标注与编辑组合回归。 |
| `RISK-005` | 兼容性风险 | iPhone Safari 的长按选词、浮动工具栏、PWA 安全区域和公式滚动行为与桌面浏览器不同。 | 已补齐 viewport-fit、iPhone PWA 元数据、主屏幕图标、静态资源 Service Worker 和带 safe-area 的底部 Tab Bar；移动端不使用左右滑动切卡，表格/公式/选词已在 390x844 视口验证无横向溢出。真实 iPhone Safari 的长按选词、浮动工具栏和 Home Indicator 仍需 PH5-01 设备验收。 |
| `RISK-006` | 外部服务 | AI Provider 的接口格式、模型可用性、速率限制和费用可能变化。 | PH4-01 已在后端完成多套配置、模型选择和 API Key AES-256-GCM 加密存储，配置读取永不返回密钥；PH4-02 已完成 OpenAI 兼容请求适配、连接测试、超时与上游错误隔离，失败不会写入或改变正文、标注和复习状态。真实 Provider 的可用模型、速率限制和费用仍需在 PH4-05 及发布前验收。 |
| `RISK-007` | 数据库兼容性 | utf8mb4 下过长的唯一索引会超过 MySQL 3072 字节上限，导致初始迁移中途失败并留下部分表。 | 已将 resources.relative_path 的唯一索引字段收敛为 VARCHAR(512)，清理本次半成品数据库后重新迁移成功；后续新增索引必须按 utf8mb4 字节上限做迁移测试。 |
| `RISK-008` | 范围与安全风险 | 本机启停脚本已经集成 cloudflared tunnel，连接器开放顺序必须继续受账号门禁约束。 | PH5-12 已恢复本机 PUBLIC_ACCESS_READY、CLOUDFLARED_ENABLED 和 PH5-04 单账号门禁，项目 Tunnel 正在受管运行。后续调整必须保持门禁先于连接器的顺序，并按 PH5-05 完成 HTTPS 与账号安全回归。 |
| `RISK-009` | 导入可靠性 | 导入预览令牌和待应用资源目前只保存在 API 进程内存中，API 重启或超过 30 分钟后预览会失效。 | 预览令牌限制 30 分钟和 20 份，应用前重新校验源文件哈希；PH1-06 已用真实 HTTP 验证取消和 API 重启后的失效提示。第一版继续接受进程内令牌边界，若需要跨重启恢复再改为受管暂存目录。 |
| `RISK-010` | 内容保真风险 | 导入预览的正文修正使用纯文本表单；用户修改含公式、表格或图片的正文时，当前实现会将该卡片降级为纯文本段落。 | 导入预览中的即时修正仍按纯文本应用；资料应用后可在 PH3-03 的结构化可视编辑器中无损修改正文、表格、公式和受管图片，不再依赖 Markdown 源码。 |
| `RISK-011` | 同步一致性 | 编辑锁采用 90 秒租约；设备离线、浏览器崩溃或未执行释放时，另一设备需要等待租约过期才能编辑。同一浏览器资料中的多个标签页共享设备标识，视为同一设备。 | 浏览器每 30 秒续期，并在保存、取消、返回、切卡和卸载时主动释放；服务端用 MySQL 当前时间校验并清理过期锁，正文保存必须同时匹配设备标识和随机令牌。 |
| `RISK-012` | 数据一致性风险 | 恢复流程先提交数据库事务，再写入资源文件；若恢复后的资源目录发生磁盘写入失败，数据库和资源目录可能短暂不一致。 | 恢复前强制生成当前数据留档，恢复文件先校验资源哈希和路径，写入失败不删除旧资源；PH4-05 已通过真实 MySQL、资源目录和 JSON/备份恢复正常路径验收，磁盘写入异常下的事务补偿仍作为后续治理项。 |
| `RISK-013` | 外部配置依赖 | PH5-02 的真实 HTTPS 验收依赖 Cloudflare Tunnel 连接器和 review.panspan.cloud 边缘连接状态；DNS 与凭证已配置，但公网连接器暂不能在 PH5-04 前启动。 | 已创建 review-panspan-cloud Tunnel 并将 review.panspan.cloud DNS 路由到新 Tunnel；凭证只保存在用户目录，项目 config.yml 和 .env.local 被忽略。PH5-04 已完成且 PUBLIC_ACCESS_READY=true；PH5-05 已修复一键启动补拉逻辑并验证 review.panspan.cloud 返回 HTTP 200，后续继续执行其余 GA 回归。 |
| `RISK-014` | 体验范围变更 | 全局秋日枫叶皮肤与 AI 真实取消同时触及所有前端页面、可访问性状态和 Provider 请求生命周期；若主题侵入正文或取消只停在浏览器端，会造成阅读干扰或后台继续消耗请求。 | PH5-06 已完成：叶片装饰仅存在于环境层，正文维持纯净高对比；停止操作已将取消信号传递至 Provider，服务和 HTTP 测试确认取消后不写入半成品，桌面与 390x844 浏览器验收确认深浅主题、减少动态效果规则和无横向溢出。 |
| `RISK-015` | 范围与数据一致性风险 | 课程/科目目录、资料封面和掌握状态历史会扩展资料主数据、资源生命周期、导入归属和 JSON 恢复边界；若迁移、软删除、缩略图或旧备份兼容不完整，可能造成资料丢失、分类错位或趋势失真。 | PH5-07 已完成数据库迁移、目录事务 API、逐层 URL 导航、封面管理和状态趋势。完整 JSON、手动备份和每日备份均会保存课程、科目、排序、资料归属、封面关联及状态历史；恢复在同一事务中按依赖顺序写入，旧 JSON 自动生成不可删除的“待整理 / 待整理”默认目录。恢复继续复用资源路径、哈希和敏感字段校验，单测已验证封面原图/缩略图实际写回，真实 MySQL 备份回归已确认新增元数据和资源归档。残余风险仅是恢复提交后磁盘写入失败的既有补偿边界，归属 RISK-012。 |
| `RISK-016` | 测试隔离风险 | M4 数据治理集成测试的备份断言会读取开发库当前所有受管资源；开发库已有资料时，夹具资源会混入 manifest，导致与固定预期不一致。 | PH5-07 已使用不依赖全库资源清单的真实复习集成回归确认旧资料写入兼容。本次不改动既有 M4 测试；后续数据治理回归应为夹具使用独立资源目录或将备份范围限定为夹具数据。 |
| `RISK-017` | 体验范围变更 | 手动明暗模式会覆盖浏览器系统偏好；若 CSS 覆盖优先级不完整，皮肤、表单和阅读正文可能出现混合明暗色或可读性下降。 | PH5-08 以根元素 data-color-mode 统一控制深浅 token，并将跟随系统保留为默认项；2026-08-19 已使用终端 Playwright 覆盖简约与秋日枫叶两套皮肤、跟随系统/浅色/深色三档切换、刷新持久化及 390x844/1440x900 无横向溢出。后续新皮肤或跨设备偏好需单独回归深浅 token。 |
| `RISK-018` | 工程回归阻塞 | CatalogPanel 的 route 参数排除 manage 分支，但父组件直接传入完整 MaterialsRoute，导致 App.tsx:3217 的 TypeScript 检查失败并阻塞全量前端验收。 | PH5-07 已通过将 manage 分支从 CatalogPanel 入参显式缩窄来收敛路由类型；npm run typecheck、npm run build 与 npm test 均已通过。后续显示模式任务可基于该通过状态独立执行浏览器回归。 |
| `RISK-019` | 内容安全与可读性风险 | AI Provider 返回 Markdown 文本；若复习页直接显示字符串，会暴露 Markdown 标记并破坏讲解可读性，若直接注入 HTML 则可能扩大不可信响应的攻击面。 | PH5-09 使用 remark 的 Markdown/GFM/数学解析结果复用现有只读节点渲染，安全链接规则保持不变，原始 HTML 和未受管图片均不渲染；另将模型常见的 `**“强调文本”**` 与 `*“强调文本”*` 从未解析文本节点补为强调节点，待浏览器自动化环境恢复后补齐页面元素回归。 |
| `RISK-020` | 导入协议与安全风险 | JSON 与 Excel 内容导入扩展了不受信任输入边界；JSON 高亮还需把外部文本或公式匹配为结构化正文锚点。若将完整备份、资源路径或学习状态误当成普通资料导入，或接受歧义匹配，可能覆盖既有数据、产生错误挖空或扩大秘密泄露面。 | PH5-10 固定为单资料内容协议：JSON 必须匹配 format/version 与层级字段，Excel 只读取固定工作表和五列；正文复用 Markdown 安全解析与既有预览、重复哈希和应用事务。PH5-20 将文本主协议改为 body 内 [[hl:原文]]：解析后移除标记并按所在位置生成锚点，避免重复文本 occurrence 推算；PH5-21 已修复内部令牌触发表格预校验的问题，单个 GFM 表格单元格内的普通文本标记可正常导入，跨单元格或 Markdown 节点仍阻止。正文修改后只有唯一重新定位才能应用。highlights 数组继续兼容 formula 及旧 text/occurrence 文件，完整 JSON 恢复仍只能经数据治理确认流程执行，资源、学习状态、课程科目归属与密钥不进入普通资料导入。 |
| `RISK-021` | 验收环境阻塞 | Codex 内置浏览器连接初始化时出现 Cannot redefine property: process，无法执行资料详情页、AI 讲解页和 JSON/Excel 导入页的桌面/iPhone 浏览器交互与无横向溢出回归。 | PH5-07 已通过独立 Playwright 完成资料格与详情页的桌面/390px 回归，状态摘要、无横向溢出和 URL 返回均已验证。PH5-10 已以类型检查、生产构建和 70 项 API 测试覆盖 JSON/Excel 解析、模板下载、普通导入安全边界及既有 Markdown 回归。PH5-13 已使用独立 Playwright 的本地认证与 Dashboard 响应桩完成当前资料选择、统计切换和 390px 无横向溢出回归，不读取本机账户凭证或写入业务数据。PH5-24 的逐条优化格式按钮和 PH5-25 的 HTML 换行/表格解析均已通过类型、构建和 API 回归覆盖，但本轮内置浏览器仍以 Cannot redefine property: process 失败，未能补做实际页面验收。2026-08-19 通过 playwright-cli 在不读取本机账号凭据的旧本地服务上完成 390x844 复习页回归，确认无横向溢出、底部安全区布局和正文选词后的高亮按钮可用；仍不能替代真实登录态、iPhone Safari 原生选词浮动工具栏、主屏幕安装和 Home Indicator 验收。待运行时恢复或取得真实设备后，优先补测 AI 讲解、导入页、HTML 换行/表格阅读、资料详情逐卡编辑以及 PH5-01 的真实设备项。 |
| `RISK-022` | 运行与安全风险 | 为缩短本机状态查看而临时屏蔽账号密码登录；若同时启动公网 Tunnel，会绕过既定的单账号安全门禁。 | PH5-11 已因对外提供要求暂缓，PH5-12 已恢复认证配置和项目 Tunnel，并验证未登录业务接口返回 401、公网首页返回 200。本机免登录例外当前未启用，后续必须先确认隔离模型。 |
| `RISK-024` | 工程配置阻塞 | PH5-15 引用了 progress/config.json 模块清单中不存在的 catalog-navigation，导致进度生成和校验均无法执行。 | PH5-15 已改用既有的 review-experience 与 data-governance 模块标识，进度生成和校验恢复可执行；未新增目录导航模块。 |
| `RISK-023` | 工程回归阻塞 | 共享 Review API 已收敛为按资料续读与资料/掌握状态筛选，但复习页面仍读取章节、节、关键词和全局 continueCard 字段，导致全量前端 TypeScript 检查失败；服务端续读迁移 SQL 的 ON DUPLICATE KEY UPDATE 还使用未限定的 setting_value，复习面板请求返回 ER_NON_UNIQ_ERROR。 | PH5-13 已收敛 ReviewPanel、筛选 Sheet 和 App 状态到新共享 API，并限定续读迁移 SQL 的 setting_value 列来源；npm run typecheck、npm run build、npm test（76 项）和真实 MySQL 复习集成均已通过。PH5-14 的逐卡编辑浏览器回归仅余既有 RISK-021 验收环境限制。 |
| `RISK-025` | 内容兼容性风险 | 编辑器新增 rowSpan/colSpan 后，合并单元格不能无损映射回 GFM Markdown；若直接扩展导入协议或导出语义，可能造成用户以为导入导出可保留合并结构。 | PH5-14 已在闪卡编辑保存的结构化正文中支持合并单元格，并同步阅读态、备份恢复白名单和服务端校验；不修改 Markdown、JSON、Excel 或 HTML 导入协议。Markdown 导出是否以展开表格、保留 HTML 或提示降级，须单独评估后实施。 |
| `RISK-026` | 数据一致性与范围风险 | 题库引入可编辑题目、AI 多版本、进行中会话和不可变作答快照；若直接复用闪卡表或未先升级备份协议，编辑与恢复可能篡改历史统计或遗漏数据。 | 已冻结为独立题库领域模型：题库、题目、AI 版本、会话与快照不写入现有闪卡表。PH6-02 必须先完成迁移、version 2 完整备份和 version 1 恢复兼容的真实 MySQL 回归，后续导入、编辑和刷题任务不得绕过该契约。 |

## 最近完成

| 任务 | 标题 | 完成日期 |
|---|---|---|
| `PH5-08` | 补齐本机深浅色显示模式切换 | 2026-08-19 |
| `PH6-01` | 冻结题库领域协议与阶段拆分 | 2026-08-19 |
| `PH6-02` | 建立题库数据模型与备份恢复兼容 | 2026-08-19 |
| `PH6-03` | 实现题库 JSON/Excel 导入与模板 | 2026-08-19 |
| `PH6-04` | 实现课程科目下的题库目录管理 | 2026-08-19 |

## 最近日志

| 日志 | 日期 | 步骤 | 下一步 |
|---|---|---|---|
| `LOG-080` | 2026-08-13 | 移除 AI 修正高亮匹配拦截 | 继续观察真实 Provider 的格式修正输出；非 JSON 或格式问题未减少时仅在对应问题行提示失败，不自动重试或润色正文。 |
| `LOG-079` | 2026-08-13 | 修复导入 AI 响应未更新预览的问题 | 真实 Provider 仍返回非 JSON 或格式错误未减少时，优先查看对应问题行的失败原因；不自动润色正文。 |
| `LOG-078` | 2026-08-13 | 完成导入正文格式 AI 逐条并发修正 | 真实 Provider 的特定模型兼容和同一卡片并发冲突提示另立任务；本步不自动重试、不整合全部错误、不润色正文。 |
| `LOG-077` | 2026-08-13 | 降低深色模式闪卡高亮刺眼度 | 若真实设备仍存在特定屏幕的对比度或色觉适配问题，另立无障碍视觉校准任务。 |
| `LOG-072` | 2026-08-12 | 扩展 JSON 模板多章节示例 | 待浏览器验收环境恢复后，继续 PH5-10 的导入页格式切换和模板下载交互回归。 |

## 使用规则

- 真相源位于 `progress/`，不在本文件。
- 开工前先确认任务边界并写入任务卡。
- 完成后更新任务、风险/技术债和日志，再执行 `npm run progress:build`。
- 提交前执行 `npm run progress:check`。
